---
description: "我只是想到，单纯的看视频并没有什么用，我现在认为实践是检验真理的唯一标准。 先来看编译的过程： 实际的编译命令 shell make -C kernel/ -j33 CROSS_COMPILE/home/topeet/rk3588-linux/prebuilts/gcc/linux-x86/aarc"
cover: /img/cover/3.webp

title: RK内核镜像构建
date: 2024-08-11 17:17:03
categories:
  - 瑞芯微开发
link: 瑞芯微开发/15_rk 内核镜像构建
---

我只是想到，单纯的看视频并没有什么用，我现在认为实践是检验真理的唯一标准。

先来看编译的过程：

实际的编译命令

~~~shell
make -C kernel/ -j33 CROSS_COMPILE=/home/topeet/rk3588-linux/prebuilts/gcc/linux-x86/aarch64/gcc-arm-10.3-2021.07-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu- ARCH=arm64 rockchip_linux_defconfig rk3588_linux.config

make -C kernel/ -j33 CROSS_COMPILE=/home/topeet/rk3588-linux/prebuilts/gcc/linux-x86/aarch64/gcc-arm-10.3-2021.07-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu- ARCH=arm64 topeet-rk3588-linux.img
/home/topeet/rk3588-linux/device/rockchip/common/scripts/mk-fitimage.sh kernel/boot.img /home/topeet/rk3588-linux/kernel/boot.its kernel/arch/arm64/boot/Image


make -j33 CROSS_COMPILE=/home/topeet/rk3588-linux/prebuilts/gcc/linux-x86/aarch64/gcc-arm-10.3-2021.07-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu- ARCH=arm64
~~~



内核镜像的生成过程以及后续压缩成 `Image.gz` 的过程可以分为以下几个步骤：

### Image 生成过程

1. **配置内核**
   - 使用 `make menuconfig` 或其他配置工具选择适当的内核选项。
   - 配置文件通常保存为 `.config`。
2. **编译内核**
   - 运行 `make` 命令，开始编译内核。
   - 编译过程主要包括：
     - **预处理**：解析头文件和宏定义。
     - **编译**：将 C 语言代码编译为汇编。
     - **汇编**：将汇编代码转换为目标代码。
     - **链接**：将目标文件链接为单个内核镜像 `vmlinux`。
3. **生成 Image**
   - `vmlinux` 是一个未压缩的 ELF 格式文件。
   - 通过 `objcopy` 工具，将 `vmlinux` 转换为可加载的 `Image` 格式（通常是裸数据格式）。

### Image.gz 生成过程

1. **压缩内核**
   - 使用 `gzip` 工具压缩 `Image` 文件，生成 `Image.gz`。
   - 压缩后的文件更小，方便传输和存储。
2. **安装内核（可选）**
   - 将 `Image.gz` 复制到引导加载程序可访问的目录。
   - 更新引导加载程序配置以加载新的内核。

### 总结

- **Image** 是内核的未压缩版本，适合直接加载。
- **Image.gz** 是压缩版本，适合节省空间。

这种流程适用于大多数 Linux 内核的构建和压缩过程。具体步骤可能根据不同的硬件平台和配置选项有所调整。



wsl.exe --system -d Ubuntu-22.04 df -h /mnt/wslg/distro

列出已经安装的发行版镜像

```
wsl --list --verbose
```

![image-20240809211103188](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202408092111207.png)



注销Linux发行版

~~~shell
wsl --unregister <DistributionName>
~~~