---
title: U-Boot编译过程浅析
date: 2023-09-10 17:17:04
categories:
  - 瑞芯微开发
link: 02_瑞芯微/06 U-Boot编译过程浅析
---

## 一、U-Boot源代码获取

可以参考我之前输出的这篇文章：

> [[RK356x\] [Firefly-Linux] 10min带你获取、了解与编译U-Boot源代码](https://blog.csdn.net/Neutionwei/article/details/123462959)

切换成`linux_release_v1.2.3a`版本：

~~~shell
git remote update
git checkout -b rk356x/linux_release_v1.2.3a rk356x/linux_release_v1.2.3a
~~~

## 二、编译RK3568

RK356x 配置文件查看：
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159362.png)清除历史编译状态：

```
make clean
```

使用 `make.sh` 配置 `configs/rk3568_defconfig` 并编译：

```
./make.sh rk3568
```

## 三、编译日志分析

常用编译变量说明：

| 命令      | 描述                        |
| --------- | --------------------------- |
| `HOSTCC`  | PC 机 gcc 编译命令          |
| `HOSTCXX` | PC 机 g++ 编译命令          |
| `HOSTLD`  | PC 机 ld 链接命令           |
| `CC`      | 交叉工具链 gcc 编译命令     |
| `CPP`     | 交叉工具链 gcc -E 编译命令  |
| `LD`      | 交叉工具链 ld 链接命令      |
| `OBJCOPY` | 交叉工具链 objcopy 命令     |
| `OBJDUMP` | 交叉工具链 objdump 链接命令 |
| `DTC`     | 设备树编译命令 dtc          |
| `CHECK`   | 执行静态检查 sparse         |

摘自顶层`Makefile`：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159859.png)
注意：上图中的`cc`与`gcc`是同一个东西！

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159833.png)

编译日志主要分成以下几部分：

1. 配置文件生成
2. 工具目录编译
3. U-Boot核心代码交叉编译
4. U-Boot目标文件生成
5. 设备树编译并追加到U-Boot目标文件
6. TPL与SPL代码编译
7. TPL与SPL目标文件生成
8. 最终固件打包

### 3.1 配置文件生成

执行`make rk3568_defconfig -j8`命令，生成`.config`：
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159751.png)
执行`scripts/kconfig/conf --silentoldconfig Kconfig`，这个话主要是检查是否有新的配置项，这里是清除历史输出文件后进行编译，因此所有配置项都认为是新的！

在这个过程根据`config.h`文件配置`u-boot.cfg`、`spl/u-boot.cfg`、`	/u-boot.cfg`等文件，然后产生了各自的`autoconf.mk`文件。编译`sam-offsets.s`产生`u-boot.lds`链接脚本。

另外`include/generated/version_autogenerated.h`是描述版本信息的头文件。
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159822.png)

### 3.2 工具目录编译

对`tools`下的工具进行一系列编译：
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159815.png)

### 3.3 U-Boot核心代码交叉编译

编译完成`tools`目录后，开始交叉编译核心代码，我们会看到有非常多`build-in.o`，这个输出文件很有意思，它是该文件所处目录所有`*.o`文件的集合体，例如`arch/arm/cpu/built-in.o`，那它就是`arch/arm/cpu/`目录所有`*.o`文件的集合体！

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159840.png)
核心代码的编译过程主要涉及`arch`架构代码目录、`common`通用目录、`cmd`命令目录与`driver`驱动目录，当然还有`lib`公共库目录与`examles`例程目录，注意这个编译过程不是按照顺序编译打印的，这个因为前面使用`-j8`编译选项，这个选项的意思是打开8个线程并发编译：
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159970.png)![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159113.png)![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159108.png)

### 3.4 U-Boot目标文件生成

核心代码交叉编译完毕后链接之前所有的`built-in.o`文件，通过`objcopy`命令生成`u-boot-nodtb.bin`文件与`u-boot.sym`符号表，并且使用`relocate-rela`工具对`u-boot-nodtb.bin`静态reloc（静态`rela.dyn`修复）：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159124.png)

### 3.5 设备树编译并追加到U-Boot目标文件

接下来是编译设备树`dts`，并且产生`dt.dtb`（u-boot设备树`dtb`文件）、`dt-spl.dtb`（spl设备树`dtb`文件）、`dt-tpl.dtb`（tpl设备树`dtb`文件），并且把`dtb`文件追加到u-boot文件生成`u-boot.bin`文件：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159130.png)

### 3.6 TPL与SPL代码编译

`spl`是初始化DDR内存使用的，而`spl`相当于一个精简版u-boot，，只不过它的目的是加载u-boot固件，它们编译套路与U-Boot核心代码类似，注意的是它会把编译生成的`*.o`搬到`tpl`、`spl`目录，`tpl`、`spl`目录树的排布与U-Boot目录树一样：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159177.png)

### 3.7 TPL与SPL目标文件生成

`u-boot-spl.lds`是`spl`的链接脚本，`u-boot-spl.dtb`是`spl`的设备树`dtb`文件：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159221.png)
`u-boot-spl-nodtb.bin`是`spl`目标文件，同样地把设备树`dtb`文件追加进去并产生`u-boot-spl.bin`文件：
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159361.png)
`u-boot-tpl-nodtb.bin`是`tpl`目标文件，注意这里直接复制成`u-boot-tpl.bin`（没有追加设备树`dtb`文件）：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159367.png)

还有一点需要注意的是，`u-boot-tpl.bin`是不能烧录进RK356x的，这是因为`tpl`相关代码，RK官方并没有开源！我们需要使用`rkbin`的`ddr.bin`文件替换！

### 3.8 最终固件打包

首先通过`rkbin/RKTRUST/RK3568TRUST.ini`文件描述的内容把`u-boot.bin`打包成`u-boot.itb`，紧接着根据 FIT 描述文件的内容把 ATF、OP-TEE、U-Boot、MCU 打包到一起（当然也包括设备树 DTB）：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159407.png)![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159412.png)
![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159495.png)
最后生成的固件为`uboot.img`，并且根据`rkbin/RKBOOT/RK3568MINIALL.ini`文件生成`rk356x_spl_loader_v1.12.112.bin`（注意这里并没有打包我们编译产生的`u-boot-spl.bin`文件，而是打包存放于`rkbin`目录下的`spl.bin`文件）

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202309122159479.png)