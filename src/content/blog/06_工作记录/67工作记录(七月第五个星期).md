---
title: 工作记录(七月第四个星期)
date: 2025-07-21 10:26:56
categories:
  - 工作记录
link: 06_工作记录/67工作记录(七月第五个星期)
---

# 工作记录(星期一)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

不要提前焦虑，明天的问题有明天的我来处理。

今天甚至后面很多天的任务都会是写驱动手册，但需要注意的是不能再过多的关注群聊，我说的群聊是大号里面的，确实是不太好，分散了很多的精力，行了，开始今天的任务了。

____



该系统通过主电源与备用电池的切换，保障 RTC 模块在主电源（VCC3V3_SYS）断电时，由 CR1220 电池持续供电以维持时间信息。

工作原理：

- 系统正常时，VCC3V3_SYS 经二极管 D2 为 RTC 供电，D3 反向截止避免主电源消耗电池；

- 主电源失效时，D2 截止防止电池耗电，D3 导通，CR1220 为 RTC 供电确保其运行。



本章将介绍 RTC 的软件内容，具体包括 RTC 子系统框架、驱动源码分析以及 RX8010 驱动的移植方法。



上图中 RTC 子系统分为用户空间、设备驱动层和硬件层三个层次，其中设备驱动层包含 RTC 设备驱动和 RTC 核心层，各层功能如下：

用户空间是应用程序运行层，应用程序通过多种接口与系统交互，例如访问 /dev/xxx 设备节点读写 RTC 时间，或通过 sysfs、proc 文件系统获取 / 设置硬件状态。

设备驱动层是连接用户空间与硬件层的核心：

- RTC 设备驱动负责硬件操作细节，通过与 RTC 芯片直接通信实现控制，将硬件设备以 device（设备文件）和 driver（驱动程序）形式暴露给上层，其中 /dev/xxx 是用户交互接口，driver 模块承担具体硬件控制；

- RTC 核心层负责管理协调时间管理功能，确保系统正确读写时间，并保障时钟在断电时持续运行。

硬件层为实际的 RTC 硬件，通过 I2C、SPI 等硬件接口与设备驱动层通信，提供实际计时功能。

| I2C 接口 | pinctrl function | 网络标号            | 对应的 GPIO | 功能                         |
| -------- | ---------------- | ------------------- | ----------- | ---------------------------- |
| I2C0     | I2C0_SDA         | I2C0_SDA_PMIC       | GPIO0_B2    | 被核心板 PMIC 使用           |
|          | I2C0_SCL         | I2C0_SCL_PMIC       | GPIO0_B1    | 被核心板 PMIC 使用           |
| I2C1     | I2C1_SDA         | I2C1_SDA_TP         | GPIO0_B3    | 屏幕触摸使用                 |
|          | I2C1_SCL         | I2C1_SCL_TP         | GPIO0_B4    | 屏幕触摸使用                 |
| I2C2     | I2C2_SDA_M0      | PCIE20_WAKEN_M0     | GPIO0_B5    | PCIE2.0 唤醒功能             |
|          | I2C2_SCL_M0      | TP_RST_L_GPIO0_B6   | GPIO0_B6    | MIPI 屏幕复位                |
|          | I2C2_SDA_M1      | I2C2_SDA_M1         | GPIO4_B4    | MIPI 摄像头、LVDS 屏幕的触摸 |
|          | I2C2_SCL_M1      | I2C2_SCL_M1         | GPIO4_B5    | MIPI 摄像头、LVDS 屏幕的触摸 |
| I2C3     | I2C3_SDA_M0      | I2C3_SDA_M0         | GPIO1_A0    | 未使用                       |
|          | I2C3_SCL_M0      | I2C3_SCL_M0         | GPIO1_A1    | 未使用                       |
|          | I2C3_SDA_M1      | GPIO3_B6            | GPIO3_B6    | 未使用                       |
|          | I2C3_SCL_M1      | GPIO3_B5            | GPIO3_B5    | 未使用                       |
| I2C4     | I2C4_SDA_M0      | 5G_DISABLE_L        | GPIO4_B2    | 5G 使能                      |
|          | I2C4_SCL_M0      | ETH1_REFCLLO_25M_M1 | GPIO4_B3    | 网口时钟                     |
|          | I2C4_SDA_M1      | I2C4_SDA_M1         | GPIO2_B1    | 未使用                       |
|          | I2C4_SCL_M1      | I2C4_SCL_M1         | GPIO2_B2    | 未使用                       |
| I2C5     | I2C5_SDA_M0      | I2C5_SDA_M0         | GPIO3_B4    | RTC                          |
|          | I2C5_SCL_M0      | I2C5_SCL_M0         | GPIO3_B3    | RTC                          |
|          | I2C5_SDA_M1      | HDMITX_SDA          | GPIO4_D0    | HDMI 数据线                  |
|          | I2C5_SCL_M1      | HDMITX_SCL          | GPIO4_C7    | HDMI 时钟线                  |



本章将讲解 Linux 系统中与时间相关的 date 和 hwclock 命令。其中，date 用于查看和设置系统当前时间，hwclock 则负责访问和管理硬件时钟（RTC），二者共同支撑系统时间的运维。

____

RTC的第一遍终于是改完了，但肯定是具有很多的问题，目前来看的话不管是要改成什么样子，法师都会加入自己的想法，所以无论怎样都会挨批评，所以说做这件事情就是费力不讨好，并不会有什么收获啥的，只能尽可能地少挨骂，那具体应该怎样做呢，对于一些细节的认识还是不到位，就应该从前到后一点点的找法师都改了哪些地方，不管怎样改都必然是有迹可循的，现在能做的就是整理这些痕迹，然后分辨出来其中的规律，争取做到跟法师类似的想法和思考。

现在开始从前面开始重新看一下，从第一章开始。

`i2c_register_driver()` 函数中，`i2c_driver` 结构体的 `is_registered` 变量会在驱动注册成功后赋值为 `true`。I2C 核心框架通过 `i2c_init()` 函数初始化，该函数使用 `postcore_initcall` 宏指定初始化时机。

因此，只有 I2C 总线注册成功后，才能通过 `i2c_add_driver()`（宏封装 `i2c_register_driver()`）注册驱动：先将 `i2c_driver` 与 `i2c_bus_type` 绑定，再调用 `driver_register()` 函数，通过设备模型初始化 `i2c_driver` 内部的 `device_driver`（流程遵循设备模型标准）。

`driver_register()` 的调用链为 `bus_add_driver()->driver_attach()->bus_for_each_dev()`，会遍历 I2C 总线上的所有设备（即 `i2c_client`），并调用 `_driver_attach()` 函数判断 `i2c_driver` 与 `i2c_client` 是否匹配。匹配过程通过 `driver_match_device()` 调用总线的 `match` 函数（即 I2C 总线的 `match` 函数），该函数内部调用 `i2c_match_id()`，通过 `id_table` 与 `i2c_client` 匹配。

若匹配成功，流程回到 `_driver_attach()`，再按 `driver_probe_device()->really_probe()` 的顺序调用 I2C 总线的 `probe` 函数。I2C 总线的 `probe` 函数会通过 `to_i2c_driver()` 将 `device_driver` 转换为 `i2c_driver`，最终调用 `i2c_driver` 自身的 `probe` 函数。（本段需要背诵）

___

## 关注1：命令取消格式、空格、引号

![image-20250728162544364](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202507281625396.png)

## 关注2：章节名称下的内容介绍

![image-20250728163350840](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202507281633866.png)

感觉还是不太对，最好的办法其实就是亲自去录制一遍视频，但是录制一遍视频的付出又太大了，这是我所不能接受的，而且我想的是循序渐进，先完成一个再完成第二个，由浅入深，遇到问题再去解决问题。那现在要做的是什么呢，只能先跟着洪流走，但又不能太过从众，有自己的思考。行了，回到RTC了,明天的话就是ADC。

# 工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

我可以抛下所有的身份和责任，永远置顶自己的感受。

先来回顾一下昨天，昨天一天都在研究如何将手册写好，怎样才能仿写法师的修改，达到法师的水平短时间就不想了，其实并不是有多高，而是闻道有先后，术业有专攻罢了，如果方向置换，我未必就会做的差了，如是而已，行了，先继续昨天的内容进行修改了，看完一遍，再看第三遍，以三遍为终止。

___

行了，就先到这里了，然后开始IIO和ADC的修改，这两个章节也是我之前写的，我认为应该比改苹果写的好很多，

| 步骤                    | 具体过程                                                     |
| ----------------------- | ------------------------------------------------------------ |
| 1. 信号输入与暂存       | 模拟输入信号 U1 进入 ADC，由采样电路暂存                     |
| 2. SAR 初始化与转换启动 | 逐次逼近寄存器（SAR）初始化，通过置数选择逻辑电路，按二分法逐步确定数字输出值 |
| 3. 逐位比较与确定       | - SAR 从最高位开始假设为 1，将当前 N 位数字值传递给 D/A 转换器 - DAC 将数字值转换为模拟电压 U0，送入比较器与 U1 比较 - 若 U0 < U1，当前位保持 1；否则改为 0 - 重复上述过程，直至所有 N 位确定 |
| 4. 结果输出             | SAR 输出确定的 N 位数字值                                    |

# 工作记录(星期三)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

我不再执着于满足他人，而是去探索自己的路。

昨天的话看完了ADC章节，上午的话就是看两遍

IIO 子系统是管理 ADC、陀螺仪、加速度计等模拟设备与传感器的核心框架。作为 IIO 子系统的重要组成部分，ADC 的操作依托于 IIO 提供的机制，用户可通过 sysfs 接口或用户空间 API 实现对 ADC 数据的访问。本章将详细讲解操作 ADC 的具体步骤。

___

行了，开始IIO的查看，ADC就到这里了，感觉这个IIO应该不是很简单，

IIO总共是有五章，这次就涉及到很多的代码分析了，并且没有那些好写的实际例程了，终于要开始上强度了，行了，开始看了，第一次看不奢望改太多，先把代码和图的标号加上，粗略的看一遍，第二遍开始改内容，第三遍检查内容，第四遍开始最终检查。

IIO（Industrial Input/Output，工业输入 / 输出）是 Linux 内核中专门用于管理模数转换器（ADC）、数模转换器（DAC）及各类工业传感器的子系统。



该子系统的开发始于 2009 年。随着嵌入式系统与物联网（IoT）技术的兴起，越来越多设备需要与模拟或数字信号交互，典型场景包括：



- 模数转换（ADC）：将温度、压力、光强度等模拟信号转换为数字信号；
- 数模转换（DAC）：将数字信号转换为模拟信号（如控制电机转速、调节音量等）；
- 其他传感器：如加速度计、陀螺仪、磁力计等。



这些设备功能各异，但均需与操作系统进行交互。若缺乏统一框架，每个设备都需单独开发驱动程序，会造成代码冗余、维护成本激增。为此，Linux 社区引入了 IIO 子系统，以实现对这类设备的标准化管理。

IIO 的特点如下：

- **统一接口**：IIO 提供标准化 API 与数据结构，使不同类型设备可通过相同接口交互。用户空间应用程序能借助标准文件系统接口（/sys/bus/iio/devices/）访问 IIO 设备，实现对传感器、转换器等硬件的统一管理与操作。

- **多设备类型支持**：IIO 子系统兼容多种设备，涵盖 ADC（模数转换器）、DAC（数模转换器）、温度传感器、光传感器、压力传感器，以及加速度计、陀螺仪等。广泛的设备适配性，使其成为处理工业及消费级传感器的理想方案。

- **事件与触发机制**：IIO 支持基于事件的触发机制（如传感器数值超阈值时触发中断），同时兼容硬件触发与软件触发。这一特性适用于实时数据采集场景，可满足时间敏感型应用的需求。





32-149 行：通过 API 从设备树获取硬件属性，配置硬件资源。

152-160 行：初始化 IIO 设备核心信息，包括设置设备属性、操作函数集、工作模式，以及配置通道列表和数量，为后续操作打基础。

第 184 行：用 devm_iio_device_register 函数注册 iio 设备。至此，saradc 的 probe 函数讲解结束，下一节详细讲该注册函数。



重磅！RK3568 视频教程《Linux 系统编程（重制版）》后半部分现已更新完毕！

本次更新聚焦实战核心，依次深入讲解 I2C、SPI、Socket 及 CAN 的应用编程。课程延续 “理论 — 工具 — 实验” 三维教学法，通过核心知识精讲夯实基础，结合开发工具实操演示直观教学，再以典型场景实验验证巩固所学，助力大家构建系统化知识体系，高效提升实战编程能力。

即刻开启学习，解锁 Linux 系统编程新技能！







当然可以，我根据你的内容重新设计一版推文文案，既突出技术干货，又兼顾传播力，适合用于微信公众号、B站动态、社群推送等场景。以下是三个不同风格的版本，你可以根据使用平台和偏好选择：

------

### ✅ **版本一：官方专业风格（适合公众号/培训机构）**

🎬 **《Linux系统编程（重制版）》课程更新！**

本次更新内容聚焦四大核心模块：**I2C、SPI、Socket、CAN**，围绕接口通信与网络编程，深入剖析在RK3568平台上的实际应用开发。

课程延续“三维教学法”——**理论讲解 + 工具演示 + 场景实验**，帮助学习者构建系统化知识结构，全面提升嵌入式实战能力。

📌 适用人群：嵌入式开发者、Linux系统工程师、在校技术类学生
 📌 教学平台：RK3568 + Linux
 📌 视频教程持续更新中，欢迎关注！

------

### 🚀 **版本二：技术极客风格（适合B站/知乎/技术社区）**

RK3568 教程更新！
 这次我们把 **I2C / SPI / Socket / CAN** 都讲透了！

不止讲原理，还带你动手开发，工具演示、实验验证一个不少。
 通信接口 + 网络编程，一次掌握 Linux 系统编程的关键能力！

📦 理论 × 工具 × 实验 = 真正能用的技术
 📍 适合所有想搞懂底层驱动 & 应用编程的朋友

------

### 🎯 **版本三：简洁传播风格（适合社群、朋友圈、标题党）**

📢 Linux系统编程重制版更新！

I2C、SPI、Socket、CAN 四大模块正式上线。
 理论 + 工具 + 实验三位一体教学，讲清楚也带你做！

一站式掌握 RK3568 平台通信编程核心技能
 👉 视频已更新，欢迎查收！

------

如需配套封面标题，我也可以帮你起，比如：

- “掌控通信接口：I2C/SPI/Socket/CAN全解析！”
- “RK3568 系统编程教程更新，带你进阶应用开发”
- “这才是实战派 Linux 教程：接口通信全覆盖”

需要的话我也可以帮你写一个视频开头的旁白或字幕文案。你希望哪一版更贴近你的需求？









📢 Linux系统编程(重制版)更新！==直接说明更新的是啥==

补全了I2C、SPI、Socket以及CAN模块的应用编程。教程采用"理论→工具→实验"三维教学法，通过核心知识精讲+开发工具实操演示+典型场景实验验证，帮助大家建立系统化知识体系，提升实战能力。





明白了！你希望侧重微信公众号的推文风格，内容可以更**丰富、详尽、逻辑清晰**，适合读者**阅读全文、全面了解更新点和学习价值**。以下我为你整理了多个版本，都是针对你本次 **I2C、SPI、Socket、CAN 四个模块的更新**，每一条都是微信公众号风格的内容推文模板，你可以直接使用或根据需要进行组合改写：

------

### 📌 **版本一：正式说明 + 教学亮点清晰列出**

标题：

> 《Linux系统编程（重制版）》完结！I2C、SPI、Socket、CAN四大通信模块更新

正文内容：

《Linux系统编程（重制版）》教程迎来完结，本次新增四个关键通信模块的应用编程内容，覆盖：

- ✅ I2C 应用编程
- ✅ SPI  应用编程
- ✅ Socket 应用编程
- ✅ CAN  应用编程

课程继续采用“原理讲解**→**工具实操**→**代码编程实战”这一层层递进的讲解方法，建立从知识理解到能力落地的完整闭环。

《Linux系统编程（重制版）》教程完整内容大纲如下

![image-20250730163058830](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202507301630874.png)



从结构体成员可推断：IIO 子系统通过 FIFO 缓存事件数据，用等待队列唤醒监听者，以链表维护事件属性并经 sysfs 供用户配置监控，借助互斥锁保障多线程安全，凭标志位记录接口状态，从而高效完成事件的检测、配置、存储、通知与处理。

继续分析 iio_device_register_eventset 函数 18 行后的代码，其核心是为 IIO 设备初始化事件接口：先初始化事件接口的设备属性链表与中断处理函数；再统计预定义事件属性数量，尝试添加动态事件属性；随后分配内存存储所有事件属性并整合到事件组；最后将事件组加入设备的属性组列表，以此完成事件接口的注册与配置。

# 工作记录(星期四)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

___

我可以抛下所有的身份和责任，永远置顶自己的感受

今天的任务呢就是先看一下3562的这个usb问题，然后继续改驱动手册，驱动手册这个东西呢，感觉有些想法可以阐述一下，先按照顺序来吧。

echo host > /sys/devices/platform/ff740000.usb2-phy/otg_mode



echo host > /sys/kernel/debug/usb/fe500000.usb/mode

echo host > /sys/devices/platform/ff740000.usb2-phy/otg_mode

echo otg > /sys/devices/platform/ff740000.usb2-phy/otg_mode

echo otg > /sys/kernel/debug/usb/fe500000.usb/mode

cat /sys/devices/platform/ff740000.usb2-phy/otg_mode

cat /sys/kernel/debug/usb/fe500000.usb/mode

```
 [  133.353081] dwc3 fe500000.usb: request ffffff8013938400 was not queued to ep0out  
```



~~~c
/usr/bin/usbdevice: 129: [: =: argument expected
/usr/bin/usbdevice: 133: echo: echo: I/O error
[2024-06-18 04:32:37] Starting instance: ffs.adb
~~~

目前的现象是这样的，唯有在ADB的情况下切换拨码开关才是有效果的，如果上电的时候直接就是host模式，这肯定是不行的，所以就应该一直保持adb的模式，然后什么时候需要用u盘来测试了，就输入命令来进行模式的切换

____



~~~c
&usbdrd3_0 {
	status = "okay";
};

&usbdrd_dwc3_0 {
	status = "okay";
	dr_mode = "otg";
    phys = <&u2phy0_otg>;
    phy-names = "usb2-phy";
    maximum-speed = "high-speed";
    extcon = <&u2phy0>;
};

&u2phy0 {
	status = "okay";
};

&u2phy0_otg {
	status = "okay";
};
&usbdp_phy0 {
	status = "disabled";
};

&usbdp_phy0_dp {
	status = "disabled";
};

&usbdp_phy0_u3 {
	status = "disabled";
};



&usbdrd3_0 {
	status = "okay";
};

&usbdrd_dwc3_0 {
	status = "okay";
	dr_mode = "otg";
    extcon = <&u2phy0>;
};

&u2phy0 {
	status = "okay";
};

&u2phy0_otg {
	status = "okay";
};
&usbdp_phy0 {
	status = "okay";
};

&usbdp_phy0_dp {
	status = "okay";
};

&usbdp_phy0_u3 {
	status = "disabled";
};
~~~



~~~c
repo init --repo-url https://gerrit.rock-chips.com:8443/repo-release/tools/repo -u https://gerrit.rock-chips.com:8443/linux/rockchip/platform/manifests -b rk3562 -m rk3562_linux_release.xml
~~~



~~~c
core.o     drd.o        dwc3-of-simple.o      ep0.o     host.o
debugfs.o  dwc3-haps.o  dwc3-rockchip-inno.o  gadget.o  trace.o
~~~



~~~c
gadget.c  gadget.h
core.c core.h
debug.h    debugfs.c
ep0.c dwc3-rockchip-inno.c    host.c
~~~

# 工作记录(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

我不在执着于满足他人，而是去探索自己的路

今天终于是又一次技术支持了，吸取昨天的问题以及上一次技术支持的问题。

___

上午做的倒是挺好的，

该函数的返回值为文件描述符 fd，由第 24 行的 anon_inode_getfd 函数创建并返回。anon_inode_getfd 的作用是生成匿名文件描述符 —— 这种特殊的 fd 不与实际文件系统路径或设备节点关联，而是内核动态创建的虚拟文件。匿名 fd 拥有独立的文件操作集，通过 ioctl 可借助它创建虚拟接口，让用户无需依赖传统文件系统，即可与内核中的特定功能或数据进行交互。



第 36-40 行代码根据文件打开模式（阻塞 / 非阻塞）及缓冲区状态，动态确定需等待的数据量：非阻塞模式下，to_wait 直接设为 0（不等待，立即返回）；阻塞模式下，则取用户请求字节数对应的单元数量与缓冲区 rb->watermark 的较小值，确保等待数据量既满足需求又不超过缓冲区性能限制。



45-71 行的 do-while 循环流程如下：先检查设备有效性，再判断缓冲区数据是否充足。若数据不足，阻塞模式会进入等待状态（直至数据满足或被信号中断），非阻塞模式则直接返回资源不可用错误；等待中可响应信号并处理中断，且通过循环保证条件满足时重试读取。当缓冲区数据充足，调用 rb->access->read_first_n (rb, n, buf)，按用户请求字节数 n 从缓冲区 rb 读取数据至用户空间 buf，并返回实际读取字节数。



该结构体定义了一组函数指针和标志位，构成 IIO 缓冲区的操作接口。通过这些接口，IIO 子系统可灵活管理缓冲区的存储、读取、配置及生命周期，同时支持多种工作模式与功能扩展。其中，read_first_n 读函数会在 iio_buffer_read_first_n_outer 函数中被实际调用。



该函数首先从文件结构中获取设备及缓冲区信息并验证有效性，随后通过 poll_wait 将当前文件描述符加入等待队列（确保数据到达时能被唤醒），最后调用 iio_buffer_ready 检查缓冲区是否满足读取条件（如达到水印值）。若满足，返回 EPOLLIN | EPOLLRDNORM 表示数据可读；否则返回 0 表示无事件。这种机制支持高效异步 I/O，可让用户空间程序在数据可用时及时响应。

# 工作记录(星期六)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

___

==我不再背负他人情绪，不再成为他人投射痛苦的容器==

今天的任务除了技术支持就只有写手册了，其他的倒是没有了。需要注意的就只有25分钟法则了，行了，开始工作。

至此，虚拟 fd 的文件操作集函数已讲解完毕，且验证了上一章节末尾关于 IIO 子系统工作流程的猜想。若需支持多个系统调用，可通过 ioctl 函数创建匿名文件描述符，并为其绑定独立的文件操作集。这种方式既能扩展功能，又因匿名文件描述符不会在文件系统留下痕迹，可避免污染文件系统，从而保持系统的整洁与高效。
