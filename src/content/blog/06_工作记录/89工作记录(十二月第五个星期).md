---
title: 工作记录(十二月第五个星期)
date: 2025-12-29 10:26:56
categories:
  - 工作记录
link: 06_工作记录/89工作记录(十二月第五个星期)
---

# 工作记录(星期一)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

今天的任务就是继续更新源码，先把RK3588 邮票孔的还有3588S的镜像和源码更新一下。

> 1.rkbin
>
> 2.内核源码除了设备树
>
> 3.buildroot board
>
> 4.ubuntu debian
>
> 5.phy驱动

# 工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

____

今天的任务就是继续更新源码，昨天呢是将rk3588的更新完成了，今天的话就继续更新RK3568的，RK3568的源码要更新的有两个，一个是4.19 一个是5.10，先改完5.10的再说，4.19最后再更新即可。

~~~shell
# x64
curl -L https://gitee.com/RubyMetric/chsrc/releases/download/pre/chsrc-x64-linux -o chsrc; chmod +x ./chsrc

# aarch64
curl -L https://gitee.com/RubyMetric/chsrc/releases/download/pre/chsrc-aarch64-linux -o chsrc; chmod +x ./chsrc

# riscv64
curl -L https://gitee.com/RubyMetric/chsrc/releases/download/pre/chsrc-riscv64-linux -o chsrc; chmod +x ./chsrc

# armv7
curl -L https://gitee.com/RubyMetric/chsrc/releases/download/pre/chsrc-armv7-linux -o chsrc; chmod +x ./chsrc
~~~





~~~shell
# Debian Buster 归档源（官方+国内镜像）
deb http://archive.debian.org/debian/ buster main contrib non-free
deb http://archive.debian.org/debian/ buster-updates main contrib non-free
deb http://archive.debian.org/debian-security/ buster/updates main contrib non-free
deb http://archive.debian.org/debian/ buster-backports main contrib non-free
~~~





| 信号名称 | 描述                                                        | 方向    |
| -------- | ----------------------------------------------------------- | ------- |
| TXC      | 发送时钟信号，频率为 125 MHz（1000 Mbit/s 速率下）          | MAC→PHY |
| TX_CTL   | 发送通道控制信号，用于控制发送数据和状态（如 TX_EN，TX_ER） | MAC→PHY |
| TXD[3:0] | 发送通道数据信号，4 位并行数据，在时钟上升沿和下降沿传输    | MAC→PHY |
| RXC      | 接收时钟信号，频率为 125 MHz（1000 Mbit/s 速率下）          | PHY→MAC |
| RX_CTL   | 接收通道控制信号，用于指示接收数据和状态（如 RX_DV，RX_ER） | PHY→MAC |
| RXD[3:0] | 接收通道数据信号，4 位并行数据，在时钟上升沿和下降沿传输    | PHY→MAC |

# 工作记录(星期三)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

___

今天的任务是继续更新源码，然后修改一下驱动的手册，先改手册

| 信号名称 | 描述                                                         |
| -------- | ------------------------------------------------------------ |
| MDIO     | 数据线，用于 MAC 层与 PHY 层之间传输配置、管理数据（如 PHY 地址配置、速率协商参数、状态查询等） |
| MDC      | 时钟线，为 MDIO 数据传输提供同步时钟，频率通常在 1MHz~2.5MHz（需匹配 PHY 芯片要求） |

**Streamlining Explanations**

I've been working on simplifying the introductions for Sections 3 and 4 of Chapter 19. My goal is to match the style and brevity of the rest of the chapter, particularly the existing introductions. I'm focusing on making them more accessible while retaining the core technical information about the device driver's functional and network layers.

**Refining Network Layers**

I'm now revising Sections 1 and 2 to streamline the presentation and focus on the practical implications for the user. I've integrated `dev_queue_xmit`, `net_if_rx`, `sk_buff`, `struct net_device`, and `struct net_device_ops`, focusing on the interface aspects. I'm also including information on `net_device_ops` implementation, and the interplay of interrupt handling with hardware register setup, TX/RX, and hardware state management.

**Detailing Physical Connections**

I'm currently focused on the PHY layer. I'm exploring how it handles signal conversions, the mediums it utilizes (cables, waves), and the crucial MII/RGMII interfaces for connection. The goal is to keep it concise, ensuring each section has about 2-3 short paragraphs, while maintaining consistent terminology, especially concerning the references to "MAC," "PHY," and "RGMII" that appear in previous sections.

根据你提供的完整章节内容，我为你优化了第3和第4小节的介绍。这两部分在保持专业性的同时，字数和逻辑深度调整得与前两小节（协议接口层、设备接口层）非常对齐。

以下是为你撰写的补充内容：

------

### 3. 设备驱动功能层

设备驱动功能层是网络驱动框架中负责具体硬件操作的核心层。它向上对接网络设备接口层定义的net_device_ops回调函数，向下直接控制硬件寄存器。驱动开发者的主要工作几乎都集中在这一层。

其核心任务是将内核的通用请求转化为底层硬件的具体指令。例如，在发送数据时，该层负责将sk_buff中的数据映射到 DMA 缓冲区并触发硬件发送开关；在接收数据时，从硬件 FIFO 或内存中提取原始帧并封装为sk_buff递交给上层。此外，该层还负责硬件中断的申请、时钟管理以及统计数据的维护，是驱动程序中逻辑最复杂、与硬件耦合度最高的部分。

### 4. 网络设备与媒介层

网络设备与媒介层是整个网络子系统的物理基石，对应于 OSI 模型中的物理层（PHY）和数据链路层（MAC）。它由具体的硬件控制器、物理接口芯片以及传输介质组成，负责将数字信号最终转换为物理信号进行传输。

在嵌入式系统中，这一层通常表现为 SoC 内部集成的MAC 控制器、板载的PHY 芯片以及RJ45 接口或天线。MAC 负责处理数据帧的封装、校验与介质访问控制；PHY 负责数模转换及链路协商；两者之间通过RGMII 或 RMII等标准接口进行数据通信，并通过MDIO总线进行配置管理。对于驱动开发者而言，这一层的正确性直接决定了链路是否能“Up”（建立连接），是硬件调试和设备树配置的重点。



~~~c
sudo vim binary/usr/local/bin/wifi_blue.sh

    
insmod ${MODULE_BASE}/${BOARD}/btbcm.ko
insmod ${MODULE_BASE}/${BOARD}/btintel.ko
insmod ${MODULE_BASE}/${BOARD}/btrtl.ko
insmod ${MODULE_BASE}/${BOARD}/btusb.ko
sudo cp /home/topeet/Linux/07_rk3568/rk356x_linux/buildroot/board/rockchip/rk356x/fs-overlay-system/usr/local/modules/*.ko binary/usr/local/modules/rk3568/


sudo XZ_OPT="-T0 -0" tar -cpJvf ubuntu-jammy-arm64.tar.xz binary
~~~



~~~c
echo 7 4 1 7 > /proc/sys/kernel/printk
~~~



~~~shell

# 安装编译所需的依赖包
install_package() {
    # 检查网络连接
    HOSTRELEASE=$(grep VERSION_CODENAME /etc/os-release | cut -d"=" -f2)
    echo -e "\e[33m当前运行的系统为 $HOSTRELEASE.\e[0m"
    echo -e "\e[34m正在检查网络连接...\e[0m"
    if ping -c 1 8.8.8.8 &> /dev/null; then
        echo -e "\e[32m网络连接正常，开始安装依赖包。\e[0m"

        # 通用必需依赖包列表，适用于内核、Buildroot、U-Boot、设备树等
        COMMON_PACKAGES=(
            whiptail dialog psmisc acl uuid uuid-runtime curl gpg gnupg gawk git
            aptly aria2 bc binfmt-support bison btrfs-progs build-essential
            ca-certificates ccache cpio cryptsetup debian-archive-keyring
            debian-keyring debootstrap device-tree-compiler dirmngr dosfstools
            dwarves f2fs-tools fakeroot flex gcc-arm-linux-gnueabihf gdisk imagemagick
            jq kmod libbison-dev libc6-dev-armhf-cross libelf-dev libfdt-dev
            libfile-fcntllock-perl libfl-dev liblz4-tool libncurses-dev libssl-dev
            libusb-1.0-0-dev linux-base locales lzop ncurses-base ncurses-term
            nfs-kernel-server ntpdate p7zip-full parted patchutils pigz pixz pkg-config
            pv python3-dev qemu-user-static rsync swig systemd-container u-boot-tools
            udev unzip uuid-dev wget zip zlib1g-dev distcc lib32ncurses-dev
            lib32stdc++6 libc6-i386 python3 expect expect-dev cmake vim openssh-server
            net-tools texinfo htop libgmp-dev libmpc-dev
        )

        # Ubuntu 18.04 特定的依赖包
        UBUNTU_18_PACKAGES=(
            liblz-dev liblzo2-2 liblzo2-dev mtd-utils squashfs-tools schedtool
            g++-multilib lib32z1-dev lib32ncurses5-dev lib32readline-dev gcc-multilib
            patchelf chrpath texinfo diffstat python3-pip subversion sed binutils
            bzip2 patch gzip perl tar file bc tcl android-tools-fsutils openjdk-8-jdk
            libsdl1.2-dev libesd-java libwxgtk3.0-dev repo bzr cvs mercurial pngcrush xsltproc
            gperf libc6-dev libgmp-dev libmpc-dev
        )

        # Ubuntu 20.04 和 22.04 特定的依赖包
        UBUNTU_20_22_PACKAGES=(
            python2 python3-distutils libpython2.7-dev
        )

        if [ "$HOSTRELEASE" == "bionic" ]; then
            echo -e "\e[33m正在安装 Ubuntu 18.04 编译所需依赖包...\e[0m"
            sudo apt-get update
            sudo apt-get -y upgrade
            sudo apt-get install -y --no-install-recommends "${COMMON_PACKAGES[@]}" "${UBUNTU_18_PACKAGES[@]}"
            echo -e "\e[32m依赖包安装完成。\e[0m"
        elif [ "$HOSTRELEASE" == "focal" ] || [ "$HOSTRELEASE" == "jammy" ]; then
            echo -e "\e[33m正在安装 Ubuntu 20.04 / 22.04 编译所需依赖包...\e[0m"
            sudo apt-get update
            sudo apt-get -y upgrade
            sudo apt-get install -y --no-install-recommends "${COMMON_PACKAGES[@]}" "${UBUNTU_20_22_PACKAGES[@]}"
            echo -e "\e[32m依赖包安装完成。\e[0m"
        elif [ "$HOSTRELEASE" == "noble" ]; then
            echo -e "\e[33m正在安装 Ubuntu 24.04 编译所需依赖包...\e[0m"
            sudo apt-get update
            sudo apt-get -y upgrade
            sudo apt-get install -y --no-install-recommends "${COMMON_PACKAGES[@]}"
            echo -e "\e[32m依赖包安装完成。\e[0m"
        else
            echo -e "\e[33m您的系统不是 Ubuntu 18.04 / 20.04 / 22.04 / 24.04，请自行安装依赖包。\e[0m"
        fi
    else
        # 没有网络连接，提示并退出函数
        echo -e "\e[31m未检测到网络连接，请确保已安装编译所需要的依赖包。\e[0m"
    fi
}
~~~

