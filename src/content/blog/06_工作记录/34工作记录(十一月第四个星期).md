---
title: 工作记录(十一月第四个星期)
date: 2024-11-25 10:26:56
categories:
  - 工作记录
link: 06_工作记录/34工作记录(十一月第四个星期)
---

# 11月25日

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

___

今天的任务其实很简单，那就是继续写对应的复用手册。



# 11月26日

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

___

搞NPU手册

| Category     | Name                                                         | Dtype     | Model Download Link                                          | Support platform                                             |
| ------------ | ------------------------------------------------------------ | --------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 图像分类     | [mobilenet](https://github.com/onnx/models/tree/8e893eb39b131f6d3970be6ebd525327d3df34ea/vision/classification/mobilenet/model/mobilenetv2-12.onnx) | FP16/INT8 | [mobilenetv2-12.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/mobilenet/mobilenetv2-12.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RV1103\|RV1106<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图像分类     | [resnet](https://github.com/onnx/models/tree/8e893eb39b131f6d3970be6ebd525327d3df34ea/vision/classification/resnet/model/resnet50-v2-7.onnx) | FP16/INT8 | [resnet50-v2-7.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/resnet/resnet50-v2-7.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov5](https://github.com/airockchip/yolov5)               | FP16/INT8 | [./yolov5s_relu.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5/yolov5s_relu.onnx)<br/>[./yolov5n.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5/yolov5n.onnx)<br/>[./yolov5s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5/yolov5s.onnx)<br/>[./yolov5m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5/yolov5m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RV1103\|RV1106<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov6](https://github.com/airockchip/yolov6)               | FP16/INT8 | [./yolov6n.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov6/yolov6n.onnx)<br/>[./yolov6s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov6/yolov6s.onnx)<br/>[./yolov6m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov6/yolov6m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov7](https://github.com/airockchip/yolov7)               | FP16/INT8 | [./yolov7-tiny.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov7/yolov7-tiny.onnx)<br/>[./yolov7.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov7/yolov7.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov8](https://github.com/airockchip/ultralytics_yolov8)   | FP16/INT8 | [./yolov8n.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8/yolov8n.onnx)<br/>[./yolov8s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8/yolov8s.onnx)<br/>[./yolov8m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8/yolov8m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov8_obb](https://github.com/airockchip/ultralytics_yolov8) | INT8      | [./yolov8n-obb.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8_obb/yolov8n-obb.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolov10](https://github.com/THU-MIG/yolov10)                | FP16/INT8 | [./yolov10n.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov10/yolov10n.onnx)<br/>[./yolov10s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov10/yolov10s.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RV1103\|RV1106<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolo11](https://github.com/airockchip/ultralytics_yolo11)   | FP16/INT8 | [./yolo11n.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolo11/yolo11n.onnx)<br/>[./yolo11s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolo11/yolo11s.onnx)<br/>[./yolo11m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolo11/yolo11m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RV1103\|RV1106<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolox](https://github.com/airockchip/YOLOX)                 | FP16/INT8 | [./yolox_s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolox/yolox_s.onnx)<br/>[./yolox_m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolox/yolox_m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [ppyoloe](https://github.com/PaddlePaddle/PaddleDetection/blob/release/2.6/configs/ppyoloe) | FP16/INT8 | [./ppyoloe_s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/ppyoloe/ppyoloe_s.onnx)<br/>[./ppyoloe_m.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/ppyoloe/ppyoloe_m.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 物体检测     | [yolo_world](https://github.com/AILab-CVC/YOLO-World)        | FP16/INT8 | [./yolo_world_v2s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolo_world/yolo_world_v2s.onnx)<br/>[./clip_text.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolo_world/clip_text.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>                  |
| 人体关键点   | [yolov8_pose](https://github.com/airockchip/ultralytics_yolov8) | INT8      | [./yolov8n-pose.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8_pose/yolov8n-pose.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576                       |
| 图像分割     | deeplabv3                                                    | FP16/INT8 | [./deeplab-v3-plus-mobilenet-v2.pb](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/deeplabv3/deeplab-v3-plus-mobilenet-v2.pb) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图像分割     | [yolov5_seg](https://github.com/airockchip/yolov5)           | FP16/INT8 | [./yolov5n-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5_seg/yolov5n-seg.onnx)<br/>[./yolov5s-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5_seg/yolov5s-seg.onnx)<br/>[./yolov5m-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov5_seg/yolov5m-seg.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图像分割     | [yolov8_seg](https://github.com/airockchip/ultralytics_yolov8) | FP16/INT8 | [./yolov8n-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8_seg/yolov8n-seg.onnx)<br/>[./yolov8s-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8_seg/yolov8s-seg.onnx)<br/>[./yolov8m-seg.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yolov8_seg/yolov8m-seg.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图像分割     | [ppseg](https://github.com/PaddlePaddle/PaddleSeg/tree/release/2.8) | FP16/INT8 | [pp_liteseg_cityscapes.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/ppseg/pp_liteseg_cityscapes.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图像分割     | [mobilesam](https://github.com/ChaoningZhang/MobileSAM)      | FP16      | [mobilesam_encoder_tiny.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/mobilesam/mobilesam_encoder_tiny.onnx)<br />[mobilesam_decoder.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/mobilesam/mobilesam_decoder.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576                       |
| 人脸关键点   | [RetinaFace](https://github.com/biubug6/Pytorch_Retinaface)  | INT8      | [RetinaFace_mobile320.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/RetinaFace/RetinaFace_mobile320.onnx)<br/>[RetinaFace_resnet50_320.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/RetinaFace/RetinaFace_resnet50_320.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 车牌识别     | [LPRNet](https://github.com/sirius-ai/LPRNet_Pytorch/)       | FP16/INT8 | [./lprnet.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/LPRNet/lprnet.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RV1103\|RV1106<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 文字检测     | [PPOCR-Det](https://github.com/PaddlePaddle/PaddleOCR/tree/release/2.7) | FP16/INT8 | [../ppocrv4_det.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/PPOCR/ppocrv4_det.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 文字识别     | [PPOCR-Rec](https://github.com/PaddlePaddle/PaddleOCR/tree/release/2.7) | FP16      | [../ppocrv4_rec.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/PPOCR/ppocrv4_rec.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 自然语言翻译 | [lite_transformer](https://github.com/airockchip/lite-transformer) | FP16      | [lite-transformer-encoder-16.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/lite_transformer/lite-transformer-encoder-16.onnx)<br/>[lite-transformer-decoder-16.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/lite_transformer/lite-transformer-decoder-16.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>RK1808\|RK3399PRO<br/>RV1109\|RV1126 |
| 图文匹配     | [clip](https://huggingface.co/openai/clip-vit-base-patch32)  | FP16      | [./clip_images.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/clip/clip_images.onnx)<br/>[./clip_text.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/clip/clip_text.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>                  |
| 语音识别     | [wav2vec2](https://github.com/facebookresearch/fairseq/tree/main/examples/wav2vec#wav2vec-20) | FP16      | [wav2vec2_base_960h_20s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/wav2vec2/wav2vec2_base_960h_20s.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576                       |
| 语音识别     | [whisper](https://github.com/openai/whisper)                 | FP16      | [whisper_encoder_base_20s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/whisper/whisper_encoder_base_20s.onnx)<br/>[whisper_decoder_base_20s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/whisper/whisper_decoder_base_20s.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>                  |
| 语音识别     | [zipformer](https://huggingface.co/csukuangfj/k2fsa-zipformer-bilingual-zh-en-t) | FP16      | [encoder-epoch-99-avg-1.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/zipformer/encoder-epoch-99-avg-1.onnx)<br/>[decoder-epoch-99-avg-1.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/zipformer/decoder-epoch-99-avg-1.onnx)<br/>[joiner-epoch-99-avg-1.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/zipformer/joiner-epoch-99-avg-1.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>                  |
| 语音分类     | [yamnet](https://www.tensorflow.org/hub/tutorials/yamnet)    | FP16      | [yamnet_3s.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/yamnet/yamnet_3s.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576                       |
| 文字转语音   | [mms_tts](https://huggingface.co/facebook/mms-tts-eng)       | FP16      | [mms_tts_eng_encoder_200.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/mms_tts/mms_tts_eng_encoder_200.onnx)<br/>[mms_tts_eng_decoder_200.onnx](https://ftrg.zbox.filez.com/v2/delivery/data/95f00b0fc900458ba134f8b180b3f7a1/examples/mms_tts/mms_tts_eng_decoder_200.onnx) | RK3566\|RK3568\|RK3588\|RK3562\|RK3576<br/>                  |





以下是整理后的模型信息表格：

| 模型名称         | 模型类型     | 主要功能       | 应用场景               | 特点                           |
| ---------------- | ------------ | -------------- | ---------------------- | ------------------------------ |
| mobilenet        | 图像分类     | 图像分类       | 移动设备、边缘计算     | 轻量化，适用于资源受限的设备   |
| resnet           | 图像分类     | 图像分类       | 计算机视觉、图像识别   | 深层网络结构，具有残差连接     |
| yolov5           | 物体检测     | 实时物体检测   | 监控、自动驾驶         | 高效、准确，支持多种尺寸模型   |
| yolov6           | 物体检测     | 实时物体检测   | 视频监控、智能交通     | 性能优化，支持多种硬件平台     |
| yolov7           | 物体检测     | 实时物体检测   | 安防监控、无人驾驶     | 高精度，快速推理               |
| yolov8           | 物体检测     | 实时物体检测   | 机器人视觉、工业检测   | 改进的架构，适应复杂环境       |
| yolov8_obb       | 物体检测     | 边界框检测     | 物体识别、自动化控制   | 精准的边界框预测               |
| yolov10          | 物体检测     | 实时物体检测   | 智能监控、无人机       | 增强的检测能力                 |
| yolo11           | 物体检测     | 实时物体检测   | 视频分析、交通监控     | 多任务能力，灵活性高           |
| yolox            | 物体检测     | 实时物体检测   | 智能家居、监控系统     | 创新的检测策略，快速响应       |
| ppyoloe          | 物体检测     | 实时物体检测   | 无人驾驶、安防         | 高效，适合多种应用             |
| yolo_world       | 物体检测     | 综合物体检测   | 通用物体识别           | 多种模型支持，灵活性高         |
| yolov8_pose      | 人体关键点   | 人体姿态识别   | 运动分析、健康监测     | 高精度关键点检测               |
| deeplabv3        | 图像分割     | 语义分割       | 医学影像、自动驾驶     | 强大的分割能力，适用于复杂场景 |
| yolov5_seg       | 图像分割     | 实例分割       | 物体检测、影像分析     | 高效的分割算法，适合实时应用   |
| yolov8_seg       | 图像分割     | 实例分割       | 计算机视觉、自动化检测 | 改进的分割技术，适应复杂环境   |
| ppseg            | 图像分割     | 图像分割       | 城市街景分析           | 针对特定数据集优化             |
| mobilesam        | 图像分割     | 图像分割       | 移动设备应用           | 轻量级设计，适合边缘计算       |
| RetinaFace       | 人脸关键点   | 人脸检测与识别 | 人脸识别系统、安全监控 | 高精度人脸识别能力             |
| LPRNet           | 车牌识别     | 车牌识别       | 停车管理、交通监控     | 高效的车牌识别算法             |
| PPOCR-Det        | 文字检测     | 文字检测       | 文档处理、场景文本识别 | 适应多种文本环境               |
| PPOCR-Rec        | 文字识别     | 文字识别       | 文档数字化、OCR应用    | 高准确率，支持多种语言         |
| lite_transformer | 自然语言翻译 | 文本翻译       | 跨语言沟通             | 轻量化设计，适合移动应用       |
| clip             | 图文匹配     | 图文匹配       | 内容推荐、搜索引擎     | 多模态学习能力                 |
| wav2vec2         | 语音识别     | 语音转文本     | 语音助手、会议记录     | 高准确率，适应多种语音环境     |
| whisper          | 语音识别     | 语音转文本     | 语音控制、转写         | 高效能，支持多种语音输入       |
| zipformer        | 语音识别     | 语音转文本     | 实时语音识别、会议记录 | 高精度和快速响应               |
| yamnet           | 语音分类     | 音频分类       | 环境声音识别           | 适用于各种音频输入             |
| mms_tts          | 文字转语音   | 文本转语音     | 语音助手、阅读应用     | 自然的发音，支持多种语言       |

希望这个表格对您有帮助！

以下是根据您的要求修改后的模型信息表格：

| 模型名称         | 模型类型     | 主要功能                     | 应用场景               | 特点                             |
| ---------------- | ------------ | ---------------------------- | ---------------------- | -------------------------------- |
| mobilenet        | 图像分类     | 对图像进行分类识别           | 移动设备、边缘计算     | 轻量级高效的卷积神经网络         |
| resnet           | 图像分类     | 对图像进行分类识别           | 计算机视觉、图像识别   | 深度残差网络，扩展性强           |
| yolov5           | 物体检测     | 检测图像中的物体位置和类别   | 智能交通、安防监控     | 单阶段高精度目标检测模型         |
| yolov6           | 物体检测     | 检测图像中的物体位置和类别   | 视频监控、智能交通     | 性能优化，支持多种硬件平台       |
| yolov7           | 物体检测     | 检测图像中的物体位置和类别   | 自动驾驶、安防监控     | 性能优于yolov5，速度快           |
| yolov8           | 物体检测     | 检测图像中的物体位置和类别   | 机器人视觉、工业检测   | 改进的yolov5，性能更优           |
| yolov8_obb       | 物体检测     | 检测图像中的物体位置和类别   | 物体识别、自动化控制   | 精准的边界框预测                 |
| yolov10          | 物体检测     | 检测图像中的物体位置和类别   | 智能监控、无人机       | 最新版yolo系列模型，性能持续提升 |
| yolo11           | 物体检测     | 检测图像中的物体位置和类别   | 视频分析、交通监控     | 简单高效的一阶段目标检测模型     |
| yolox            | 物体检测     | 检测图像中的物体位置和类别   | 智能家居、监控系统     | 创新的检测策略，快速响应         |
| ppyoloe          | 物体检测     | 检测图像中的物体位置和类别   | 无人驾驶、安防         | 性能优于yolov5和yolov6           |
| yolo_world       | 物体检测     | 检测图像中的物体位置和类别   | 通用物体识别           | 通用目标检测                     |
| yolov8_pose      | 人体关键点   | 检测图像中的人体关键点       | 人机交互               | 单阶段高精度行为检测             |
| deeplabv3        | 图像分割     | 对图像进行像素级别的语义分割 | 自动驾驶、医学影像分析 | 基于深度可分离卷积的高效语义分割 |
| yolov5_seg       | 图像分割     | 对图像进行像素级别的语义分割 | 物体检测、影像分析     | 基于yolov5的语义分割模型         |
| yolov8_seg       | 图像分割     | 对图像进行像素级别的语义分割 | 计算机视觉、自动化检测 | 基于yolov8的语义分割模型         |
| ppseg            | 图像分割     | 对图像进行像素级别的语义分割 | 城市街景分析           | 针对特定数据集优化               |
| mobilesam        | 图像分割     | 对图像进行像素级别的语义分割 | 移动设备应用           | 轻量级设计，适合边缘计算         |
| RetinaFace       | 人脸关键点   | 检测图像中的人脸位置         | 人脸识别系统、安全监控 | 高准确率人脸识别                 |
| LPRNet           | 车牌识别     | 从车辆图像中检测和识别车牌   | 停车管理、交通监控     | 高效的车牌识别算法               |
| PPOCR-Det        | 文字检测     | 从图像中检测和识别文字       | 文档扫描、图像文字提取 | 适应多种文本环境                 |
| PPOCR-Rec        | 文字识别     | 对文本进行分类识别           | 文档数字化、OCR应用    | 高准确率，支持多种语言           |
| lite_transformer | 自然语言翻译 | 对文本进行翻译               | 跨语言沟通             | 轻量级Transformer模型            |
| clip             | 图文匹配     | 对图像和文本进行匹配         | 内容推荐、搜索引擎     | 多模态学习能力                   |
| wav2vec2         | 语音识别     | 语音转文本                   | 语音助手、会议记录     | 高准确率，适应多种语音环境       |
| whisper          | 语音识别     | 语音转文本                   | 语音控制、转写         | 高效能，支持多种语音输入         |
| zipformer        | 语音识别     | 语音转文本                   | 实时语音识别、会议记录 | 高精度和快速响应                 |
| yamnet           | 语音分类     | 对音频进行分类               | 环境声音识别           | 适用于各种音频输入               |
| mms_tts          | 文字转语音   | 文本转语音                   | 语音助手、阅读应用     | 自然的发音，支持多种语言         |

希望这个表格符合您的要求！



| Linux环境版本 | 对应Pythion版本号 |
| ------------- | ----------------- |
| Ubuntu22      | 3.10              |
| Debian12      | 3.11              |





~~~shell
./build-linux.sh -t rk3576 -a aarch64 -d deeplabv3
./build-linux.sh -t rk3576 -a aarch64 -d lite_transformer
./build-linux.sh -t rk3576 -a aarch64 -d LPRNet
./build-linux.sh -t rk3576 -a aarch64 -d mobilenet
./build-linux.sh -t rk3576 -a aarch64 -d PPOCR-Rec
./build-linux.sh -t rk3576 -a aarch64 -d PPOCR-Det
./build-linux.sh -t rk3576 -a aarch64 -d PPOCR-System
./build-linux.sh -t rk3576 -a aarch64 -d ppseg
./build-linux.sh -t rk3576 -a aarch64 -d ppyoloe
./build-linux.sh -t rk3576 -a aarch64 -d resnet
./build-linux.sh -t rk3576 -a aarch64 -d RetinaFace
./build-linux.sh -t rk3576 -a aarch64 -d yolov5
./build-linux.sh -t rk3576 -a aarch64 -d yolov5_seg
./build-linux.sh -t rk3576 -a aarch64 -d yolov6
./build-linux.sh -t rk3576 -a aarch64 -d yolov7
./build-linux.sh -t rk3576 -a aarch64 -d yolov8
./build-linux.sh -t rk3576 -a aarch64 -d yolov8_seg
./build-linux.sh -t rk3576 -a aarch64 -d yolov8_obb
./build-linux.sh -t rk3576 -a aarch64 -d yolov8_pose
./build-linux.sh -t rk3576 -a aarch64 -d yolox
./build-linux.sh -t rk3576 -a aarch64 -d yolov10
./build-linux.sh -t rk3576 -a aarch64 -d yolo11
./build-linux.sh -t rk3576 -a aarch64 -d yolo_world
./build-linux.sh -t rk3576 -a aarch64 -d mobilesam

./build-linux.sh -t rk3576 -a aarch64 -d clip
./build-linux.sh -t rk3576 -a aarch64 -d wav2vec2
./build-linux.sh -t rk3576 -a aarch64 -d whisper
./build-linux.sh -t rk3576 -a aarch64 -d zipformer
./build-linux.sh -t rk3576 -a aarch64 -d yamnet
./build-linux.sh -t rk3576 -a aarch64 -d mms_tts
~~~

有问题

~~~shell

python3 clip.py --text_model clip_text.onnx --img_model clip_images.rknn rk3576
./build-linux.sh -t rk3576 -a aarch64 -d clip
~~~



~~~shell
clip  
python3 convert.py ../../model/clip_images.onnx rk3576 fp
python3 convert.py ../../model/clip_text.onnx rk3576 fp

lite_transformer  
python3  convert.py ../model/lite-transformer-decoder-16.onnx rk3576 fp
python3  convert.py ../model/lite-transformer-encoder-16.onnx rk3576 fp

mms_tts  
python3 convert.py ../model/mms_tts_eng_decoder_200.onnx rk3576 fp
python3 convert.py ../model/mms_tts_eng_encoder_200.onnx rk3576 fp
python3 mms_tts.py --decoder_model_path ../model/mms_tts_eng_decoder_200.rknn --encoder_model_path ../model/mms_tts_eng_encoder_200.rknn --target rk3576

mobilesam  
python3 convert.py ../../model/mobilesam_decoder.onnx rk3576 fp
python3 convert.py ../../model/mobilesam_encoder.onnx rk3576 fp
python3 mobilesam.py  --encoder ../model/mobilesam_decoder.rknn --decoder ../model/mobilesam_decoder.rknn --target rk3576

ppseg    
python3 convert.py ../model/pp_liteseg_cityscapes.onnx rk3576 fp

resnet      
python3 resnet.py ../model/resnet50-v2-7.onnx rk3576 fp

wav2vec2  
python3 convert.py ../model/wav2vec2_base_960h_20s.onnx rk3576 fp

yamnet
python3 convert.py ../model/yamnet_3s.onnx rk3576 fp

yolov10
python3 convert.py ../model/yolov10s.onnx rk3576 fp

yolov5_seg
python3 convert.py ../model/yolov5s-seg.onnx rk3576 fp

yolov7
python3 convert.py ../model/yolov7-tiny.onnx rk3576 fp

yolov8_obb
python3 convert.py ../model/yolov8n-obb.onnx rk3576 fp

yolov8_seg
python3 convert.py ../model/yolov8s-seg.onnx rk3576 fp

yolox
python3 convert.py ../model/yolox_s.onnx rk3576 fp

deeplabv3
python3 convert.py ../model/deeplab-v3-plus-mobilenet-v2.pb rk3576 fp

LPRNet   
python3 convert.py ../model/lprnet.onnx rk3576 fp

mobilenet
python3 mobilenet.py --model ../model/mobilenetv2-12.onnx --target rk3576 --dtype fp

PPOCR    
python3 convert.py ../model/ppocrv4_det.onnx rk3576 fp
python3 convert.py ../model/ppocrv4_rec.onnx rk3576 fp

ppyoloe
python3 convert.py ../model/ppyoloe_s.onnx rk3576 fp

RetinaFace
python3 convert.py ../model/RetinaFace_mobile320.onnx rk3576 fp

whisper  
python3 convert.py ../model/whisper_encoder_base_20s.onnx rk3576 fp
python3 convert.py ../model/whisper_decoder_base_20s.onnx rk3576 fp

yolo11  
python3 convert.py ../model/yolo11n.onnx rk3576 fp

yolov5   
python3 convert.py ../model/yolov5s_relu.onnx rk3576 fp

yolov6   
python3 convert.py ../model/yolov6n.onnx rk3576 fp

yolov8
python3 convert.py ../model/yolov8n.onnx rk3576 fp

yolov8_pose
python3 convert.py ../model/yolov8n-pose.onnx rk3576 fp

yolo_world 
python3 convert.py ../../model/yolo_world_v2s.onnx rk3576 fp
python3 convert.py ../../model/clip_text.onnx rk3576 fp

zipformer
python3 convert.py ../model/joiner-epoch-99-avg-1.onnx rk3576 fp
python3 convert.py ../model/encoder-epoch-99-avg-1.onnx rk3576 fp
python3 convert.py ../model/decoder-epoch-99-avg-1.onnx rk3576 fp
~~~

# 11.28日(星期四)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

____

昨天下午写完了RK3576的NPU测试手册，但是法师呢想着是对这些进行一下包装，现在都是用图片来进行的例程演示，这时候就在想，如果将这些内容进行一下包装，显的更炫酷，我的本意是认为这些并没有什么用，做这些也并没有什么意义，但既然是法师让做的，一切也都变的合理了起来，我先看看瑞芯微提供的那些具体例子吧。

- 写一个手册，更新镜像内容的手册，感觉倒是挺简单的。
  - 有点问题，这个东西啊，说简单也简单，说复杂也复杂，他们的这个东西目前是只有一个update.img，连源码都没有啊，我丢，有点思路了，我直接搞一个新的SDK，专门给这个客户提供的那种。
  - ==搞定了，我是真的聪明啊，哈哈==



- 看瑞芯微的例程
  - 看到clip，想到昨天的报错问题，想换高版本的编译看看 （==啊靠，编译成功了，牛逼，ubuntu20不行，ubuntu22就能编译过去~，不错啊，果然心态改变了，相应的运气也会变好==）
  - 高兴的太早了，也只是编译成功了yolo-world和clip分割，而且这两个我并不想写个例程，主要是没啥用啊，行了例程就先看到这，还是没啥帮助，然后
- 看正点原子的AI资源
  - 我认为是没啥可写的，确实没啥可以利用的东西，不是很喜欢

```
./build-linux.sh -t rk3576 -a aarch64 -d yolov5

set(OpenCV_DIR ${CMAKE_CURRENT_SOURCE_DIR}/../../../3rdparty/opencv/opencv-linux-aarch64/lib/cmake/opencv4)  
find_package(OpenCV REQUIRED)
  ${OpenCV_LIBS}
```

cmake的问题解决是解决了，但是又有了新的问题





~~~shell
sudo apt-get install build-essential 

sudo apt-get install cmake git libgtk2.0-dev pkg-config libavcodec-dev libavformat-dev libswscale-dev

sudo apt-get install python-dev-is-python3 python-numpy libtbb2 libtbb-dev libjpeg-dev libpng-dev libtiff-dev  libdc1394-22-dev

pip3 install numpy
 
sudo update-alternatives --install /usr/bin/python python /usr/bin/python2 100
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3 150
~~~



# 11月29日(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

___

继续分析昨天遗留下的问题，目前的任务是用摄像头来实现。

安装管理任务需求

~~~shell
sudo apt-get install nautilus
~~~



~~~shell
sudo apt update
sudo apt install libtiff5=4.3.0-6ubuntu0.10 libopencv-dev
g++ -o camera demo.cc `pkg-config --cflags --libs opencv4`
~~~



~~~c++
    #include "opencv2/core/core.hpp"
    #include "opencv2/highgui/highgui.hpp"
    #include "opencv2/imgproc/imgproc.hpp"
#include <iostream>

using namespace cv;
using namespace std;

int main(int argc, char** argv)
{
    if (argc < 3)
    {
        cout << "Usage: " << argv[0] << " <camera_type> <device_path>" << endl;
        cout << "camera_type: usb, mipi, or hdmiin" << endl;
        return -1;
    }

    string camera_type = argv[1];
    string device_path = argv[2];
    VideoCapture capture;

    if (camera_type == "usb")
    {
        capture.open(device_path);
    }
    else if (camera_type == "mipi")
    {
        string pipeline = "v4l2src device=" + device_path + " ! video/x-raw, format=(string)NV12 ! appsink";
        capture.open(pipeline, CAP_GSTREAMER);
    }
    else if (camera_type == "hdmiin")
    {
        string pipeline = "v4l2src device=" + device_path + " ! video/x-raw, format=(string)BGR ! videoconvert ! appsink";
        capture.open(pipeline, CAP_GSTREAMER);
    }
    else
    {
        cout << "Invalid camera type. Use 'usb', 'mipi', or 'hdmiin'." << endl;
        return -1;
    }

    if (!capture.isOpened())
    {
        cout << "无法打开摄像头" << endl;
        return -1;
    }

    double time0 = static_cast<double>(getTickCount()); // 获取开始时间
    while (true)
    {
        // 从摄像头捕获帧
        Mat frame;
        capture >> frame;

        // 如果捕获到帧，则显示它
        if (!frame.empty())
        {
            if (camera_type == "mipi")
            {
                cvtColor(frame, frame, COLOR_YUV2BGR_NV12); // 将 NV12 格式转换为 BGR
            }
            double time1 = static_cast<double>(getTickCount()); // 获取当前时间
            double fps = getTickFrequency() / (time1 - time0); // 计算实时帧率
            time0 = time1; // 更新开始时间
            putText(frame, "FPS: " + to_string(fps), Point(10, 30), FONT_HERSHEY_SIMPLEX, 1, Scalar(0, 0, 255), 2); // 在图像上显示帧率
            imshow("Camera", frame);
        }

        // 按下'q'键退出循环
        if (waitKey(1) == 'q')
        {
            break;
        }
    }

    // 释放资源并关闭窗口
    capture.release();
    destroyAllWindows();

    return 0;
}
~~~

现在的摄像头显示绝对是没有任何问题的，然后慢慢的分析一下，现在的代码

~~~c++
// Copyright (c) 2023 by Rockchip Electronics Co., Ltd. All Rights Reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*-------------------------------------------
                Includes
-------------------------------------------*/
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "yolov5.h"
#include "image_utils.h"
#include "file_utils.h"
#include "image_drawing.h"


/*-------------------------------------------
                  Main Function
-------------------------------------------*/
int main(int argc, char **argv)
{
    // 判断输入参数是否正确，程序需要两个参数：模型路径和图片路径
    if (argc != 3)
    {
        // 如果参数不正确，打印使用方法
        printf("%s <model_path> <image_path>\n", argv[0]);
        return -1;
    }

    // 获取模型路径和图片路径
    const char *model_path = argv[1];
    const char *image_path = argv[2];

    int ret;
    rknn_app_context_t rknn_app_ctx;
    memset(&rknn_app_ctx, 0, sizeof(rknn_app_context_t));  // 初始化应用上下文

    init_post_process();  // 初始化后处理

    // 初始化YOLOv5模型
    ret = init_yolov5_model(model_path, &rknn_app_ctx);
    if (ret != 0)
    {
        // 如果初始化模型失败，打印错误信息并跳转到out标签
        printf("init_yolov5_model fail! ret=%d model_path=%s\n", ret, model_path);
        goto out;
    }

    image_buffer_t src_image;
    memset(&src_image, 0, sizeof(image_buffer_t));  // 初始化图像缓冲区
    ret = read_image(image_path, &src_image);  // 读取输入图像

    if (ret != 0)
    {
        // 如果读取图像失败，打印错误信息并跳转到out标签
        printf("read image fail! ret=%d image_path=%s\n", ret, image_path);
        goto out;
    }

    object_detect_result_list od_results;

    // 进行推理，得到检测结果
    ret = inference_yolov5_model(&rknn_app_ctx, &src_image, &od_results);
    if (ret != 0)
    {
        // 如果推理失败，打印错误信息并跳转到out标签
        printf("inference_yolov5_model fail! ret=%d\n", ret);
        goto out;
    }

    // 画框和概率
    char text[256];
    for (int i = 0; i < od_results.count; i++)
    {
        // 遍历检测结果
        object_detect_result *det_result = &(od_results.results[i]);
        // 打印检测到的物体类别和框坐标及概率
        printf("%s @ (%d %d %d %d) %.3f\n", coco_cls_to_name(det_result->cls_id),
               det_result->box.left, det_result->box.top,
               det_result->box.right, det_result->box.bottom,
               det_result->prop);
        int x1 = det_result->box.left;
        int y1 = det_result->box.top;
        int x2 = det_result->box.right;
        int y2 = det_result->box.bottom;

        // 在图像上画出检测框
        draw_rectangle(&src_image, x1, y1, x2 - x1, y2 - y1, COLOR_BLUE, 3);

        // 在框上方显示类别名称和检测概率
        sprintf(text, "%s %.1f%%", coco_cls_to_name(det_result->cls_id), det_result->prop * 100);
        draw_text(&src_image, text, x1, y1 - 20, COLOR_RED, 10);
    }

    // 将处理后的图像保存到文件
    write_image("out.png", &src_image);

out:
    // 后处理和资源释放
    deinit_post_process();

    // 释放YOLOv5模型资源
    ret = release_yolov5_model(&rknn_app_ctx);
    if (ret != 0)
    {
        // 如果释放模型失败，打印错误信息
        printf("release_yolov5_model fail! ret=%d\n", ret);
    }

    // 释放图像内存
    if (src_image.virt_addr != NULL)
    {
        // 否则释放普通内存
        free(src_image.virt_addr);
    }

    return 0;
}
~~~

在第63行使用了**read_image**函数对输入图形进行了读取，read_img函数看样子是RK提供的使用RGA编写的一个函数，具体看一下吧。这里肯定是要花费很多很多的时间的，其实主要还是一个流程问题，我不知道这中间都经历过了哪些步骤，这里需要自己研究看看，然后再努力编写一下对应的RGA mpp应用程序才对，然后分析一下对应的流程，看看怎样完成这个任务对我的收益最大化，以及完成任务的过程更舒服。

> 1.分析RK demo，确定最终的实现流程
>
> 2.使用opencv来实现最终的效果
>
> 3.学习mpp rga的使用，分析其中的流程和使用方法
>
> 4.使用mpp rga来实现最终的效果
>
> 5.提供对应的可以使用的例程和手册

先试着用opencv来实现最终的效果。

又浅显的分析了一下，还是好难啊，找不到实现的头绪啊，我发现之前的例程也是直接用的人家写好的，我自己都并没有，我所拥有的也只是简单的修改罢了，现在感觉到的是莫名的空虚，要想实现最终的效果，必然要彻底的了解其中的根枝末节，不然没法子搞。他这些源码的集成度太高了。我一点点的分析吧，先从最简单的rknpu2的demo开始分析，然后是车牌识别的demo，然后是model_zoo的demo，最后是rknpu2的videodemo。

## rknpu2 demo分析

直接来看主函数，首先是一些定义

~~~c
  if (argc < 3)
  {
    printf("Usage: %s <rknn model> <input_image_path> <resize/letterbox> <output_image_path>\n", argv[0]);
    return -1;
  }
  int ret;
  rknn_context ctx;
  size_t actual_size = 0;
  int img_width = 0;
  int img_height = 0;
  int img_channel = 0;
  const float nms_threshold = NMS_THRESH;      // 默认的NMS阈值
  const float box_conf_threshold = BOX_THRESH; // 默认的置信度阈值
  struct timeval start_time, stop_time;
  char *model_name = (char *)argv[1];
  char *input_path = argv[2];
  std::string option = "letterbox";
  std::string out_path = "./out.jpg";
  if (argc >= 4)
  {
    option = argv[3];
  }
  if (argc >= 5)
  {
    out_path = argv[4];
  }

  // init rga context
  rga_buffer_t src;
  rga_buffer_t dst;
  memset(&src, 0, sizeof(src));
  memset(&dst, 0, sizeof(dst));

  printf("post process config: box_conf_threshold = %.2f, nms_threshold = %.2f\n", box_conf_threshold, nms_threshold);
~~~

# 11.30日(星期六)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

___

昨天真的算是摸了一天的鱼啊，跟田田搞了一天的ull，怎么说呢，这个东西呀，感觉确实提速有些困难，我是没啥想法了，让田田自己努力吧，而我呢就仍旧是按照昨天的想法继续分析源码。

一系列的参数定义之后就是创建RKNN模型，这里我能懂，api也不用看啥

~~~c

  /* Create the neural network */
  printf("Loading mode...\n");
  int model_data_size = 0;
  unsigned char *model_data = load_model(model_name, &model_data_size);
  ret = rknn_init(&ctx, model_data, model_data_size, 0, NULL);
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
~~~

上面初始化了rknn模型，然后呢可以通过这个得到的ctx来打印一些参数，例如版本号，模型输入数和输出数量，具体如下所示：
~~~c
 rknn_sdk_version version;
  ret = rknn_query(ctx, RKNN_QUERY_SDK_VERSION, &version, sizeof(rknn_sdk_version));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("sdk version: %s driver version: %s\n", version.api_version, version.drv_version);

  rknn_input_output_num io_num;
  ret = rknn_query(ctx, RKNN_QUERY_IN_OUT_NUM, &io_num, sizeof(io_num));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("model input num: %d, output num: %d\n", io_num.n_input, io_num.n_output);
~~~

这其实也是打印初始化之后rknn模型的一些信息，上面打印的是输入和输出的tenser属性，而下面呢是打印输入输出的各种参数，

~~~c
 rknn_tensor_attr input_attrs[io_num.n_input];
  memset(input_attrs, 0, sizeof(input_attrs));
  for (int i = 0; i < io_num.n_input; i++)
  {
    input_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_INPUT_ATTR, &(input_attrs[i]), sizeof(rknn_tensor_attr));
    if (ret < 0)
    {
      printf("rknn_init error ret=%d\n", ret);
      return -1;
    }
    dump_tensor_attr(&(input_attrs[i]));
  }

  rknn_tensor_attr output_attrs[io_num.n_output];
  memset(output_attrs, 0, sizeof(output_attrs));
  for (int i = 0; i < io_num.n_output; i++)
  {
    output_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_OUTPUT_ATTR, &(output_attrs[i]), sizeof(rknn_tensor_attr));
    dump_tensor_attr(&(output_attrs[i]));
  }
~~~

本来我想的是如果删除掉上面的打印会不会更清爽呢，然后发现并不是这样，上面的内容是有必要的，因为后面的代码里需要知道输入的dims，type类型，格式等等，然后继续看后面的内容，其实也是一个打印，以及根据模型格式进行参数修改：
~~~c

  int channel = 3;
  int width = 0;
  int height = 0;
  if (input_attrs[0].fmt == RKNN_TENSOR_NCHW)
  {
    printf("model is NCHW input fmt\n");
    channel = input_attrs[0].dims[1];
    height = input_attrs[0].dims[2];
    width = input_attrs[0].dims[3];
  }
  else
  {
    printf("model is NHWC input fmt\n");
    height = input_attrs[0].dims[1];
    width = input_attrs[0].dims[2];
    channel = input_attrs[0].dims[3];
  }

  printf("model input height=%d, width=%d, channel=%d\n", height, width, channel);
~~~

将获取到的属性赋予nhwc，图片的长宽和通道数，然后是input也就是模型推理输入数据的一些个赋值

~~~c

  rknn_input inputs[1];
  memset(inputs, 0, sizeof(inputs));
  inputs[0].index = 0;
  inputs[0].type = RKNN_TENSOR_UINT8;
  inputs[0].size = width * height * channel;
  inputs[0].fmt = RKNN_TENSOR_NHWC;
  inputs[0].pass_through = 0;
~~~

这上面只是一些基本的属性，但要推理的图片呢？图片的数据还没有获取到呢，所以下面必然是图形的读取，我认为这里是最重要的一部分

~~~c
 // 读取图片
  printf("Read %s ...\n", input_path);
  cv::Mat orig_img = cv::imread(input_path, 1);
  if (!orig_img.data)
  {
    printf("cv::imread %s fail!\n", input_path);
    return -1;
  }
  cv::Mat img;
  cv::cvtColor(orig_img, img, cv::COLOR_BGR2RGB);
  img_width = img.cols;
  img_height = img.rows;
  printf("img width = %d, img height = %d\n", img_width, img_height);

  // 指定目标大小和预处理方式,默认使用LetterBox的预处理
  BOX_RECT pads;
  memset(&pads, 0, sizeof(BOX_RECT));
  cv::Size target_size(width, height);
  cv::Mat resized_img(target_size.height, target_size.width, CV_8UC3);
  // 计算缩放比例
  float scale_w = (float)target_size.width / img.cols;
  float scale_h = (float)target_size.height / img.rows;

  if (img_width != width || img_height != height)
  {
    // 直接缩放采用RGA加速
    if (option == "resize")
    {
      printf("resize image by rga\n");
      ret = resize_rga(src, dst, img, resized_img, target_size);
      if (ret != 0)
      {
        fprintf(stderr, "resize with rga error\n");
        return -1;
      }
      // 保存预处理图片
      cv::imwrite("resize_input.jpg", resized_img);
    }
    else if (option == "letterbox")
    {
      printf("resize image with letterbox\n");
      float min_scale = std::min(scale_w, scale_h);
      scale_w = min_scale;
      scale_h = min_scale;
      letterbox(img, resized_img, pads, min_scale, target_size);
      // 保存预处理图片
      cv::imwrite("letterbox_input.jpg", resized_img);
    }
    else
    {
      fprintf(stderr, "Invalid resize option. Use 'resize' or 'letterbox'.\n");
      return -1;
    }
    inputs[0].buf = resized_img.data;
  }
  else
  {
    inputs[0].buf = img.data;
  }
~~~

第1-13行：首先使用opencv读取到了图片的数据，imread，但这时候的图片数据是BGR的，也就是opencv读取到的基本数据，但这时候呢是不对的，需要转换成RGB的格式才行，所以后面进行了颜色通道的转换。

第15-19行：这里没咋看懂啊，不知道这里的逻辑是什么，明明感觉直接用高和宽指代就行了

第21-22行：由于推理模型输入的长宽高都已确定，但是输入的图像可能并不是这个大小的图像，所以这里要计算一下缩放比例

第24-59行：就都是图像缩放的内容了，如果是等宽等高那就不用缩放，直接将img的data赋予给buf，但如果不等宽也不等高就要进行缩放了，缩放有两种方法，一种是使用rga的方法进行缩放，一种是使用letterbox的方法进行缩放，这里两种方法都是可以的，但具体是怎样实现的目前先不用管。

然后看接下来的内容：
~~~c
 gettimeofday(&start_time, NULL);
  rknn_inputs_set(ctx, io_num.n_input, inputs);

  rknn_output outputs[io_num.n_output];
  memset(outputs, 0, sizeof(outputs));
  for (int i = 0; i < io_num.n_output; i++)
  {
    outputs[i].index = i;
    outputs[i].want_float = 0;
  }
~~~

这里呢，是使用rknn_inputs_set设置输入数据，上面获取到了输入的长宽 format、输入的个数，输入的数据，，但并没设置，所以这里先进行了设置，然后又对输出进行了设置，接下来就到了推理环节了。推理很简单，就一行的代码，具体如下所示：
~~~c
  // 执行推理
  ret = rknn_run(ctx, NULL);
  ret = rknn_outputs_get(ctx, io_num.n_output, outputs, NULL);
  gettimeofday(&stop_time, NULL);
  printf("once run use %f ms\n", (__get_us(stop_time) - __get_us(start_time)) / 1000);
~~~

这里的代码重要的是第2 3 ，第2行是推理，第三行是将推理到的结果进行保存，保存到outputs里，其他没啥。接下来就是最重要的后处理部分了，具体代码如下所示：
~~~c
  // 后处理
  detect_result_group_t detect_result_group;
  std::vector<float> out_scales;
  std::vector<int32_t> out_zps;
  for (int i = 0; i < io_num.n_output; ++i)
  {
    out_scales.push_back(output_attrs[i].scale);
    out_zps.push_back(output_attrs[i].zp);
  }
  post_process((int8_t *)outputs[0].buf, (int8_t *)outputs[1].buf, (int8_t *)outputs[2].buf, height, width,
               box_conf_threshold, nms_threshold, pads, scale_w, scale_h, out_zps, out_scales, &detect_result_group);

~~~

我认为这个代码呢最重要的地方就在于第10行的后处理部分，通过这个后处理函数将处理得到的数据保存在了detect_result_group结构体当中，这个结构体呢具体如下：
~~~c
typedef struct _BOX_RECT
{
    int left;
    int right;
    int top;
    int bottom;
} BOX_RECT;

typedef struct __detect_result_t
{
    char name[OBJ_NAME_MAX_SIZE];
    int  class_id;
    BOX_RECT box;
    float prop;
} detect_result_t;
~~~

包括，推理得到的id，name 以及推理得到的四个边角，都存放到了detect_result_group里面，得到了这些矩形数据之后，就可以进行画框了，下面是画框的代码，这里我也不需要写吧。

~~~c
  char text[256];
  for (int i = 0; i < detect_result_group.count; i++)
  {
    detect_result_t *det_result = &(detect_result_group.results[i]);
    sprintf(text, "%s %.1f%%", det_result->name, det_result->prop * 100);
    printf("%s @ (%d %d %d %d) %f\n", det_result->name, det_result->box.left, det_result->box.top,
           det_result->box.right, det_result->box.bottom, det_result->prop);
    int x1 = det_result->box.left;
    int y1 = det_result->box.top;
    int x2 = det_result->box.right;
    int y2 = det_result->box.bottom;
    rectangle(orig_img, cv::Point(x1, y1), cv::Point(x2, y2), cv::Scalar(256, 0, 0, 256), 3);
    putText(orig_img, text, cv::Point(x1, y1 + 12), cv::FONT_HERSHEY_SIMPLEX, 0.4, cv::Scalar(255, 255, 255));
  }

  printf("save detect result to %s\n", out_path.c_str());
  imwrite(out_path, orig_img);
  ret = rknn_outputs_release(ctx, io_num.n_output, outputs);
~~~

这里就算我后面用也可以直接用，并没啥，再然后是耗时统计，具体内容如下所示：
~~~c

  // 耗时统计
  int test_count = 10;
  gettimeofday(&start_time, NULL);
  for (int i = 0; i < test_count; ++i)
  {
    rknn_inputs_set(ctx, io_num.n_input, inputs);
    ret = rknn_run(ctx, NULL);
    ret = rknn_outputs_get(ctx, io_num.n_output, outputs, NULL);
#if PERF_WITH_POST
    post_process((int8_t *)outputs[0].buf, (int8_t *)outputs[1].buf, (int8_t *)outputs[2].buf, height, width,
                 box_conf_threshold, nms_threshold, pads, scale_w, scale_h, out_zps, out_scales, &detect_result_group);
#endif
    ret = rknn_outputs_release(ctx, io_num.n_output, outputs);
  }
  gettimeofday(&stop_time, NULL);
  printf("loop count = %d , average run  %f ms\n", test_count,
         (__get_us(stop_time) - __get_us(start_time)) / 1000.0 / test_count);

~~~

这里可以删掉，后面就是一些释放内存的操作，不用理会。然后这是删掉没用代码的main.cc

~~~c
// Copyright (c) 2021 by Rockchip Electronics Co., Ltd. All Rights Reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*-------------------------------------------
                Includes
-------------------------------------------*/
#include <dlfcn.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/time.h>

#define _BASETSD_H

#include "RgaUtils.h"

#include "postprocess.h"

#include "rknn_api.h"
#include "preprocess.h"

#define PERF_WITH_POST 1
/*-------------------------------------------
                  Functions
-------------------------------------------*/

static void dump_tensor_attr(rknn_tensor_attr *attr)
{
  std::string shape_str = attr->n_dims < 1 ? "" : std::to_string(attr->dims[0]);
  for (int i = 1; i < attr->n_dims; ++i)
  {
    shape_str += ", " + std::to_string(attr->dims[i]);
  }

  printf("  index=%d, name=%s, n_dims=%d, dims=[%s], n_elems=%d, size=%d, w_stride = %d, size_with_stride=%d, fmt=%s, "
         "type=%s, qnt_type=%s, "
         "zp=%d, scale=%f\n",
         attr->index, attr->name, attr->n_dims, shape_str.c_str(), attr->n_elems, attr->size, attr->w_stride,
         attr->size_with_stride, get_format_string(attr->fmt), get_type_string(attr->type),
         get_qnt_type_string(attr->qnt_type), attr->zp, attr->scale);
}

double __get_us(struct timeval t) { return (t.tv_sec * 1000000 + t.tv_usec); }

static unsigned char *load_data(FILE *fp, size_t ofst, size_t sz)
{
  unsigned char *data;
  int ret;

  data = NULL;

  if (NULL == fp)
  {
    return NULL;
  }

  ret = fseek(fp, ofst, SEEK_SET);
  if (ret != 0)
  {
    printf("blob seek failure.\n");
    return NULL;
  }

  data = (unsigned char *)malloc(sz);
  if (data == NULL)
  {
    printf("buffer malloc failure.\n");
    return NULL;
  }
  ret = fread(data, 1, sz, fp);
  return data;
}

static unsigned char *load_model(const char *filename, int *model_size)
{
  FILE *fp;
  unsigned char *data;

  fp = fopen(filename, "rb");
  if (NULL == fp)
  {
    printf("Open file %s failed.\n", filename);
    return NULL;
  }

  fseek(fp, 0, SEEK_END);
  int size = ftell(fp);

  data = load_data(fp, 0, size);

  fclose(fp);

  *model_size = size;
  return data;
}

static int saveFloat(const char *file_name, float *output, int element_size)
{
  FILE *fp;
  fp = fopen(file_name, "w");
  for (int i = 0; i < element_size; i++)
  {
    fprintf(fp, "%.6f\n", output[i]);
  }
  fclose(fp);
  return 0;
}

/*-------------------------------------------
                  Main Functions
-------------------------------------------*/
int main(int argc, char **argv)
{
  if (argc < 3)
  {
    printf("Usage: %s <rknn model> <input_image_path> <resize/letterbox> <output_image_path>\n", argv[0]);
    return -1;
  }
  int ret;
  rknn_context ctx;
  size_t actual_size = 0;
  int img_width = 0;
  int img_height = 0;
  int img_channel = 0;
  const float nms_threshold = NMS_THRESH;      // 默认的NMS阈值
  const float box_conf_threshold = BOX_THRESH; // 默认的置信度阈值
  char *model_name = (char *)argv[1];
  char *input_path = argv[2];
  std::string option = "letterbox";
  std::string out_path = "./out.jpg";
  if (argc >= 4)
  {
    option = argv[3];
  }
  if (argc >= 5)
  {
    out_path = argv[4];
  }

  // init rga context
  rga_buffer_t src;
  rga_buffer_t dst;
  memset(&src, 0, sizeof(src));
  memset(&dst, 0, sizeof(dst));

  printf("post process config: box_conf_threshold = %.2f, nms_threshold = %.2f\n", box_conf_threshold, nms_threshold);

  /* Create the neural network */
  printf("Loading mode...\n");
  int model_data_size = 0;
  unsigned char *model_data = load_model(model_name, &model_data_size);
  ret = rknn_init(&ctx, model_data, model_data_size, 0, NULL);
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }

  rknn_sdk_version version;
  ret = rknn_query(ctx, RKNN_QUERY_SDK_VERSION, &version, sizeof(rknn_sdk_version));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("sdk version: %s driver version: %s\n", version.api_version, version.drv_version);

  rknn_input_output_num io_num;
  ret = rknn_query(ctx, RKNN_QUERY_IN_OUT_NUM, &io_num, sizeof(io_num));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("model input num: %d, output num: %d\n", io_num.n_input, io_num.n_output);

  rknn_tensor_attr input_attrs[io_num.n_input];
  memset(input_attrs, 0, sizeof(input_attrs));
  for (int i = 0; i < io_num.n_input; i++)
  {
    input_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_INPUT_ATTR, &(input_attrs[i]), sizeof(rknn_tensor_attr));
    if (ret < 0)
    {
      printf("rknn_init error ret=%d\n", ret);
      return -1;
    }
    dump_tensor_attr(&(input_attrs[i]));
  }

  rknn_tensor_attr output_attrs[io_num.n_output];
  memset(output_attrs, 0, sizeof(output_attrs));
  for (int i = 0; i < io_num.n_output; i++)
  {
    output_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_OUTPUT_ATTR, &(output_attrs[i]), sizeof(rknn_tensor_attr));
    dump_tensor_attr(&(output_attrs[i]));
  }

  int channel = 3;
  int width = 0;
  int height = 0;
  if (input_attrs[0].fmt == RKNN_TENSOR_NCHW)
  {
    printf("model is NCHW input fmt\n");
    channel = input_attrs[0].dims[1];
    height = input_attrs[0].dims[2];
    width = input_attrs[0].dims[3];
  }
  else
  {
    printf("model is NHWC input fmt\n");
    height = input_attrs[0].dims[1];
    width = input_attrs[0].dims[2];
    channel = input_attrs[0].dims[3];
  }

  printf("model input height=%d, width=%d, channel=%d\n", height, width, channel);

  rknn_input inputs[1];
  memset(inputs, 0, sizeof(inputs));
  inputs[0].index = 0;
  inputs[0].type = RKNN_TENSOR_UINT8;
  inputs[0].size = width * height * channel;
  inputs[0].fmt = RKNN_TENSOR_NHWC;
  inputs[0].pass_through = 0;

  // 读取图片
  printf("Read %s ...\n", input_path);
  cv::Mat orig_img = cv::imread(input_path, 1);
  if (!orig_img.data)
  {
    printf("cv::imread %s fail!\n", input_path);
    return -1;
  }
  cv::Mat img;
  cv::cvtColor(orig_img, img, cv::COLOR_BGR2RGB);
  img_width = img.cols;
  img_height = img.rows;
  printf("img width = %d, img height = %d\n", img_width, img_height);

  // 指定目标大小和预处理方式,默认使用LetterBox的预处理
  BOX_RECT pads;
  memset(&pads, 0, sizeof(BOX_RECT));
  cv::Size target_size(width, height);
  cv::Mat resized_img(target_size.height, target_size.width, CV_8UC3);
  // 计算缩放比例
  float scale_w = (float)target_size.width / img.cols;
  float scale_h = (float)target_size.height / img.rows;

  if (img_width != width || img_height != height)
  {
    // 直接缩放采用RGA加速
    if (option == "resize")
    {
      printf("resize image by rga\n");
      ret = resize_rga(src, dst, img, resized_img, target_size);
      if (ret != 0)
      {
        fprintf(stderr, "resize with rga error\n");
        return -1;
      }
      // 保存预处理图片
      cv::imwrite("resize_input.jpg", resized_img);
    }
    else if (option == "letterbox")
    {
      printf("resize image with letterbox\n");
      float min_scale = std::min(scale_w, scale_h);
      scale_w = min_scale;
      scale_h = min_scale;
      letterbox(img, resized_img, pads, min_scale, target_size);
      // 保存预处理图片
      cv::imwrite("letterbox_input.jpg", resized_img);
    }
    else
    {
      fprintf(stderr, "Invalid resize option. Use 'resize' or 'letterbox'.\n");
      return -1;
    }
    inputs[0].buf = resized_img.data;
  }
  else
  {
    inputs[0].buf = img.data;
  }

  rknn_inputs_set(ctx, io_num.n_input, inputs);

  rknn_output outputs[io_num.n_output];
  memset(outputs, 0, sizeof(outputs));
  for (int i = 0; i < io_num.n_output; i++)
  {
    outputs[i].index = i;
    outputs[i].want_float = 0;
  }

  // 执行推理
  ret = rknn_run(ctx, NULL);
  ret = rknn_outputs_get(ctx, io_num.n_output, outputs, NULL);

  // 后处理
  detect_result_group_t detect_result_group;
  std::vector<float> out_scales;
  std::vector<int32_t> out_zps;
  for (int i = 0; i < io_num.n_output; ++i)
  {
    out_scales.push_back(output_attrs[i].scale);
    out_zps.push_back(output_attrs[i].zp);
  }
  post_process((int8_t *)outputs[0].buf, (int8_t *)outputs[1].buf, (int8_t *)outputs[2].buf, height, width,
               box_conf_threshold, nms_threshold, pads, scale_w, scale_h, out_zps, out_scales, &detect_result_group);

  // 画框和概率
  char text[256];
  for (int i = 0; i < detect_result_group.count; i++)
  {
    detect_result_t *det_result = &(detect_result_group.results[i]);
    sprintf(text, "%s %.1f%%", det_result->name, det_result->prop * 100);
    printf("%s @ (%d %d %d %d) %f\n", det_result->name, det_result->box.left, det_result->box.top,
           det_result->box.right, det_result->box.bottom, det_result->prop);
    int x1 = det_result->box.left;
    int y1 = det_result->box.top;
    int x2 = det_result->box.right;
    int y2 = det_result->box.bottom;
    rectangle(orig_img, cv::Point(x1, y1), cv::Point(x2, y2), cv::Scalar(256, 0, 0, 256), 3);
    putText(orig_img, text, cv::Point(x1, y1 + 12), cv::FONT_HERSHEY_SIMPLEX, 0.4, cv::Scalar(255, 255, 255));
  }

  printf("save detect result to %s\n", out_path.c_str());
  imwrite(out_path, orig_img);
  ret = rknn_outputs_release(ctx, io_num.n_output, outputs);

  deinitPostProcess();

  // release
  ret = rknn_destroy(ctx);

  if (model_data)
  {
    free(model_data);
  }

  return 0;
}
~~~

这里备份一下，然后开始添加opencv的内容，就有点奇怪反正。添加应该是不难，这里需要改的地方也就只有图像处理相关的，我认为加还是很好加的，但是编译并不好编译，还是尽可能的选择在开发板上进行编译，这样的编译

~~~shell
apt install libtiff5=4.3.0-6ubuntu0.10 libopencv-dev
apt install cmake 
~~~

然后又删掉了一些没用的东西，例如后处理的选择，例如第四个参数等等，在备份一下子，现在添加一下关于opencv摄像头相关的内容，先改一下Cmake的配置文件。删掉opencv相关的路径设置，而是直接find

![image-20241130141253548](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411301412607.png)

经过测试发现是没问题的，可以直接使用系统里面的opencv，一切正常，然后继续修改，添加摄像头相关的内容,yolov5的倒是可以了



~~~c
// Copyright (c) 2021 by Rockchip Electronics Co., Ltd. All Rights Reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*-------------------------------------------
                Includes
-------------------------------------------*/
#include <dlfcn.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/time.h>
#include <iostream>

#include "opencv2/core/core.hpp"
#include "opencv2/highgui/highgui.hpp"
#include "opencv2/imgproc/imgproc.hpp"
#define _BASETSD_H

#include "RgaUtils.h"

#include "postprocess.h"

#include "rknn_api.h"
#include "preprocess.h"

using namespace cv;
using namespace std;

#define PERF_WITH_POST 1
/*-------------------------------------------
                  Functions
-------------------------------------------*/

static void dump_tensor_attr(rknn_tensor_attr *attr)
{
  std::string shape_str = attr->n_dims < 1 ? "" : std::to_string(attr->dims[0]);
  for (int i = 1; i < attr->n_dims; ++i)
  {
    shape_str += ", " + std::to_string(attr->dims[i]);
  }

  printf("  index=%d, name=%s, n_dims=%d, dims=[%s], n_elems=%d, size=%d, w_stride = %d, size_with_stride=%d, fmt=%s, "
         "type=%s, qnt_type=%s, "
         "zp=%d, scale=%f\n",
         attr->index, attr->name, attr->n_dims, shape_str.c_str(), attr->n_elems, attr->size, attr->w_stride,
         attr->size_with_stride, get_format_string(attr->fmt), get_type_string(attr->type),
         get_qnt_type_string(attr->qnt_type), attr->zp, attr->scale);
}

double __get_us(struct timeval t) { return (t.tv_sec * 1000000 + t.tv_usec); }

static unsigned char *load_data(FILE *fp, size_t ofst, size_t sz)
{
  unsigned char *data;
  int ret;

  data = NULL;

  if (NULL == fp)
  {
    return NULL;
  }

  ret = fseek(fp, ofst, SEEK_SET);
  if (ret != 0)
  {
    printf("blob seek failure.\n");
    return NULL;
  }

  data = (unsigned char *)malloc(sz);
  if (data == NULL)
  {
    printf("buffer malloc failure.\n");
    return NULL;
  }
  ret = fread(data, 1, sz, fp);
  return data;
}

static unsigned char *load_model(const char *filename, int *model_size)
{
  FILE *fp;
  unsigned char *data;

  fp = fopen(filename, "rb");
  if (NULL == fp)
  {
    printf("Open file %s failed.\n", filename);
    return NULL;
  }

  fseek(fp, 0, SEEK_END);
  int size = ftell(fp);

  data = load_data(fp, 0, size);

  fclose(fp);

  *model_size = size;
  return data;
}

static int saveFloat(const char *file_name, float *output, int element_size)
{
  FILE *fp;
  fp = fopen(file_name, "w");
  for (int i = 0; i < element_size; i++)
  {
    fprintf(fp, "%.6f\n", output[i]);
  }
  fclose(fp);
  return 0;
}

/*-------------------------------------------
                  Main Functions
-------------------------------------------*/
int main(int argc, char **argv)
{
  if (argc < 3)
  {
    printf("Usage: %s <rknn model> <input_image_path>\n", argv[0]);
    return -1;
  }
  int ret;
  rknn_context ctx;
  size_t actual_size = 0;
  int img_width = 0;
  int img_height = 0;
  int img_channel = 0;
  const float nms_threshold = NMS_THRESH;      // 默认的NMS阈值
  const float box_conf_threshold = BOX_THRESH; // 默认的置信度阈值
  char *model_name = (char *)argv[1];
  char *input_path = argv[2];

  // init rga context
  rga_buffer_t src;
  rga_buffer_t dst;
  memset(&src, 0, sizeof(src));
  memset(&dst, 0, sizeof(dst));

  printf("post process config: box_conf_threshold = %.2f, nms_threshold = %.2f\n", box_conf_threshold, nms_threshold);

  /* Create the neural network */
  printf("Loading mode...\n");
  int model_data_size = 0;
  unsigned char *model_data = load_model(model_name, &model_data_size);
  ret = rknn_init(&ctx, model_data, model_data_size, 0, NULL);
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }

  rknn_sdk_version version;
  ret = rknn_query(ctx, RKNN_QUERY_SDK_VERSION, &version, sizeof(rknn_sdk_version));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("sdk version: %s driver version: %s\n", version.api_version, version.drv_version);

  rknn_input_output_num io_num;
  ret = rknn_query(ctx, RKNN_QUERY_IN_OUT_NUM, &io_num, sizeof(io_num));
  if (ret < 0)
  {
    printf("rknn_init error ret=%d\n", ret);
    return -1;
  }
  printf("model input num: %d, output num: %d\n", io_num.n_input, io_num.n_output);

  rknn_tensor_attr input_attrs[io_num.n_input];
  memset(input_attrs, 0, sizeof(input_attrs));
  for (int i = 0; i < io_num.n_input; i++)
  {
    input_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_INPUT_ATTR, &(input_attrs[i]), sizeof(rknn_tensor_attr));
    if (ret < 0)
    {
      printf("rknn_init error ret=%d\n", ret);
      return -1;
    }
    dump_tensor_attr(&(input_attrs[i]));
  }

  rknn_tensor_attr output_attrs[io_num.n_output];
  memset(output_attrs, 0, sizeof(output_attrs));
  for (int i = 0; i < io_num.n_output; i++)
  {
    output_attrs[i].index = i;
    ret = rknn_query(ctx, RKNN_QUERY_OUTPUT_ATTR, &(output_attrs[i]), sizeof(rknn_tensor_attr));
    dump_tensor_attr(&(output_attrs[i]));
  }

  int channel = 3;
  int width = 0;
  int height = 0;
  if (input_attrs[0].fmt == RKNN_TENSOR_NCHW)
  {
    printf("model is NCHW input fmt\n");
    channel = input_attrs[0].dims[1];
    height = input_attrs[0].dims[2];
    width = input_attrs[0].dims[3];
  }
  else
  {
    printf("model is NHWC input fmt\n");
    height = input_attrs[0].dims[1];
    width = input_attrs[0].dims[2];
    channel = input_attrs[0].dims[3];
  }

  printf("model input height=%d, width=%d, channel=%d\n", height, width, channel);

  rknn_input inputs[1];
  memset(inputs, 0, sizeof(inputs));
  inputs[0].index = 0;
  inputs[0].type = RKNN_TENSOR_UINT8;
  inputs[0].size = width * height * channel;
  inputs[0].fmt = RKNN_TENSOR_NHWC;
  inputs[0].pass_through = 0;

  // 读取图片
  cv::VideoCapture capture(input_path);;
  while (true)
  {
    cv::Mat orig_img;
    capture >> orig_img;
    
    // 如果捕获到帧，则显示它
    if (!orig_img.empty())
    {
      cv::Mat img;
      cv::cvtColor(orig_img, img, cv::COLOR_BGR2RGB);
      img_width = img.cols;
      img_height = img.rows;
      printf("img width = %d, img height = %d\n", img_width, img_height);

      // 指定目标大小和预处理方式,默认使用LetterBox的预处理
      BOX_RECT pads;
      memset(&pads, 0, sizeof(BOX_RECT));
      cv::Size target_size(width, height);
      cv::Mat resized_img(target_size.height, target_size.width, CV_8UC3);
      // 计算缩放比例
      float scale_w = (float)target_size.width / img.cols;
      float scale_h = (float)target_size.height / img.rows;

      if (img_width != width || img_height != height)
      {
        printf("resize image with letterbox\n");
        float min_scale = std::min(scale_w, scale_h);
        scale_w = min_scale;
        scale_h = min_scale;
        letterbox(img, resized_img, pads, min_scale, target_size);
        inputs[0].buf = resized_img.data;
      }
      else
      {
        inputs[0].buf = img.data;
      }

      rknn_inputs_set(ctx, io_num.n_input, inputs);

      rknn_output outputs[io_num.n_output];
      memset(outputs, 0, sizeof(outputs));
      for (int i = 0; i < io_num.n_output; i++)
      {
        outputs[i].index = i;
        outputs[i].want_float = 0;
      }

      // 执行推理
      ret = rknn_run(ctx, NULL);
      ret = rknn_outputs_get(ctx, io_num.n_output, outputs, NULL);

      // 后处理
      detect_result_group_t detect_result_group;
      std::vector<float> out_scales;
      std::vector<int32_t> out_zps;
      for (int i = 0; i < io_num.n_output; ++i)
      {
        out_scales.push_back(output_attrs[i].scale);
        out_zps.push_back(output_attrs[i].zp);
      }
      post_process((int8_t *)outputs[0].buf, (int8_t *)outputs[1].buf, (int8_t *)outputs[2].buf, height, width,
                  box_conf_threshold, nms_threshold, pads, scale_w, scale_h, out_zps, out_scales, &detect_result_group);
      rknn_outputs_release(ctx, io_num.n_output, outputs);
      // 画框和概率
      char text[256];
      for (int i = 0; i < detect_result_group.count; i++)
      {
        detect_result_t *det_result = &(detect_result_group.results[i]);
        sprintf(text, "%s %.1f%%", det_result->name, det_result->prop * 100);
        printf("%s @ (%d %d %d %d) %f\n", det_result->name, det_result->box.left, det_result->box.top,
              det_result->box.right, det_result->box.bottom, det_result->prop);
        int x1 = det_result->box.left;
        int y1 = det_result->box.top;
        int x2 = det_result->box.right;
        int y2 = det_result->box.bottom;
        rectangle(orig_img, cv::Point(x1, y1), cv::Point(x2, y2), cv::Scalar(256, 0, 0, 256), 3);
        putText(orig_img, text, cv::Point(x1, y1 + 12), cv::FONT_HERSHEY_SIMPLEX, 0.4, cv::Scalar(255, 255, 255));
      }

      imshow("Camera", orig_img);

      // deinitPostProcess();
    }
    // 按下'q'键退出循环
    if (waitKey(1) == 'q')
    {
        break;
    }
  }
  // release
  ret = rknn_destroy(ctx);

  if (model_data)
  {
    free(model_data);
  }

  return 0;
}
~~~

行了，第一个摄像头例程改完了，我终于是找到了问题所在，果然是这个api的问题“**rknn_outputs_release**(ctx, io_num.n_output, outputs);”然后开始第二步，本来第二步是rga和mpp的学习的，但现在好不容易有了点进展，那就乘胜追击，去看一看rknn_model_zoo的yolov5是怎样写的。

~~~shell
./build-linux.sh -t rk3576 -a aarch64 -d yolov5
~~~

好麻烦啊，感觉rknn model zoo里面为啥要这样写，不是很理解，继续分析内容吧，直接来看主函数

~~~c
    // 判断输入参数是否正确，程序需要两个参数：模型路径和图片路径
    if (argc != 3)
    {
        // 如果参数不正确，打印使用方法
        printf("%s <model_path> <image_path>\n", argv[0]);
        return -1;
    }

    // 获取模型路径和图片路径
    const char *model_path = argv[1];
    const char *image_path = argv[2];

    int ret;
    rknn_app_context_t rknn_app_ctx;
    memset(&rknn_app_ctx, 0, sizeof(rknn_app_context_t));  // 初始化应用上下文
~~~

这里最重要的是第14行的内容，创建了一个ctx，对应结构体的具体内容如下所示：
~~~c
typedef struct {
    rknn_context rknn_ctx;
    rknn_input_output_num io_num;
    rknn_tensor_attr* input_attrs;
    rknn_tensor_attr* output_attrs;
    int model_channel;
    int model_width;
    int model_height;
    bool is_quant;
} rknn_app_context_t;
~~~

其实这里的修改也挺好的，这些都是一些必要的参数，在RKNPU2里面就是将这些内容分来的，其实方式都差不多，然后继续向下看，接下来是这个初始化后处理的功能。

~~~c
init_post_process();  // 初始化后处理
~~~

该函数的具体内容如下所示：
~~~c
int init_post_process()
{
    int ret = 0;
    ret = loadLabelName(LABEL_NALE_TXT_PATH, labels);
    if (ret < 0)
    {
        printf("Load %s failed!\n", LABEL_NALE_TXT_PATH);
        return -1;
    }
    return 0;
}
~~~

这里其实也是API函数的嵌套，这里的函数是loadLabelName，继续追踪该函数

~~~c
static int loadLabelName(const char *locationFilename, char *label[])
{
    printf("load lable %s\n", locationFilename);
    readLines(locationFilename, label, OBJ_CLASS_NUM);
    return 0;
}
~~~

感觉这个就是个匹配函数，数字 类别 名称等等，然后来看下面的内容，初始化模型

~~~c
    ret = init_yolov5_model(model_path, &rknn_app_ctx);
    if (ret != 0)
    {
        printf("init_yolov5_model fail! ret=%d model_path=%s\n", ret, model_path);
        goto out;
    }
~~~

这个函数的具体内容如下所示，好长啊：
~~~c
int init_yolov5_model(const char *model_path, rknn_app_context_t *app_ctx)
{
    int ret;
    int model_len = 0;
    char *model;
    rknn_context ctx = 0;

    // Load RKNN Model
    model_len = read_data_from_file(model_path, &model);
    if (model == NULL)
    {
        printf("load_model fail!\n");
        return -1;
    }

    ret = rknn_init(&ctx, model, model_len, 0);
    free(model);
    if (ret < 0)
    {
        printf("rknn_init fail! ret=%d\n", ret);
        return -1;
    }

    // Get Model Input Output Number
    rknn_input_output_num io_num;
    ret = rknn_query(ctx, RKNN_QUERY_IN_OUT_NUM, &io_num, sizeof(io_num));
    if (ret != RKNN_SUCC)
    {
        printf("rknn_query fail! ret=%d\n", ret);
        return -1;
    }
    printf("model input num: %d, output num: %d\n", io_num.n_input, io_num.n_output);

    // Get Model Input Info
    printf("input tensors:\n");
    rknn_tensor_attr input_attrs[io_num.n_input];
    memset(input_attrs, 0, sizeof(input_attrs));
    for (int i = 0; i < io_num.n_input; i++)
    {
        input_attrs[i].index = i;
        ret = rknn_query(ctx, RKNN_QUERY_INPUT_ATTR, &(input_attrs[i]), sizeof(rknn_tensor_attr));
        if (ret != RKNN_SUCC)
        {
            printf("rknn_query fail! ret=%d\n", ret);
            return -1;
        }
        dump_tensor_attr(&(input_attrs[i]));
    }

    // Get Model Output Info
    printf("output tensors:\n");
    rknn_tensor_attr output_attrs[io_num.n_output];
    memset(output_attrs, 0, sizeof(output_attrs));
    for (int i = 0; i < io_num.n_output; i++)
    {
        output_attrs[i].index = i;
        ret = rknn_query(ctx, RKNN_QUERY_OUTPUT_ATTR, &(output_attrs[i]), sizeof(rknn_tensor_attr));
        if (ret != RKNN_SUCC)
        {
            printf("rknn_query fail! ret=%d\n", ret);
            return -1;
        }
        dump_tensor_attr(&(output_attrs[i]));
    }

    // Set to context
    app_ctx->rknn_ctx = ctx;

    // TODO
    if (output_attrs[0].qnt_type == RKNN_TENSOR_QNT_AFFINE_ASYMMETRIC && output_attrs[0].type != RKNN_TENSOR_FLOAT16)
    {
        app_ctx->is_quant = true;
    }
    else
    {
        app_ctx->is_quant = false;
    }

    app_ctx->io_num = io_num;
    app_ctx->input_attrs = (rknn_tensor_attr *)malloc(io_num.n_input * sizeof(rknn_tensor_attr));
    memcpy(app_ctx->input_attrs, input_attrs, io_num.n_input * sizeof(rknn_tensor_attr));
    app_ctx->output_attrs = (rknn_tensor_attr *)malloc(io_num.n_output * sizeof(rknn_tensor_attr));
    memcpy(app_ctx->output_attrs, output_attrs, io_num.n_output * sizeof(rknn_tensor_attr));

    if (input_attrs[0].fmt == RKNN_TENSOR_NCHW)
    {
        printf("model is NCHW input fmt\n");
        app_ctx->model_channel = input_attrs[0].dims[2];
        app_ctx->model_height = input_attrs[0].dims[1];
        app_ctx->model_width = input_attrs[0].dims[0];
    }
    else
    {
        printf("model is NHWC input fmt\n");
        app_ctx->model_height = input_attrs[0].dims[2];
        app_ctx->model_width = input_attrs[0].dims[1];
        app_ctx->model_channel = input_attrs[0].dims[0];
    }
    printf("model input height=%d, width=%d, channel=%d\n",
           app_ctx->model_height, app_ctx->model_width, app_ctx->model_channel);

    return 0;
}
~~~

这些内容其实跟RKNPU2的API都一样，只是集成在了一起，简单，我们继续向下读。

~~~c
    image_buffer_t src_image;
    memset(&src_image, 0, sizeof(image_buffer_t));  // 初始化图像缓冲区
    ret = read_image(image_path, &src_image);  // 读取输入图像

    if (ret != 0)
    {
        // 如果读取图像失败，打印错误信息并跳转到out标签
        printf("read image fail! ret=%d image_path=%s\n", ret, image_path);
        goto out;
    }
~~~

就是这里的问题，在RKNPU2里面这里是使用的opencv的data，直接传递的，但是这里就不对了，这里是直接搞了一个结构体~~，这里有一个很重要的结构体，也就是image_buffer_t

~~~c
typedef struct {
    int width;
    int height;
    int width_stride;
    int height_stride;
    image_format_t format;
    unsigned char* virt_addr;
    int size;
    int fd;
} image_buffer_t;
~~~





```
cat /dev/ttyUSB2 & echo -e "AT+QCFG=\"usbnet\",0\r\n" > /dev/ttyUSB2
```



~~~c
    int width = img.cols;
    int height = img.rows;
    int width_stride = img.step[0];
    int height_stride = 1; // 可以根据需要调整
    image_format_t format;

    if (img.type() == CV_8UC1) {
        format = FORMAT_GRAY;
    } else if (img.type() == CV_8UC3) {
        format = FORMAT_BGR;
    } else if (img.type() == CV_8UC4) {
        format = FORMAT_BGRA;
    } else {
        format = FORMAT_UNKNOWN;
    }

    unsigned char* virt_addr = img.data;
    int size = img.total() * img.elemSize();
~~~





\# opencv

find_package(OpenCV REQUIRED)



 ${OpenCV_LIBS}



