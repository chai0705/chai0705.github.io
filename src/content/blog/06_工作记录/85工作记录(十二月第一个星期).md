---
title: 工作记录(十二月第二个星期)
date: 2025-12-01 10:26:56
categories:
  - 工作记录
link: 06_工作记录/85工作记录(十二月第一个星期)
---

# 工作记录(星期一)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点你将永远不快乐==

首先修改内核驱动文件dw-mipi-dsi2-rockchip.c：

找到struct dw_mipi_dsi2结构体，添加reset_gpio结构体定义

~~~c
struct gpio_desc *reset_gpio; 
~~~

在dw_mipi_dsi2_phy_ratio_cfg函数上方，定义一个dsi_external_bradge_power_on函数，函数内容如下所示：

~~~c
static void dsi_external_bradge_power_on(struct dw_mipi_dsi2 *dsi2)
{
	if (dsi2->reset_gpio) {
        gpiod_direction_output(dsi2->reset_gpio, 0); 
        usleep_range(1000, 2000);
        gpiod_direction_output(dsi2->reset_gpio, 1); 
        usleep_range(1000, 2000);
        gpiod_direction_output(dsi2->reset_gpio, 0); 
        usleep_range(1000, 2000);
    }
}
~~~

在dw_mipi_dsi2_pre_enable函数的out上方，调用dsi_external_bradge_power_on函数，

~~~c
dsi_external_bradge_power_on(dsi2);
~~~

然后找到dw_mipi_dsi2_probe函数，在platform_set_drvdata(pdev, dsi2);下方添加下面的内容

~~~c
    dsi2->reset_gpio = devm_gpiod_get_optional(dev, "reset", 0); 
    if (IS_ERR(dsi2->reset_gpio)) {
        ret = PTR_ERR(dsi2->reset_gpio);
        dev_err(dev, "failed to request reset GPIO: %d\n", ret);
        return ret;
    }
~~~

然后修改uboot驱动文件dw_mipi_dsi2.c，首先找到dw_mipi_dsi2结构体，添加reset_gpio结构体变量

~~~c
 struct gpio_desc reset_gpio;
~~~

然后找到dw_mipi_dsi2_connector_prepare函数，在dw_mipi_dsi2_get_mipi_pixel_clk函数上方添加以下内容

~~~c
    if (dm_gpio_is_valid(&dsi2->reset_gpio))
        dm_gpio_set_value(&dsi2->reset_gpio, 1);
    udelay(1000);
    if (dm_gpio_is_valid(&dsi2->reset_gpio))
        dm_gpio_set_value(&dsi2->reset_gpio, 0);
    udelay(1000);
    if (dm_gpio_is_valid(&dsi2->reset_gpio))
        dm_gpio_set_value(&dsi2->reset_gpio, 1);
~~~

然后找到probe函数在of_alias_get_id函数下，添加以下内容

~~~c
    ret = gpio_request_by_name(dev, "reset-gpios", 0,&dsi2->reset_gpio, GPIOD_IS_OUT);
    if (ret && ret != -ENOENT) {
         printf("%s: Cannot get reset GPIO: %d\n", __func__, ret);
    }
~~~





| 引脚号 | 名 称    | 功能 (SD 模式) | 功能 (SPI 模式)  |
| ------ | -------- | -------------- | ---------------- |
| 1      | DAT3/CS  | 数据线 3       | 片选 / 从选 (SS) |
| 2      | CMD/DI   | 命令线         | 主出从入 (MOSI)  |
| 3      | VSS1     | 电源地         | 电源地           |
| 4      | VDD      | 电源           | 电源             |
| 5      | CLK      | 时钟           | 时钟 (SCK)       |
| 6      | VSS2     | 电源地         | 电源地           |
| 7      | DAT0/DO  | 数据线 0       | 主人从出 (MISO)  |
| 8      | DAT1/IRQ | 数据线 1       | 保留             |
| 9      | DAT2/NC  | 数据线 2       | 保留             |





| No（引脚序号） | SD 引脚 | SPI 引脚 |
| -------------- | ------- | -------- |
| 1              | DAT2    | -        |
| 2              | DAT3    | CS       |
| 3              | CMD     | DI       |
| 4              | Vdd     | Vdd      |
| 5              | CLK     | SCLK     |
| 6              | Vss     | Vss      |
| 7              | DAT0    | DO       |
| 8              | DAT1    | -        |





| 寄存器名称 | 宽度   | 核心功能                                                     |
| ---------- | ------ | ------------------------------------------------------------ |
| CID        | 128bit | 卡唯一标识，含制造商 ID、产品信息、序列号、制造日期等核心身份数据 |
| RCA        | 16bit  | 本地系统动态分配的卡地址（仅 SDIO 模式支持，SPI 模式不适用） |
| CSD        | 128bit | 存储 SD 卡操作条件、数据传输特性等关键配置参数               |
| SCR        | 64bit  | 记录 SD 卡特定功能的配置信息（如扩展功能支持状态）           |
| OCR        | 32bit  | 定义 SD 卡关键工作条件，核心为操作电压范围参数               |



# 工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

___

昨天一天过的倒是还行，我说的是在工作的上面，在家过的确实是很有问题的，一到晚上就疲惫，只想躺着，这肯定是不行的，还是应该维系一下在家的一些行为，至少让看电脑应该成为一个常态，而不能直接就去睡觉，就算是睡觉也应该摘掉眼镜才对，摘掉眼镜总是比不摘更好的，晚上的事情晚上再说了，现在开始今天的任务了，今天的任务就是继续写驱动手册，然后写看门狗章节。

~~~c
#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/init.h>
#include <linux/sched.h>
#include <linux/slab.h>
#include <linux/fs.h>
#include <linux/errno.h>
#include <linux/types.h>
#include <linux/fcntl.h>
#include <linux/hdreg.h>
#include <linux/kdev_t.h>
#include <linux/vmalloc.h>
#include <linux/genhd.h>
#include <linux/blk-mq.h>
#include <linux/buffer_head.h>
#include <linux/bio.h>
#include <linux/blkdev.h>
#include <linux/mm.h> // 包含kmap/kunmap头文件

#define VMEM_SIZE  (1024*1024)  // 1M
#define QUEUE_DEPTH 2           // 队列深度

// 设备结构体
struct vmem_dev {
    unsigned char *vmembuf;     // 模拟块设备内存
    struct gendisk *gendisk; 
    struct blk_mq_tag_set tag_set; 
    struct request_queue *queue; // 保存请求队列指针
};

struct vmem_dev *dev; // 设备结构体指针

// 核心修复：完全适配4.19内核的队列处理函数
static blk_status_t _queue_rq(struct blk_mq_hw_ctx *hctx, const struct blk_mq_queue_data* bd)
{
    struct request *req = bd->rq;
    struct bio *bio = req->bio; // 手动获取第一个bio
    blk_status_t ret = BLK_STS_OK;
    unsigned long req_total_start = blk_rq_pos(req) << 9; // 请求总起始字节（扇区→字节）

    blk_mq_start_request(req);

    // 手动遍历bio链（bio->bi_next）
    while (bio && ret == BLK_STS_OK) {
        struct bvec_iter iter; // 4.19内核要求：必须用struct bvec_iter作为迭代器
        struct bio_vec bv;
        // 当前bio的起始字节 = 总起始 + (当前bio扇区 - 请求起始扇区) * 512
        unsigned long bio_start = req_total_start + 
                                 ((bio->bi_iter.bi_sector - blk_rq_pos(req)) << 9);
        unsigned long dev_addr = bio_start; // 设备中的操作地址

        // 遍历bio数据段：4.19内核正确用法（迭代器为struct bvec_iter）
        bio_for_each_segment(bv, bio, iter) {
            void *buffer;
            unsigned int len = bv.bv_len;

            // 4.19内核用kmap替代kmap_local_page
            buffer = kmap(bv.bv_page) + bv.bv_offset;

            // 边界检查：避免超出1M设备范围
            if (dev_addr >= VMEM_SIZE) {
                ret = BLK_STS_IOERR;
                kunmap(bv.bv_page);
                goto out;
            }
            if (dev_addr + len > VMEM_SIZE) {
                len = VMEM_SIZE - dev_addr;
            }

            // 读写数据
            if (rq_data_dir(req) == READ) {
                memcpy(buffer, dev->vmembuf + dev_addr, len);
            } else if (rq_data_dir(req) == WRITE) {
                memcpy(dev->vmembuf + dev_addr, buffer, len);
            } else {
                ret = BLK_STS_IOERR;
                kunmap(bv.bv_page);
                goto out;
            }

            // 更新地址和bio处理进度
            dev_addr += len;
            bio_advance(bio, len);
            kunmap(bv.bv_page); // 对应kmap，用kunmap卸载

            // 若当前bio已处理完，跳出循环
            if (bio->bi_iter.bi_size == 0)
                break;
        }

        // 处理下一个bio
        bio = bio->bi_next;
    }

out:
    blk_mq_end_request(req, ret);
    return ret;
}

static struct blk_mq_ops mq_ops = {
    .queue_rq = _queue_rq,
};

// 块设备操作函数
int vmem_open(struct block_device *bdev, fmode_t mode)
{
    printk(KERN_INFO "vmem: device opened\n");
    return 0;
}

void vmem_release(struct gendisk *disk, fmode_t mode)
{
    printk(KERN_INFO "vmem: device released\n");
}

int vmem_getgeo(struct block_device *bdev, struct hd_geometry *geo)
{
    geo->heads = 2;
    geo->cylinders = 32;
    geo->sectors = VMEM_SIZE / (geo->heads * geo->cylinders * 512);
    return 0;
}

static struct block_device_operations vmem_fops = {
    .owner = THIS_MODULE,
    .open = vmem_open,
    .release = vmem_release,
    .getgeo = vmem_getgeo,
};

// 初始化函数
static int __init vmem_init(void)
{
    int ret = 0;
    dev_t devno = 0;
    int major;

    // 1. 分配设备结构体
    dev = kzalloc(sizeof(*dev), GFP_KERNEL);
    if (!dev) {
        printk(KERN_ERR "vmem: failed to alloc dev struct\n");
        return -ENOMEM;
    }

    // 2. 分配模拟块设备内存
    dev->vmembuf = vmalloc(VMEM_SIZE);
    if (!dev->vmembuf) {
        printk(KERN_ERR "vmem: failed to alloc vmembuf\n");
        ret = -ENOMEM;
        goto err_free_dev;
    }
    memset(dev->vmembuf, 0, VMEM_SIZE);

    // 3. 初始化tag_set
    dev->tag_set.ops = &mq_ops;
    dev->tag_set.nr_hw_queues = 1;
    dev->tag_set.queue_depth = QUEUE_DEPTH;
    dev->tag_set.numa_node = NUMA_NO_NODE;
    dev->tag_set.flags = BLK_MQ_F_SHOULD_MERGE;

    ret = blk_mq_alloc_tag_set(&dev->tag_set);
    if (ret) {
        printk(KERN_ERR "vmem: failed to alloc tag set (ret=%d)\n", ret);
        goto err_free_vmembuf;
    }

    // 4. 创建请求队列
    dev->queue = blk_mq_init_queue(&dev->tag_set);
    if (IS_ERR(dev->queue)) {
        ret = PTR_ERR(dev->queue);
        printk(KERN_ERR "vmem: failed to init queue (ret=%d)\n", ret);
        goto err_free_tag_set;
    }

    // 5. 注册块设备号
    major = register_blkdev(0, "ramblock");
    if (major < 0) {
        printk(KERN_ERR "vmem: failed to register blkdev (ret=%d)\n", major);
        ret = major;
        goto err_cleanup_queue;
    }
    devno = MKDEV(major, 0);

    // 6. 分配gendisk
    dev->gendisk = alloc_disk(1);
    if (!dev->gendisk) {
        printk(KERN_ERR "vmem: failed to alloc gendisk\n");
        ret = -ENOMEM;
        goto err_unregister_blkdev;
    }

    // 7. 初始化gendisk
    dev->gendisk->major = major;
    dev->gendisk->first_minor = MINOR(devno);
    dev->gendisk->fops = &vmem_fops;
    dev->gendisk->queue = dev->queue;
    sprintf(dev->gendisk->disk_name, "ramblock");
    set_capacity(dev->gendisk, VMEM_SIZE >> 9);

    // 8. 注册磁盘
    add_disk(dev->gendisk);
    printk(KERN_INFO "vmem: initialized successfully (major=%d)\n", major);

    return 0;

    // 错误处理（逆序释放资源）
err_unregister_blkdev:
    unregister_blkdev(major, "ramblock");
err_cleanup_queue:
    blk_cleanup_queue(dev->queue);
err_free_tag_set:
    blk_mq_free_tag_set(&dev->tag_set);
err_free_vmembuf:
    vfree(dev->vmembuf);
err_free_dev:
    kfree(dev);
    return ret;
}

// 退出函数
static void __exit vmem_exit(void)
{
    int major = dev->gendisk->major;

    del_gendisk(dev->gendisk);
    put_disk(dev->gendisk);
    unregister_blkdev(major, "ramblock");
    blk_cleanup_queue(dev->queue);
    blk_mq_free_tag_set(&dev->tag_set);
    vfree(dev->vmembuf);
    kfree(dev);

    printk(KERN_INFO "vmem: unloaded successfully\n");
}

module_init(vmem_init);
module_exit(vmem_exit);
MODULE_LICENSE("GPL v2");
MODULE_AUTHOR("topeet");
MODULE_DESCRIPTION("1M RAM Block Device Driver (Linux 4.19 Compatible)");
~~~



# 工作记录(星期三)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

____



一、柴博洋11月份OKR：

继续学习研究海思的模型部署和测试流程，完善3516 测试例程

二、完成情况：

在十一月的第一个星期以及第二个星期的前半部分，写完了海思3516开发板的NPU例程测试手册，包括图像分类模型测试与目标检测模型测试，模型转换和仿真模拟，中间测试了RK3588 新地板的EDP接口，创建了最初版本的astrbot机器人，可以回复一些简单的问题，然后在十一月的第二个星期后半段开始了对astrbot机器人的优化，添加了不同SOC的手册分类数据库、优化并独立出不同的人格，支持图片ocr内容提取，在十一月的第三个星期解决了RK3588S软关机重启的问题，然后开始移植Hi3403的驱动开发手册，到现在整理到了第26章网络设备相关章节。

三、12月OKR:

整理完Hi3403驱动开发手册

移植linux 6.1源码



~~~shell
repo init --repo-url https://gerrit.rock-chips.com:8443/repo-release/tools/repo -u https://gerrit.rock-chips.com:8443/Android_U/manifests -b master -m Android14.xml
~~~

# 工作记录(星期四)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

___

唯愿追寻本心，一往无前。

以后可以变的更频繁一点，这倒是没有什么问题的。

现在的任务是测试适配客户的屏幕，有一个任务确实就挺好的，至少是比写手册要好很多的，现在来分析思考现在的这个任务要如何解决。

> 1.先整体编译安卓13，为后续调试做准备
>
> 2.通过Linux 来验证各个屏幕的触摸
>
> 3.研究学习安卓多屏异触

1.正在编译中

![image-20251204102211804](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202512041022928.png)

2.测试Linux镜像

分辨屏幕



## 1️⃣ 修改 `drm_edid.c`

路径：

```c
vim kernel/drivers/gpu/drm/drm_edid.c
```

搜索：

```c
static const struct drm_display_mode drm_dmt_modes[]
```

在表格末尾添加以下代码：

```c
/* custom - 1856x262@60Hz */
{ DRM_MODE("1856x262", DRM_MODE_TYPE_DRIVER, 74250, 
    1856, 1960, 2000, 2200, 0,
     262, 270, 275, 290, 0,
    DRM_MODE_FLAG_PHSYNC | DRM_MODE_FLAG_PVSYNC) },
```

------

## 2️⃣ 修改 `dw-hdmi.c`

路径：

```c
vim kernel/drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c
```

找到：

```c
static const struct drm_display_mode dw_hdmi_default_modes[]
```

在顶层添加：

```c
/* Custom mode - 1856x262@60Hz */
	{ DRM_MODE("1856x262", DRM_MODE_TYPE_DRIVER, 74250,
    1856, 1960, 2000, 2200, 0,
     262, 270, 275, 290, 0,
    DRM_MODE_FLAG_PHSYNC | DRM_MODE_FLAG_PVSYNC),
  .picture_aspect_ratio = HDMI_PICTURE_ASPECT_NONE, },
```

然后将drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c 的dw_hdmi_connector_get_modes函数修改为以下内容：

```c
static int dw_hdmi_connector_get_modes(struct drm_connector *connector)
{
	struct dw_hdmi_qp *hdmi = container_of(connector, struct dw_hdmi_qp, connector);
	struct drm_display_mode *mode;

	/* 不读 EDID，直接使用固定模式 */
	mode = drm_mode_duplicate(connector->dev, &dw_hdmi_default_modes[0]);
	if (!mode)
		return 0;

	drm_mode_probed_add(connector, mode);
	dev_info(hdmi->dev, "Force use fixed mode: %s\n", mode->name);

	return 1; /* 返回模式数量 */
}
```

然后修改 `drivers/gpu/drm/bridge/synopsys/Makefile`：

```
ccflags-y += -Wno-unused-function
```

除了改内核之外，还需要修改一下uboot，将uboot的hdmi显示给取消，否则内核会卡死。

修改一下，因为有两个HDMI接口，然后继续看一下多屏的代码

然后将drivers/gpu/drm/bridge/synopsys/dw-hdmi-qp.c 的dw_hdmi_connector_get_modes修改为以下内容

~~~c
static int dw_hdmi_connector_get_modes(struct drm_connector *connector)
{
    struct dw_hdmi_qp *hdmi =
        container_of(connector, struct dw_hdmi_qp, connector);
    struct hdr_static_metadata *metedata =
        &connector->hdr_sink_metadata.hdmi_type1;
    struct edid *edid;
    struct drm_display_mode *mode;
    struct drm_display_info *info = &connector->display_info;
    void *data = hdmi->plat_data->phy_data;
    struct drm_property_blob *edid_blob_ptr = connector->edid_blob_ptr;
    int i, ret = 0;
    static const struct drm_display_mode custom_mode = {
        DRM_MODE("1856x262", DRM_MODE_TYPE_DRIVER, 74250,
                 1856, 1960, 2000, 2200, 0,
                 262, 270, 275, 290, 0,
                 DRM_MODE_FLAG_PHSYNC | DRM_MODE_FLAG_PVSYNC)
    };
    /* 使用设备名判断（例如： "dwhdmi-rockchip fdea0000.hdmi"） */
    {
        const char *devname = dev_name(hdmi->dev);

        if (devname && strstr(devname, "fdea0000")) {
            dev_info(hdmi->dev, "[%s] Force fixed mode: 1856x262@60Hz\n", devname);


            mode = drm_mode_duplicate(connector->dev, &custom_mode);
            if (!mode)
                return 0;

            mode->type |= DRM_MODE_TYPE_PREFERRED;
            drm_mode_probed_add(connector, mode);

            hdmi->support_hdmi = true;
            hdmi->sink_has_audio = true;
            return 1;
        }
    }

    if (hdmi->plat_data->right && hdmi->plat_data->right->next_bridge) {
        struct drm_bridge *bridge = hdmi->plat_data->right->next_bridge;

        if (bridge->ops & DRM_BRIDGE_OP_MODES) {
            if (!drm_bridge_get_modes(bridge, connector))
                return 0;
        }
    }

    if (hdmi->panel)
        return drm_panel_get_modes(hdmi->panel, connector);

    if (hdmi->next_bridge && hdmi->next_bridge->ops & DRM_BRIDGE_OP_MODES)
        return drm_bridge_get_modes(hdmi->next_bridge, connector);

    if (!hdmi->ddc)
        return 0;

    memset(metedata, 0, sizeof(*metedata));

    if (edid_blob_ptr && edid_blob_ptr->length) {
        edid = kmalloc(edid_blob_ptr->length, GFP_KERNEL);
        if (!edid)
            return -ENOMEM;
        memcpy(edid, edid_blob_ptr->data, edid_blob_ptr->length);
    } else {
        edid = drm_get_edid(connector, hdmi->ddc);
        hdmi->hdcp_caps = dw_hdmi_qp_hdcp_capable(hdmi);
    }

    if (edid) {
        dev_dbg(hdmi->dev, "got edid: width[%d] x height[%d]\n",
            edid->width_cm, edid->height_cm);

        hdmi->support_hdmi = drm_detect_hdmi_monitor(edid);
        hdmi->sink_has_audio = drm_detect_monitor_audio(edid);
        drm_connector_update_edid_property(connector, edid);
        if (hdmi->cec_notifier)
            cec_notifier_set_phys_addr_from_edid(hdmi->cec_notifier, edid);
        if (hdmi->plat_data->get_edid_dsc_info)
            hdmi->plat_data->get_edid_dsc_info(data, edid);
        ret = drm_add_edid_modes(connector, edid);
        if (hdmi->plat_data->get_colorimetry)
            hdmi->plat_data->get_colorimetry(data, edid);
        if (hdmi->plat_data->get_yuv422_format)
            hdmi->plat_data->get_yuv422_format(connector, edid);
        dw_hdmi_update_hdr_property(connector);
        hdmi->hdcp_caps = dw_hdmi_qp_hdcp_capable(hdmi);
        if (ret > 0 && hdmi->plat_data->split_mode) {
            struct dw_hdmi_qp *secondary = NULL;
            void *secondary_data;

            if (hdmi->plat_data->left)
                secondary = hdmi->plat_data->left;
            else if (hdmi->plat_data->right)
                secondary = hdmi->plat_data->right;

            if (!secondary) {
                kfree(edid);
                return -ENOMEM;
            }
            secondary_data = secondary->plat_data->phy_data;

            list_for_each_entry(mode, &connector->probed_modes, head)
                hdmi->plat_data->convert_to_split_mode(mode);

            secondary->sink_is_hdmi = drm_detect_hdmi_monitor(edid);
            secondary->sink_has_audio = drm_detect_monitor_audio(edid);
            if (secondary->cec_notifier)
                cec_notifier_set_phys_addr_from_edid(secondary->cec_notifier,
                                     edid);
            if (secondary->plat_data->get_edid_dsc_info)
                secondary->plat_data->get_edid_dsc_info(secondary_data, edid);
        }
        kfree(edid);
    } else {
        hdmi->support_hdmi = true;
        hdmi->sink_has_audio = true;

        for (i = 0; i < ARRAY_SIZE(dw_hdmi_default_modes); i++) {
            const struct drm_display_mode *ptr =
                &dw_hdmi_default_modes[i];

            mode = drm_mode_duplicate(connector->dev, ptr);
            if (mode) {
                if (!i)
                    mode->type = DRM_MODE_TYPE_PREFERRED;
                drm_mode_probed_add(connector, mode);
                ret++;
            }
        }
        if (ret > 0 && hdmi->plat_data->split_mode) {
            struct drm_display_mode *mode;

            list_for_each_entry(mode, &connector->probed_modes, head)
                hdmi->plat_data->convert_to_split_mode(mode);
        }
        info->edid_hdmi_dc_modes = 0;
        info->hdmi.y420_dc_modes = 0;
        info->color_formats = 0;

        dev_info(hdmi->dev, "failed to get edid\n");
    }

    return ret;
}

~~~





~~~shell
console:/ $ cat /sys/kernel/debug/dri/0/summary                                
Video Port0: ACTIVE
    Connector: HDMI-A-1
        bus_format[2025]: YUV8_1X24
        overlay_mode[1] output_mode[f] color_space[3], eotf:0
    Display mode: 1920x1080p60
        clk[148500] real_clk[148500] type[40] flag[5]
        H: 1920 2008 2052 2200
        V: 1080 1084 1089 1125
    Esmart0-win0: ACTIVE
        win_id: 8
        format: AB24 little-endian (0x34324241) SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[1] csc mode[1]
        zpos: 2
        src: pos[0, 0] rect[1920 x 48]
        dst: pos[0, 0] rect[1920 x 48]
        buf[0]: addr: 0x0000000001705000 pitch: 7680 offset: 0
    Cluster0-win0: ACTIVE
        win_id: 0
        format: AB24 little-endian (0x34324241)[AFBC] SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[1] csc mode[1]
        zpos: 0
        src: pos[58, 313] rect[1164 x 654]
        dst: pos[0, 0] rect[1920 x 1080]
        buf[0]: addr: 0x00000000003d0000 pitch: 5120 offset: 0
    Cluster0-win1: ACTIVE
        win_id: 1
        format: AB24 little-endian (0x34324241)[AFBC] SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[1] csc mode[1]
        zpos: 1
        src: pos[0, 0] rect[1920 x 1080]
        dst: pos[0, 0] rect[1920 x 1080]
        buf[0]: addr: 0x0000000000eed000 pitch: 7680 offset: 0
    Esmart0-win1: ACTIVE
        win_id: 8
        format: AB24 little-endian (0x34324241) SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[1] csc mode[1]
        zpos: 2
        src: pos[0, 0] rect[1920 x 96]
        dst: pos[0, 984] rect[1920 x 96]
        buf[0]: addr: 0x0000000000ddf000 pitch: 7680 offset: 0
Video Port1: ACTIVE
    Connector: HDMI-A-2
        bus_format[100a]: RGB888_1X24
        overlay_mode[0] output_mode[f] color_space[0], eotf:0
    Display mode: 1856x262p116
        clk[74250] real_clk[74250] type[48] flag[5]
        H: 1856 1960 2000 2200
        V: 262 270 275 290
    Esmart1-win0: ACTIVE
        win_id: 10
        format: AB24 little-endian (0x34324241) SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[0] csc mode[0]
        zpos: 1
        src: pos[0, 0] rect[1856 x 262]
        dst: pos[0, 0] rect[1856 x 262]
        buf[0]: addr: 0x0000000000a29000 pitch: 7424 offset: 0
    Cluster1-win0: ACTIVE
        win_id: 2
        format: AB24 little-endian (0x34324241)[AFBC] SDR[0] color_space[0] glb_alpha[0xff]
        rotate: xmirror: 0 ymirror: 0 rotate_90: 0 rotate_270: 0
        csc: y2r[0] r2y[0] csc mode[0]
        zpos: 0
        src: pos[58, 313] rect[1164 x 654]
        dst: pos[695, 0] rect[465 x 262]
        buf[0]: addr: 0x00000000003d0000 pitch: 5120 offset: 0
Video Port2: DISABLED
Video Port3: DISABLED
console:/ $ 
~~~





~~~shell
vim device/rockchip/rk3588/rk3588_s/rk3588_s.mk
PRODUCT_PROPERTY_OVERRIDES += vendor.hwc.device.primary=HDMI-A-1
PRODUCT_PROPERTY_OVERRIDES += vendor.hwc.device.extend=HDMI-A-2
PRODUCT_PROPERTY_OVERRIDES += persist.sys.rotation.efull-1=true
~~~

、

```
make clean-ApiDemos
 make ApiDemos -j8
```





~~~shell
#!/bin/bash

# 获取显示器名称
display_name=$(xrandr | grep " connected" | awk '{ print $1 }')

# 判断是否存在触摸屏设备 ("goodix-ts" 或 "ft5x06")
touch_name=$(xinput list | grep -Ei "goodix-ts|ft5x06" | grep -oP '(?<=↳ ).*(?=\s+id)')
touch_id=$(xinput list | grep -Ei "goodix-ts|ft5x06" | grep -oP '(?<=id=)[0-9]+')

# 如果没有找到显示器，退出
if [ -z "$display_name" ]; then
    echo "没有找到连接的显示器！"
    exit 1
fi

# 提示是否启用触摸屏同步（如果没有找到触摸屏设备则跳过）
if [ -z "$touch_id" ]; then
    echo "警告：未检测到触摸屏设备，触摸屏同步将被跳过。"
    enable_touch=false
else
    enable_touch=true
fi

# 检查是否提供了参数
if [ -z "$1" ]; then
    echo "使用方法: $0 [选项]"
    echo "选项:"
    echo "1 - 正常显示"
    echo "2 - 向左旋转90度"
    echo "3 - 向右旋转90度"
    echo "4 - 旋转180度"
    exit 1
fi

# 判断输入参数
case $1 in
    1)
        # 正常显示
        xrandr --output "$display_name" --rotate normal
        if [ "$enable_touch" = true ]; then
            xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 1 0 0 0 1 0 0 0 1
        fi
        ;;
    2)
        # 向左旋转90度
        xrandr --output "$display_name" --rotate left
        if [ "$enable_touch" = true ]; then
            xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 -1 1 1 0 0 0 0 1
        fi
        ;;
    3)
        # 向右旋转90度
        xrandr --output "$display_name" --rotate right
        if [ "$enable_touch" = true ]; then
            xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 1 0 -1 0 1 0 0 1
        fi
        ;;
    4)
        # 旋转180度
        xrandr --output "$display_name" --rotate inverted
        if [ "$enable_touch" = true ]; then
            xinput set-prop "$touch_id" "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1
        fi
        ;;
    *)
        echo "无效选项！使用1、2、3或4作为参数。"
        exit 1
        ;;
esac

echo "显示器旋转完成。"
if [ "$enable_touch" = true ]; then
    echo "触摸屏旋转同步完成。"
else
    echo "未同步触摸屏旋转（未检测到触摸屏设备）。"
fi
~~~



水平竖直翻转

~~~c
xinput set-prop "8" "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1   //水平竖直翻转
xinput set-prop "8" "Coordinate Transformation Matrix" -1 0 1 0 1 0 0 0 1   //水平翻转
xinput set-prop "8" "Coordinate Transformation Matrix"  1 0 0 0 -1 1 0 0 1   //竖直翻转
xinput set-prop "8" "Coordinate Transformation Matrix"  1 0 0 0 1 0 0 0 1 //恢复默认
~~~



| 需求                 | 坐标变换矩阵数值   | 核心计算    |
| -------------------- | ------------------ | ----------- |
| 仅水平翻转（左右反） | -1 0 1 0 1 0 0 0 1 | X=-x+1，Y=y |
| 仅垂直翻转（上下反） | 1 0 0 0 -1 1 0 0 1 | X=x，Y=-y+1 |
| 恢复默认（取消翻转） | 1 0 0 0 1 0 0 0 1  | X=x，Y=y    |

xinput list



| 翻转需求                  | 完整执行命令                                                 | 适用场景                         |
| ------------------------- | ------------------------------------------------------------ | -------------------------------- |
| 180 度翻转（水平 + 垂直） | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1 | 屏幕旋转 180 度后，触摸同步翻转  |
| 仅水平翻转（左右反向）    | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" -1 0 1 0 1 0 0 0 1 | 屏幕镜像显示，触摸左右同步反     |
| 仅垂直翻转（上下反向）    | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 1 0 0 0 -1 1 0 0 1 | 屏幕上下倒置，触摸上下同步反     |
| 恢复默认（取消所有翻转）  | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 1 0 0 0 1 0 0 0 1 | 还原触摸坐标与屏幕坐标一致       |
| 90 度顺时针翻转           | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 1 0 -1 0 1 0 0 1 | 屏幕顺时针转 90 度，触摸同步适配 |
| 90 度逆时针翻转           | xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 -1 1 1 0 0 0 0 1 | 屏幕逆时针转 90 度，触摸同步适配 |

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 1 0 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" 1 0 0 0 -1 1 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" 1 0 0 0 1 0 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" 0 1 0 -1 0 1 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" 0 -1 1 1 0 0 0 0 1

![image-20251204110504235](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202512041105294.png)



~~~c
# 触摸ID=8，直接执行
xinput set-prop 8 "Coordinate Transformation Matrix" 0.283 0 0 0 0.040 0 0 0 1
~~~



~~~c
# 请将浮点数精确到小数点后五位，以获得最佳效果
xinput set-prop 8 "Coordinate Transformation Matrix" 0.02844 0 -0.00325 0 0.00381 0.00850 0 0 1
~~~

| 矩阵位置 | 数值 | 意义                        | 计算方法（你的参数）                         |
| -------- | ---- | --------------------------- | -------------------------------------------- |
| a        | -1   | X 轴缩放比例（核心）        | 屏幕宽度 / 触摸 X 最大值 = 1856/65535≈0.0283 |
| b        | 0    | X 轴与 Y 轴的耦合（无交互） | 固定为 0（2D 触摸无需耦合）                  |
| c        | 1    | X 轴偏移量（无偏移）        | 无翻转时 = 0（翻转时才需要调整）             |
| d        | 0    | Y 轴与 X 轴的耦合（无交互） | 固定为 0（2D 触摸无需耦合）                  |
| e        | 1    | Y 轴缩放比例（核心）        | 屏幕高度 / 触摸 Y 最大值 = 262/65535≈0.0040  |
| f        | 0    | Y 轴偏移量（无偏移）        | 无翻转时 = 0（翻转时才需要调整）             |
| g        | 0    | 透视变换（2D 触摸用不到）   | 固定为 0                                     |
| h        | 0    | 透视变换（2D 触摸用不到）   | 固定为 0                                     |
| i        | 1    | 齐次坐标缩放因子（固定）    | 固定为 1                                     |

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 0 0 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 1 0 0 0 1

xinput set-prop 8 "Coordinate Transformation Matrix"   1 0 0 0 1 0 0 0 1

 xinput set-prop 8 "Coordinate Transformation Matrix" -1.0317 0 0 0 0.0040 0 0 0 1

```
button release 1 a[0]=4176 a[1]=0 
button release 1 a[0]=62692 a[1]=0     
```



先X轴翻转

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 0 0 0 0 1

目前的现象是左侧有部分区域点不到，然后想着平移一下，但是平移又不知道能平移到哪里去

这个现在改的是X轴，还是很准的，然后开始改Y轴

```c
xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 0 0 0 0 1
```

~~~c
xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 1 0 0 0 1
~~~



~~~shell
xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 1 -1.07 0 0 1
~~~



~~~c
xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 1 0 0 0 1
~~~

xinput set-prop 8 "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1

# 工作记录(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着、或清醒的死去==

==我希望用一生的时间，不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

___

昨天搞了一天的客户屏幕，感觉这个方法是行不通的，总是存在一些问题，那就是通过xinput来修改，我的思路以及实现步骤肯定是没有问题的，先解决Linux系统里的触摸问题，然后开始研究多屏的触摸问题，然后再解决安卓系统的触摸问题。

___

目前的问题就在这里，y轴的坐标是对不上的，



欢迎加入迅为海思开发板大家庭！海思原厂文档内容非常详细，可在群公告网盘链接中的开发资料目录中下载。但是海思原厂文档门槛较高，阅读需要有一定的基础。如果刚拿到开发板，建议先阅读群文件中迅为编写的开发板手册，先熟悉开发板在使用海思原厂文档深入学习和开发。



~~~c
@迅为-AI助手酱    设置入群信息 欢迎加入迅为海思开发板大家庭！海思原厂文档内容详尽、专业性强，是深入开发的核心参考资料，可通过群公告的网盘链接，在「开发资料」目录中直接下载。不过原厂文档门槛较高，需要一定的技术基础才能高效理解。若您刚拿到开发板，建议优先阅读群文件中迅为专属编写的开发板手册，这份手册更贴合实操场景，能帮您快速熟悉开发板的硬件布局、基础操作与开发流程；待掌握核心基础后，再结合海思原厂文档开展深入学习与定制化开发，效率会更高～。祝您开发顺利，有任何问题都可以在群内交流！
    
@迅为-AI助手酱  设置欢迎图片  https://chai-1301855619.cos.ap-beijing.myqcloud.com/2025120411052.png
~~~



目前的问题就在这里，y轴的坐标是对不上的，现在就只能是这样了

~~~c
xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 1 0 0 0 1
~~~

然后继续找找问题。鼠标的话肯定是没问题的，我在接鼠标的情况下，鼠标的点击是没有任何问题的，甚至x轴在修改之后也是没有问题的，1856x262

~~~c
我现在有一个1856x262分辨率的特殊分辨率屏幕，默认情况下的触摸是不准的，我是ubuntu22系统，我在深入这个命令之后x轴精准了很多xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 1 0 0 0 1，但是y轴有部分是触摸不到的，帮我想想我要如何解决这个问题
~~~



~~~c
xinput set-prop 8 "Coordinate Transformation Matrix" -1.08 0 1.05  0 0.41 0.59 0 0 2
~~~

我的分辨率肯定是正确的，现在能怀疑的就是触摸屏的驱动，能不能抽象出来一个特有的这个触摸的驱动呢。



 eCalib 触摸校准命令

感觉在Linux上没有什么效果啊，不如早点换安卓

~~~c
xinput set-prop 8 "Coordinate Transformation Matrix" -1.05 0 0.99 0 0 0 0 0 1
~~~

Linux上的白板测试工具

~~~c
 sudo apt install openboard
~~~



~~~c
xinput --list-props 8 | grep "Coordinate Transformation Matrix"
~~~



~~~c
xinput set-prop 9 "Coordinate Transformation Matrix" -1.05 0 0.99 0 2 -1 0 0 1
~~~

# 工作记录(星期六)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

==清醒的活着，或清醒的死去==

___



> ```
> [    6.269886] input: eGalax Inc. eGalaxTouch CE6130 1234 v00_T1 k4.32.192 as /devices/platform/fc880000.usb/
> usb2/2-1/2-1.4/2-1.4:1.0/0003:0EEF:C002.0003/input/input12                                                   
> [    6.325030] input: eGalax Inc. eGalaxTouch CE6130 1234 v00_T1 k4.32.192 Mouse as /devices/platform/fc88000
> 0.usb/usb2/2-1/2-1.4/2-1.4:1.0/0003:0EEF:C002.0003/input/input14                                             
> [    6.325435] hid-multitouch 0003:0EEF:C002.0003: input,hiddev97,hidraw2: USB HID v1.11 Mouse [eGalax Inc. e
> GalaxTouch CE6130 1234 v00_T1 k4.32.192] on usb-fc880000.usb-1.4/input0   
> ```

```
[   73.354176] fixed_x = 1149, fixed_y = 11802
[   94.457147] fixed_x = 16310, fixed_y = 11113
[  107.668176] fixed_x = 1174, fixed_y = 16383
[  120.801177] fixed_x = 15898, fixed_y = 16261

```



Y轴的范围是4581，其实是跟262没关系的，跟16384you'guan'x

echo 7 4 1 7 > /proc/sys/kernel/printk



