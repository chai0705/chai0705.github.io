---
title: 工作记录((7月第3个星期)
date: 2024-07-03 21:26:56
categories:
  - 工作日志
link: 工作记录/21_工作记录(七月第三个星期)
---

# 工作记录(星期一)

nameserver 

弹窗

KO文件替换

脚本升级







我这里首先更新了一下软件源，然后就是安装一些依赖，就跟上面在PC上安装依赖的方法相同。

```
sudo apt-get install build-essential 

sudo apt-get install cmake git libgtk2.0-dev pkg-config libavcodec-dev libavformat-dev libswscale-dev

sudo apt-get install python-dev-is-python2 python-numpy libtbb2 libtbb-dev libjpeg-dev libpng-dev libtiff-dev  libdc1394-22-dev

pip3 install numpy
 
sudo update-alternatives --install /usr/bin/python python /usr/bin/python2 100
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3 150
```

然后将opencv源码拷贝到qumu的uubntu目录下，如下图所示：

![image-20231018220935063](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407161550456.png)

然后进行opencv源码的解压：

```
COPYunzip opencv-4.8.0.zip
```

然后在opencv源码目录下创建一个build目录进行工程

```
mkdir build
cd build
```

(2)构建动态库

```
cmake -D CMAKE_INSTALL_PREFIX=../install \
-D CMAKE_BUILD_TYPE=Release \
-D OPENCV_GENERATE_PKGCONFIG=ON \
-D WITH_QUIRC=ON  \
-D OPENCV_ENABLE_NONFREE=True \
-D OPENCV_GENERATE_PKGCONFIG=YES \
-D WITH_OPENGL=ON \
-D ENABLE_CXX11=1 \
-D WITH_OPENMP=ON \
-D WITH_1394=OFF \
-D INSTALL_C_EXAMPLES=OFF \
-D BUILD_EXAMPLES=OFF \
..

```



```c
#include "opencv2/core/core.hpp"
#include "opencv2/highgui/highgui.hpp"
#include "opencv2/imgproc/imgproc.hpp"
#include <iostream>

using namespace cv;
using namespace std;

int main(int argc, char** argv)
{
    if (argc < 3)
    {
        cout << "Usage: " << argv[0] << " <camera_type> <device_path>" << endl;
        cout << "camera_type: usb or mipi" << endl;
        return -1;
    }

    string camera_type = argv[1];
    string device_path = argv[2];
    VideoCapture capture;

    if (camera_type == "usb")
    {
        capture.open(device_path);
    }
    else if (camera_type == "mipi")
    {
        string pipeline = "v4l2src device=" + device_path + " ! video/x-raw, format=(string)NV12 ! appsink";
        capture.open(pipeline, CAP_GSTREAMER);
    }
    else
    {
        cout << "Invalid camera type. Use 'usb' or 'mipi'." << endl;
        return -1;
    }

    if (!capture.isOpened())
    {
        cout << "无法打开摄像头" << endl;
        return -1;
    }

    double time0 = static_cast<double>(getTickCount()); // 获取开始时间
    while (true)
    {
        // 从摄像头捕获帧
        Mat frame;
        capture >> frame;

        // 如果捕获到帧，则显示它
        if (!frame.empty())
        {
            if (camera_type == "mipi")
            {
                cvtColor(frame, frame, COLOR_YUV2BGR_NV12); // 将 NV12 格式转换为 BGR
            }
            double time1 = static_cast<double>(getTickCount()); // 获取结束时间
            double fps = getTickFrequency() / (time1 - time0); // 计算实时帧率
            time0 = time1; // 更新开始时间
            putText(frame, "FPS: " + to_string(fps), Point(10, 30), FONT_HERSHEY_SIMPLEX, 1, Scalar(0, 0, 255), 2); // 在图像上显示帧率
            imshow("Camera", frame);
        }

        // 按下'q'键退出循环
        if (waitKey(1) == 'q')
        {
            break;
        }
    }

    // 释放资源并关闭窗口
    capture.release();
    destroyAllWindows();

    return 0;
}
```

然后创建cmake的配置文件CMakeLists.txt，添加内容如下所示：

```
COPYcmake_minimum_required(VERSION 3.4.1)

# 设置项目名称为 opencv_demo
project(opencv_demo)

# 设置 C++ 标准为 11，并且要求编译器支持该标准
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# 查找并引入 OpenCV
find_package(OpenCV REQUIRED)

# 添加可执行文件 opencv_demo，源文件为 src/demo.cpp
add_executable(opencv_demo
        src/demo.cpp
)

# 链接 OpenCV 库
target_link_libraries(opencv_demo
  ${OpenCV_LIBS}
)

# 设置安装目录为 ./install/操作系统名
set(CMAKE_INSTALL_PREFIX ${CMAKE_SOURCE_DIR}/install/${CMAKE_SYSTEM_NAME})

# 安装可执行文件 opencv_demo 到指定目录
install(TARGETS opencv_demo DESTINATION ./)
```

z录，执行以下命令，进行配置、编译和安装三个步骤（==这里由于我是将opencv的库和头文件等放到了上一级的install目录，所以这里可能要修改一下CmakeLists，但也还好==）：

```
COPYcd build/
cmake ../
make 
make install 
```

![image-20231017230355394](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407161550974.png)image-20231017230355394

至此，opencv qemu编译就完成了。



~~~shell
export CC=aarch64-linux-gnu-gcc
export CXX=aarch64-linux-gnu-g++	
~~~







~~~c
#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>
#include <string.h>
#include <sys/ioctl.h>
#include <sys/mman.h>
#include <linux/videodev2.h>
#include <opencv2/opencv.hpp>
#include <iostream>

typedef struct camera_format {
    unsigned char description[32]; //字符串描述信息
    unsigned int pixelformat; //像素格式
} cam_fmt;

static cam_fmt cam_fmts[2];
int video_width = 640, video_height = 360; //摄像头采集数据的分辨率

//将YUYV格式的数据转换为RGB格式
void yuyv_to_rgb(unsigned char *yuyvdata, unsigned char *rgbdata, int w, int h) {
    int r, g, b;
    for (int i = 0; i < w * h / 2; i++) {
        unsigned char Y0 = yuyvdata[i * 4 + 0];
        unsigned char U  = yuyvdata[i * 4 + 1];
        unsigned char Y1 = yuyvdata[i * 4 + 2];
        unsigned char V  = yuyvdata[i * 4 + 3];

        int C1 = Y0 - 16;
        int C2 = Y1 - 16;
        int D = U - 128;
        int E = V - 128;

        r = (298 * C1 + 409 * E + 128) >> 8;
        g = (298 * C1 - 100 * D - 208 * E + 128) >> 8;
        b = (298 * C1 + 516 * D + 128) >> 8;

        rgbdata[i * 6 + 0] = (r > 255) ? 255 : ((r < 0) ? 0 : r);
        rgbdata[i * 6 + 1] = (g > 255) ? 255 : ((g < 0) ? 0 : g);
        rgbdata[i * 6 + 2] = (b > 255) ? 255 : ((b < 0) ? 0 : b);

        r = (298 * C2 + 409 * E + 128) >> 8;
        g = (298 * C2 - 100 * D - 208 * E + 128) >> 8;
        b = (298 * C2 + 516 * D + 128) >> 8;

        rgbdata[i * 6 + 3] = (r > 255) ? 255 : ((r < 0) ? 0 : r);
        rgbdata[i * 6 + 4] = (g > 255) ? 255 : ((g < 0) ? 0 : g);
        rgbdata[i * 6 + 5] = (b > 255) ? 255 : ((b < 0) ? 0 : b);
    }
}

int main(int argc, char *argv[]) {
    int ret, i;
    int fd;
    int frame_count = 0;
    int record_video = 0;
    std::string video_filename;

    if (argc < 2) {
        printf("Usage: %s <video_device> [capture_image] [record_video]\n", argv[0]);
        return -1;
    }

    if (argc > 2) {
        frame_count = std::stoi(argv[2]);
    }

    if (argc > 3) {
        record_video = 1;
        video_filename = argv[3];
    }

    //步骤一，打开视频设备
    fd = open(argv[1], O_RDWR);
    if (fd < 0) {
        printf("file open error\n");
        return -1;
    }

    //步骤二，查询设备能力
    struct v4l2_capability cap;
    if (ioctl(fd, VIDIOC_QUERYCAP, &cap) < 0) {
        perror("VIDIOC_QUERYCAP");
        return -1;
    }

    //步骤三，设置采集参数，视频帧宽度、高度、格式、视频帧率等信息
    struct v4l2_format fmt = {0};
    fmt.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    fmt.fmt.pix.width = video_width;
    fmt.fmt.pix.height = video_height;
    fmt.fmt.pix.pixelformat = V4L2_PIX_FMT_YUYV;
    if (ioctl(fd, VIDIOC_S_FMT, &fmt) < 0) {
        printf("ioctl error: VIDIOC_S_FMT\n");
        return -1;
    }

    struct v4l2_streamparm streamparm = {0};
    streamparm.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    ioctl(fd, VIDIOC_G_PARM, &streamparm);
    if (V4L2_CAP_TIMEPERFRAME & streamparm.parm.capture.capability) {
        streamparm.parm.capture.timeperframe.numerator = 1;
        streamparm.parm.capture.timeperframe.denominator = 30;
        if (0 > ioctl(fd, VIDIOC_S_PARM, &streamparm)) {
            printf("ioctl error: VIDIOC_S_PARM");
            return -1;
        }
    }

    //步骤四，请求帧缓冲
    struct v4l2_requestbuffers reqbuffer;
    reqbuffer.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    reqbuffer.count = 4;
    reqbuffer.memory = V4L2_MEMORY_MMAP;
    ret = ioctl(fd, VIDIOC_REQBUFS, &reqbuffer);
    if (ret < 0) {
        printf("Request Queue space failed \n");
        return -1;
    }

    //步骤五，映射帧缓冲
    struct v4l2_buffer mapbuffer;
    unsigned char *mptr[4];
    unsigned int size[4]; //存储大小，方便释放
    mapbuffer.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    for (i = 0; i < 4; i++) {
        mapbuffer.index = i;
        ret = ioctl(fd, VIDIOC_QUERYBUF, &mapbuffer);
        if (ret < 0) {
            printf("Kernel space queue failed\n");
            return -1;
        }
        mptr[i] = (unsigned char *)mmap(NULL, mapbuffer.length, PROT_READ | PROT_WRITE, MAP_SHARED, fd, mapbuffer.m.offset);
        size[i] = mapbuffer.length;
        //使用完毕,入队
        ret = ioctl(fd, VIDIOC_QBUF, &mapbuffer);
        if (ret < 0) {
            printf("ioctl error: VIDIOC_QBUF \n");
            return -1;
        }
    }

    //步骤六，开启视频采集
    int type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
    ret = ioctl(fd, VIDIOC_STREAMON, &type);
    if (ret < 0) {
        printf("ioctl error: VIDIOC_STREAMON \n");
        return -1;
    }

    //步骤七，读取数据、对数据进行处理
    unsigned char rgbdata[video_width * video_height * 3];
    cv::VideoWriter video_writer;

    if (record_video) {
        video_writer.open(video_filename, cv::VideoWriter::fourcc('M', 'J', 'P', 'G'), 30, cv::Size(video_width, video_height));
        if (!video_writer.isOpened()) {
            std::cerr << "Could not open the output video file for write\n";
            return -1;
        }
    }

    while (frame_count > 0 || record_video) {
        struct v4l2_buffer readbuffer;
        //出队列
        readbuffer.type = V4L2_BUF_TYPE_VIDEO_CAPTURE;
        ret = ioctl(fd, VIDIOC_DQBUF, &readbuffer);
        if (ret < 0) {
            printf("ioctl error:VIDIOC_DQBUF \n");
        }

        //将YUYV转换为RGB
        yuyv_to_rgb(mptr[readbuffer.index], rgbdata, video_width, video_height);

        if (frame_count > 0) {
            std::string filename = "capture_" + std::to_string(frame_count) + ".jpg";
            cv::Mat image(video_height, video_width, CV_8UC3, rgbdata);
            cv::imwrite(filename, image);
            frame_count--;
        }

        if (record_video) {
            cv::Mat frame(video_height, video_width, CV_8UC3, rgbdata);
            video_writer.write(frame);
        }

        //通知内核已经使用完毕，入队列
        ret = ioctl(fd, VIDIOC_QBUF, &readbuffer);
        if (ret < 0) {
            printf("ioctl error:VIDIOC_QBUF \n");
        }

        if (frame_count <= 0 && !record_video) {
            break;
        }
    }

    //步骤八，停止视频采集和释放资源
    ret = ioctl(fd, VIDIOC_STREAMOFF, &type);
    if (ret < 0) {
        printf("ioctl error:VIDIOC_STREAMOFF \n");
        return -1;
    }
    for (i = 0; i < 4; i++) {
        munmap(mptr[i], size[i]);
    }
    close(fd);
    
    if (record_video) {
        video_writer.release();
    }
    return 0;
}
~~~





# 工作记录(星期三)

x11perf -minpixwin500

![image-20240717154753671](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407171547758.png)



检查是否使用glamor

```html
cat /var/log/Xorg.0.log | grep glamor
```

![image-20240717154920038](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407171549097.png)

glamor是一种使用OpenGL的2D加速方式，并不是只有3D加速，



# 工作记录(星期四)

==当你在凝视深渊的时候，深渊也在凝视你==

___

将内容改到源码里面，其实本来也不多，至于我这个完全没必要。





#### 2.1 整体编译Android13 固件

##### ***\*注意：编译系统时一定要使用普通权限编译，保证虚拟机分配的内存大于\**** ***\*20G\*******\*。\****

进入到 3588-android13 文件夹，输入以下命令设置 java 版本为 1.8 版本，确认 java 版本是

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625862.png)1.8 版本之后，才可以进行下一步编译，如下图所示：

![image-20240720162717167](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20240720162717167.png)

|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625873.png) |

 ![image-20240720162729352](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201627399.png)

![image-20240720162746826](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201627861.png)

输入以下命令使能编译环境：

![image-20240720162805817](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201628866.png)

|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625869.png) |

 



![image-20240720162915149](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201629183.png)



输入命令查看编译目标：

![image-20240720170456929](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201704006.png)

 ![image-20240720162955928](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201629976.png)

![image-20240720171934476](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201719535.png)

![image-20240720171702613](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201717664.png)

![image-20240720171223674](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201712715.png)

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625865.jpg)![image-20240720172258006](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201722073.png) 

在这里我们输入版本对应的编号即可，我们的版本为第 86，topeet_rk3588-userdebug，我们这里输入 86 即可完成选择

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625881.png) 

我们也可以直接输入以下命令进行选择，选择结果和上述方法没有任何区别

​	lunch topeet_rk3588-userdebug	

最后输入以下命令开始整体编译

​	./build.sh -AUCKu	



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625877.png)



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625118.png) |

 



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625203.png)编译完成如下图所示，编译时间根据自身电脑配置而定，我这里给虚拟机分配的处理器内核总数为 8，处理器为 12th Gen Intel(R) Core(TM) i7-12700K 3.61GHz，用时大致为 3 小时左右:

 

 

 

 

 

 

 

 

 

 

 

 

编译生成的镜像统一放在 rockdev/Image-topeet_rk3588 目录下。

生成的镜像包括MiniLoaderAll.bin、uboot.img、baseparameter.img、resource.img、boot.img 、 misc.img 、 recovery.img 、 boot-debug.img 、 dtbo.img 、 pcba_small_misc.img、 pcba_whole_misc.img 、super.img 、vbmeta.img 等 13 个镜像，但是在烧写系统时，这 13 个 镜像并不需要全部使用。



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625226.png) |

 





![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625244.png) 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625260.png)

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625296.png)镜像作用如下表所示：

 

 

 

 

 

 

 

 

 

 

 

 

 

 

#### 2.2 单独编译Android13 固件

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625518.png)进入到 3588-android13 文件夹，输入以下命令设置 java 版本为 1.8 版本，如下图所示:

 



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625545.png) |

 



输入以下命令使能编译环境:



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625574.jpg) 

执行完上述命令，如果需要编译 uboot，则参考编译 uboot 小节，如果需要编译 Android内核，则参考编译 Android 内核小节，如果编译 Android 文件系统，则参考编译 Android 文件系统小节。

注意！要用同一个窗口普通用户进行编译。

2.2.1 编译 uboot

# 方法一：

在 Android 源码目录下执行如下命令编译 uboot：

​	./build.sh -U	



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625627.png)



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625684.png) |

 



编译完 uboot 如下图所示：



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625788.png) |

 



编译完成后在 u-boot 目录会生成 rk3588_spl_loader_v1.13.112.bin 文件、uboot.img 文件、

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625831.png)resource.img 三个镜像文件。

 

 

同 时 编 译 成 功 后 会 将 rk3588_spl_loader_v1.13.112.bin、uboot.img 拷 贝 到 rockdev/Image-topeet_rk3588 目录下，rk3588_spl_loader_v1.13.112.bin 会改名为 MiniLoaderAll.bin。MiniLoaderAll.bin 也就是一级 loader。



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625873.png)

只需要将 MiniLoaderAll.bin 和 uboot.img 烧写到开发板即可更新 uboot。

# 方法二：

方法一使用的是整体编译脚本,也可以使用 u-boot 下的编译脚本。

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625893.png)如果只是调试 uboot 或者 rkbin，可以在 u-boot 目录下直接编译，这样编译速度更快，如下图所示，输入以下命令：



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625933.png) |

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625154.png) 



 

这样编译后会在 uboot 目录下生成 uboot.img 和 rk3588_spl_loader_v1.13.112.bin，但是不会拷贝到 rockdev/Image-topeet_rk3588 下面，所以需要烧写 uboot 目录下的 uboot.img 和

rk3588_spl_loader_v 1.13.112.bin

烧写时将 rk3588_spl_loader_v1.13.112.bin 重命名为 MiniLoaderAll.bin 再烧写。

2.2.2 编译 Android 内核

# 方法一：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625208.png)在 Android 源码目录下执行如下命令编译 Android 内核：



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625230.jpg) 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625273.png)编译完成后如下图所示：

 

 

 

 

 

 

 

 

 

 

 

编译后会在 rockdev/Image-topeet_rk3588 目录下生成 boot.img，boot.img 为内核镜像。

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625305.png)boot.img 镜像里面包含了设备树镜像。所以烧写 boot.img 即可更新内核镜像。解释如下：



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625348.png)

# 方法二：

此方法常用于 kernel 的开发和调试，以下的方法既编译 kernel 部分时， 同时打包成

boot.img， 这样加快了我们开发的速度；

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625501.png)该种方法需要我们自行安装内核的交叉编译环境 aarch64-linux-gnu-gcc 编译器，在编译前我们可以输入以下命令，然后双击 Tab 键查看交叉编译环境是否已经安装



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625523.png) |

 



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625637.png)如果能出现上图红框中的编译环境则说明环境已经安装，若没有显示则需要输入以下命令安装 aarch64-linux-gnu-gcc 交叉编译器

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625653.png)安装成功后再次检查交叉编译器是否安装成功，安装成功后即可进入编译环节。进入内核目录下，输入以下命令配置环境：

 

 

配置好环境后输入以下命令进行编译，j32 代表所用的线程数为 32 个，可以根据自己虚拟机的配置自行更改



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625725.png)



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625753.png) |

 



编译完成如下图所示

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625843.png)

打包完后，在 kernel 目录有 boot.img 镜像生成，就可以把这个 boot.img 镜像单独烧入到机器中进行调试了。

 

2.2.3 编译 Android 文件系统

# 方法一：

在源码根目录的 build.sh 脚本，找到 make installclean 命令，将 make installclean 注释掉,这里需要注意的是，build.sh 脚本为只读脚本，普通用户下是无法进行文件修改的，需要在 root超级用户模式下将脚本打开，修改完成后输入 wq!进行强制保存即可。



![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625977.png) 

接着在 Android 源码目录下执行如下命令编译 Android 文件系统：

​	./build.sh-A	



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625049.png) |

 



Android 系统编译完如下图所示：



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625094.png) 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625157.png)编译完成后镜像文件会在 rockdev/Image-topeet_rk3588 目录下生成 super.img,单独烧写 super.img 即可。



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625194.png) |

 



各种镜像解释如下：



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625270.png)

# 方法二：

在 Android 源码编译的时候，如果编译整个工程可能需要两三个小时，即使二次编译也要二十分钟，如果只想单独编译某个模块，则可以使用 mm 命令，编译出来的 so 库或者其他文件，使用 adb 工具 push 到机器中进行替换，重启系统验证，不必重新烧写固件。

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625493.png)在使用 m/mm/mmm 命令之前，首先要确保执行以下命令，以下命令的作用是将 mm 等这些命令添加到环境变量当中去，没执行的话，会找不到命令。

 

 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625554.png)我们来了解下命令的含义：

 

 

比如说，修改了 HAL 层的音频文件：hardware/rockchip/audio/tinyalsa_hal/audio_hw.c，就可以使用 mm 命令来进行编译，首先要进入到 hardware/rockchip/audio/tinyalsa_hal/目录下，注意要有 Android.mk 文件哦，然后直接输入 mm 编译即可，如下图所示：

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625577.png) 

编译完成如下图所示：



 

![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625615.png)



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625651.png) |

 



在 out 目录就有新生成的 so 文件：

out/target/product/topeet_rk3588/vendor/lib/hw/audio.primary.rk30board.so 或者



|      |                                                              |
| ---- | ------------------------------------------------------------ |
|      | ![img](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202407201625678.png) |

out/target/product/topeet_rk3588/vendor/lib64/hw/audio.primary.rk30board.so



分别把lib 和lib64 目录下的audio.primary.rk30board.so 使用adb 工具push 到Android 开发板的/vendor/lib/hw 或者/vendor/lib64/hw 目录，重启系统验证。

使用 mmm 命令和 mm 命令最大的区别是 mmm 命令后面需要带上全路径。编译结果和

mm 命令编译相同。

如果无法使用 mm 编译，可以使用方法一来编译 android 的整个部分。















