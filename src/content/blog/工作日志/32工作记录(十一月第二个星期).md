---
description: "11月12日星期二 今天发现了很多的问题，首先就是RK3576的源码问题，上午先是解决了半天扩容的问题，这个问题解决了目前认为是一些工具版本的问题，目前也就先这样，我的做法是放弃掉ubuntu20 和debian11，只提供三种系统，也就是buildroot、debian12和ubutnu22，这"
cover: /img/cover/11.webp

title: 工作记录(十一月第二个星期)
date: 2024-11-10 10:26:56
categories:
  - 工作日志
link: 工作日志/32工作记录(十一月第二个星期)
---

# 11月12日(星期二)

今天发现了很多的问题，首先就是RK3576的源码问题，上午先是解决了半天扩容的问题，这个问题解决了目前认为是一些工具版本的问题，目前也就先这样，我的做法是放弃掉ubuntu20 和debian11，只提供三种系统，也就是buildroot、debian12和ubutnu22，这三个系统提供了就可以了，需要改的有编译脚本和转换脚本，目前已经改完了。

第二个问题是，喇叭和耳机接口的问题，不知道为啥，总是不能正常播放，果姐姐给我说了修改的方法，但是仍旧是不能正确的播放。

第三个问题其实是跟第二个问题一起的，因为我的耳机、喇叭不能正常控制，所以我就想通过图形化界面来进行设备的切换，我发现我的ubuntu和debian不能正常的点亮了，这就很奇怪了，但是我换上默认的debian12我发现就又没问题，这里很奇怪，很奇怪，我不是很理解，现在仍然在寻找问题，我认为这个问题的优先级应该要在第二个问题上面。

第四个问题是法师要求测试的一个问题，先把这个问题解决掉，然后从下到上依次去解决问题。

## 问题4解决

![image-20241112153957855](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411121539904.png)

~~~shell
ip link set can0 down
ip link set can0 type can bitrate 1000000
ip -details link show can0
ip link set can0 up
candump can0 &
cansend can0 123#DEADBEEF
~~~

CAN我没有更新吗，奇怪了，我明明记得我是有更新的啊，趁这个机会更新一下，先更新全能板子的，邮票孔的感觉有点费劲，就先待定了，然后继续来分析一下客户的内容：
~~~c
#include <stdio.h>
#include <sys/ioctl.h>
#include <arpa/inet.h>
#include <net/if.h>
#include <linux/socket.h>
#include <linux/can.h>
#include <linux/can/error.h>
#include <linux/can/raw.h>
#include <fcntl.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>
#include <errno.h>
#include <time.h>
#ifndef AF_CAN
#define AF_CAN 29
#endif
#ifndef PF_CAN
#define PF_CAN AF_CAN
#endif

#include <sys/time.h>  
	


static int test_can(int fd, int master)
{
	 struct can_frame frame; //定义一个 can_frame 变量
	int ret;
	struct timeval tv1,tv2;

	frame.can_id = 0x606;
	frame.can_dlc = 8; 
	frame.data[0] = 0x40; 
	frame.data[1] = 0x17; 
	frame.data[2] = 0x10; 
	
    gettimeofday(&tv1, NULL); 	
	ret = send(fd, &frame, sizeof(frame),0); //发送数据
	if(sizeof(frame) != ret) //如果 ret 不等于帧长度，就说明发送失败
		perror("write error XXX"); 
	gettimeofday(&tv2, NULL);
	printf("canSend  %d u\n", tv2.tv_usec - tv1.tv_usec); 
	
	struct can_frame framer[50];
	 ret = read(fd, &framer[0], sizeof(frame)*50);
	gettimeofday(&tv2, NULL);
	printf("canRecv  %d u\n", tv2.tv_usec - tv1.tv_usec);  
	
	return 0;
 
}
int main(int argc, char *argv[])
{
    int s;
    int ret;
    struct sockaddr_can addr;
    struct ifreq ifr;
    int master;
    srand(time(NULL));
    s = socket(PF_CAN, SOCK_RAW, CAN_RAW);                        /* 创建套接字    */
    if (s < 0) {
        perror("socket PF_CAN failed");
        return 1;
    }
    /* 把套接字绑定到can0接口    */
    strcpy(ifr.ifr_name, "can0");
    ret = ioctl(s, SIOCGIFINDEX, &ifr);                
    if (ret < 0) {
        perror("ioctl failed");
        return 1;
    }
    addr.can_family = PF_CAN;
    addr.can_ifindex = ifr.ifr_ifindex;
    ret = bind(s, (struct sockaddr *)&addr, sizeof(addr));    
    if (ret < 0) {
        perror("bind failed");
        return 1;
    }

	while (1)
	{	/* 进入测试 */
	test_can(s, master);
	usleep(10000);
	}
	#if 0
	while (1)
	{
		struct can_frame frame; //定义一个 can_frame 变量
		frame.can_id = 0x80;//如果为扩展帧，那么 frame.can_id = CAN_EFF_FLAG | 123;
		frame.can_dlc = 0; //数据长度为 3
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		
		frame.can_id = 0x301;
		frame.can_dlc = 8;
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		frame.can_id = 0x302;
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		frame.can_id = 0x305;
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		frame.can_id = 0x30b;
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		frame.can_id = 0x317;
		ret = send(s, &frame, sizeof(frame),0); //发送数据
		usleep(1000);
	}
	#endif
    close(s);
    return 0;
}
~~~



# 11月13日 工作记录（星期三）

![image-20241113105124742](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411131051131.png)

蓝牙可以了，果然蓝牙才需要那两个配置文件，WIFI是不需要的，除了这个配置文件对应的驱动程序也需要更新，不然还是不能用。

![image-20241113152235191](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411131522678.png)

先来找es8388具体要怎么适配，找其中的原理部分。先来看耳机的这个地方，具体如下所示：

![image-20241113171349736](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411131713786.png)

我说一下我的认知奥，现在R140是焊接了的，HP_DET_L这个引脚默认是接到了高电平上，所以在不插耳机的情况下，他应该是高电平，而插上耳机之后，这个2引脚会断开，从而达到低电平，通过对这个引脚的检测，可以判断耳机是否插入，现在去看看RK3588对于这个设备树的配置。

~~~c
~~~



# 11月14日 工作记录(星期四)

==摆脱一份幻觉币发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

昨天先是测试了一下RK3588全能板的喇叭和耳机接口，全能板肯定是没问题的，但是不能动态的切换，这一点有问题，我认为这一点需要改一下，但是换到全能板就不是这样了，需要再测试看看，感觉是检测引脚的上拉和下拉是不一样的。先看一下原理图，没啥问题

![image-20241114130423030](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411141304159.png)

问题其实出在这里的spk-con-gpio的问题上，全能板和精英板是不一样的，这里需要使能才能有声音，不然扬声器实际上是没有作用的，也就是这里的1脚：
![image-20241114130720510](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411141307563.png)

行了，问题解决，然后继续去搞RK3576,在去搞RK3576之前重新写一下反思。现在开始继续搞RK3576.

# 11月15日 工作记录(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

___

问题1：整体编译打包两次的问题

问题2：buildroot系统桌面和一些配置的选择。

先来看问题1.找到问题了，我发现是在firmware脚本里多了一个打包的操作，删掉就好了，我现在需要重新做一个convert脚本，脚本制作好了，然后重新做一个源码，源码差不对多了，现在开始搞第二个问题，因为RK3576和RK3568他们的GPU是一样的，所以我认为他们应该是可以共用一个buildroot镜像的，所以我现在就烧写看看

sudo apt install libsqlite3-dev libpcre2-dev

![image-20241116150652360](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202411161506509.png)







f