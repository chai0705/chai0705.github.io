---
description: "工作记录星期一 摆脱一份幻觉比发现一个真理更能使人明智 当你在凝视深渊的时候，深渊也在凝视你 弱小和无知并不是生存的障碍，傲慢才是 若想除掉旷野里的杂草，最好的办法就是种上一片庄稼 _____ 周五因为技术支持耗费了大量的时间，周六因为浮躁又耗费了大量的时间，所以周五周六对于串口这个东西来说实际上"
cover: /img/cover/3.webp

title: 工作记录(一月第四个星期)
date: 2025-01-20 10:26:56
categories:
  - 工作日志
link: 工作日志/42工作记录(一月第四个星期)
---

# 工作记录(星期一)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==若想除掉旷野里的杂草，最好的办法就是种上一片庄稼==

_____

周五因为技术支持耗费了大量的时间，周六因为浮躁又耗费了大量的时间，所以周五周六对于串口这个东西来说实际上是没有什么特别大的进展的，今天没有技术支持，那倒是还好，其实吧，我认为呢，技术支持并不是导致我效率低下的问题，问题其实在于我的心理作用，我总是会认为有人在找我，人们需要我去解决问题，但是实际上呢，并不会有人来找你，就算是有，其实也并不急迫，甚至绝大多少都是一些没有营养的废话，垃圾话，除了干扰你的内心，扰乱你的心情，慢慢来吧，其实最重要的决定就在早晨，做任何一件事情都必然是从最开始的时候就崩盘的，千里之堤，溃于蚁穴，行百里者半九十，往往都是最开始的这段路是最艰苦的，也往往是最后的这一段路是最难坚持的，所以加油啊。

____

遇到了一个问题，还是这里校验位的问题，具体代码如下所示：

~~~c
    switch (parity) 
    // 设置校验位
    {
        case 'O':  // 奇校验
            options.c_cflag |= PARENB;
            options.c_cflag |= PARODD;
            options.c_iflag |= (INPCK | ISTRIP);
            break;
        case 'E':  // 偶校验
            options.c_cflag |= PARENB;
            options.c_cflag &= ~PARODD;
            options.c_iflag |= (INPCK | ISTRIP);
            break;
        case 'N':  // 无校验
            options.c_cflag &= ~PARENB;
            break;
        default:
            options.c_cflag &= ~PARENB;
            break;
    }
~~~

这些最基础的代码我懂啊，就比如奇数的第一二句，就比如偶数的第一二句，但是第三句我就不是很懂了，所以我决定要去看一下法师讲解的视频,但是法师确实也没讲解啥，只是说了一下他们分别代表啥，要是我没写第二个会可以吗，应该是不行的，具体的原因如下所示

`ISTRIP` 标志用于剥离输入字符的第八位，即将接收到的每个字符的最高位（第八位）清零。

为什么要剥离输入字符的第八位？

剥离第八位通常用于以下情况：

1. **传输 7 位数据**：在某些通信协议中，数据仅使用 7 位表示，最高位用于其他目的或未定义。此时，设置 `ISTRIP` 可确保接收的数据符合预期的 7 位格式。
2. **处理纯 ASCII 数据**：ASCII 字符集使用 7 位表示字符。通过剥离第八位，可以确保接收到的数据在传输过程中未被高位噪声或错误影响，保持数据的纯净性。

接下来来看数据位：
~~~c

// 设置数据位
switch (data_bits) 
{
    case 7:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS7; // 设置CS7位，即设置数据位为7位
        break;
    case 8:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS8; // 设置CS8位，即设置数据位为8位
        break;
    default:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS8; // 默认设置数据位为8位
        break;
}
~~~

其实学了这个| & 的操作之后，确实是简单了，如果是想置1就用| ,如果想置零就用&=~，这样操作肯定是对的。所以这个数据位的设置倒是也没啥，CSIZE， CS8 CS7这些宏定义罢了，然后再看下面的，设置停止位

~~~c
// 设置停止位
switch (stop_bits) 
{
    case 1:
        options.c_cflag &= ~CSTOPB; // 清除CSTOPB位，即设置停止位为1位
        break;
    case 2:
        options.c_cflag |= CSTOPB; // 设置CSTOPB位，即设置停止位为2位
        break;
    default:
        options.c_cflag &= ~CSTOPB; // 默认设置停止位为1位
        break;
}
~~~

上面的奇偶校验还是有些问题啊，为啥有奇偶校验就要设置第八位去除呢，这没道理啊,感觉应该适当的学习一下奇偶校验，什么是奇校验，什么是偶校验，我认为我还是需要好好的讲解一下的。经过一系列内容的查看，发现，这个其实并不是必要的，所以以后我写的时候都不在这样写了，默认都去掉这一行。

~~~c
    // 根据需要设置奇偶校验
    switch (parity) {
        case 'N': // 无奇偶校验
            options.c_cflag &= ~PARENB; // 清除奇偶校验
            break;
        case 'E': // 偶校验
            options.c_cflag |= PARENB;   // 启用奇偶校验
            options.c_cflag &= ~PARODD;  // 设置为偶校验
            break;
        case 'O': // 奇校验
            options.c_cflag |= PARENB;   // 启用奇偶校验
            options.c_cflag |= PARODD;    // 设置为奇校验
            break;
        default:
            printf("无效的校验类型！\n");
            return -1;
    }

~~~

我写的时候肯定是按照标准来的，首先是数据位、奇偶校验位、停止位、然后是波特率，当然也可以是波特率在最前面，这当然也是可以的，我是绝对要自己敲一遍的。

~~~c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <termios.h>
#include <string.h>

#define BUFFER_SIZE 256  // 定义缓冲区大小为256字节

void set_serial_port(int fd, int baud_rate, int data_bits, int stop_bits, int parity) 
// 设置串口的波特率、数据位、停止位和校验位
{
    struct termios options;
    tcgetattr(fd, &options);  // 获取串口的设置选项
    options.c_cflag &= ~CSIZE;  // 设置数据位为8位
    options.c_lflag &= ~(ICANON | ECHO | ECHOE | ISIG);  // 关闭规范模式、回显和信号处理
    options.c_oflag &= ~OPOST;  // 关闭输出处理
    options.c_iflag &= ~(IXON | IXOFF | IXANY);  // 关闭输入处理

    options.c_cc[VTIME] = 0;  // 设置读取时的超时时间
    options.c_cc[VMIN] = 0;  // 设置读取时的最小字节数

    switch (baud_rate) 
    // 设置波特率
    {
        case 9600:
            cfsetispeed(&options, B9600);
            cfsetospeed(&options, B9600);
            break;
        case 19200:
            cfsetispeed(&options, B19200);
            cfsetospeed(&options, B19200);
            break;
        case 38400:
            cfsetispeed(&options, B38400);
            cfsetospeed(&options, B38400);
            break;
        case 57600:
            cfsetispeed(&options, B57600);
            cfsetospeed(&options, B57600);
            break;
        case 115200:
            cfsetispeed(&options, B115200);
            cfsetospeed(&options, B115200);
            break;
        default:
            cfsetispeed(&options, B115200);
            cfsetospeed(&options, B115200);
            break;
    }

    options.c_cflag |= CLOCAL | CREAD;  // 必须设置的标志，表示启用本地连接和接收数据
    options.c_cflag &= ~CRTSCTS;  // 关闭硬件流控制

    switch (parity) 
    // 设置校验位
    {
        case 'O':  // 奇校验
            options.c_cflag |= PARENB;
            options.c_cflag |= PARODD;
            break;
        case 'E':  // 偶校验
            options.c_cflag |= PARENB;
            options.c_cflag &= ~PARODD;
            break;
        case 'N':  // 无校验
            options.c_cflag &= ~PARENB;
            break;
        default:
            options.c_cflag &= ~PARENB;
            break;
    }

// 设置数据位
switch (data_bits) 
{
    case 7:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS7; // 设置CS7位，即设置数据位为7位
        break;
    case 8:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS8; // 设置CS8位，即设置数据位为8位
        break;
    default:
        options.c_cflag &= ~CSIZE; // 清除CSIZE位
        options.c_cflag |= CS8; // 默认设置数据位为8位
        break;
}
// 设置停止位
switch (stop_bits) 
{
    case 1:
        options.c_cflag &= ~CSTOPB; // 清除CSTOPB位，即设置停止位为1位
        break;
    case 2:
        options.c_cflag |= CSTOPB; // 设置CSTOPB位，即设置停止位为2位
        break;
    default:
        options.c_cflag &= ~CSTOPB; // 默认设置停止位为1位
        break;
}

tcsetattr(fd, TCSANOW, &options); // 把新的设置应用到串口上
}

int main(int argc, char *argv[]) 
{
    if (argc != 3) 
    {
        printf("Usage: %s [serial device] [send|recv]\n", argv[0]);
        return -1;
    }

    int fd;
    char buffer[BUFFER_SIZE];

    if (strcmp(argv[2], "send") == 0) // 发送数据
    {
        fd = open(argv[1], O_WRONLY | O_NOCTTY | O_SYNC);
        if (fd < 0) 
        {
            printf("Error opening serial device\n");
            return -1;
        }

        set_serial_port(fd, 115200, 8, 1, 'N'); // 设置串口参数

        while (1) // 不停地读取用户输入，并写入串口
        {
            printf("Enter message: ");
            fgets(buffer, BUFFER_SIZE, stdin);
            write(fd, buffer, strlen(buffer));
        }
    }
    else if (strcmp(argv[2], "recv") == 0) // 接收数据
    {
        fd = open(argv[1], O_RDONLY | O_NOCTTY | O_SYNC);
        if (fd < 0) 
        {
            printf("Error opening serial device\n");
            return -1;
        }

        set_serial_port(fd, 115200, 8, 1, 'N'); // 设置串口参数

        while (1) // 不停地读取串口数据，并输出到屏幕上
        {
            int bytes_read = read(fd, buffer, BUFFER_SIZE);
            if (bytes_read > 0) 
            {
                buffer[bytes_read] = '\0';
                printf("Received message: %s", buffer);
            }
        }
    } 
    else // 参数不合法
    {
        printf("Invalid option\n");
        return -1;
    }

    return 0;
}
~~~

# 工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==若想除掉旷野里的杂草，最好的办法就是种上一片庄稼==

____

昨天搞完了输入设备应用编程的视频，但是还没加字幕，下午四点多的时候可以花费四五十分钟的时间搞一下，然后现在继续熟练一下串口编程。

退出全部的无用QQ群，有些群还是留着吧，目前最想践行的就是这个25分钟法则，其他倒是没啥了，也就技术支持的前



~~~c
#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>
#include <termios.h>
#include <string.h>

void set_serial_port(int fd, int baud_rate, int date_bits, char parity, int stop_bits)
{
    struct termios options;
    tcgetattr(fd, &options);
    
    options.c_lflag &= ~(ICANON | ECHO);  // 关闭规范模式、回显和信号处理
    options.c_cflag |= CLOCAL | CREAD;  // 必须设置的标志，表示启用本地连接和接收数据
    switch (baud_rate)
    {
        case 9600:
            cfsetispeed(&options, B9600);
            cfsetospeed(&options, B9600);
            break;
        case 115200:
            cfsetispeed(&options, B115200);
            cfsetospeed(&options, B115200);
            break;
    }

    switch (date_bits)
    {
        case 7:
            options.c_cflag &= ~CSIZE;
            options.c_cflag |= CS7;
            break;
        case 8:
            options.c_cflag &= ~CSIZE;
            options.c_cflag |= CS8;
            break;
    }   

    switch (parity)
    {
        case 'O':
            options.c_cflag |= PARENB;
            options.c_cflag |= PARODD;
            break;
        case 'E':
            options.c_cflag |= PARENB;
            options.c_cflag &= ~PARODD;
            break;
        case 'N':
            options.c_cflag &= ~PARENB;
            break;
    } 
    switch (stop_bits)
    {
        case 1:
            options.c_cflag &= ~CSTOPB;
            break;
        case 2:
            options.c_cflag |= CSTOPB;
            break;
    }    
    options.c_cflag &= ~CRTSCTS;  // 关闭硬件流控制
    tcflush(fd, TCIFLUSH);
    tcsetattr(fd, TCSANOW, &options); // 把新的设置应用到串口上
}
int main(int argc, char *argv[])
{
    int fd;
    char buffer[128];
    int bytes_read;
    fd = open("/dev/ttyS9", O_RDWR | O_NOCTTY | O_NDELAY);
    set_serial_port(fd, 115200, 8, 'N', 1); 
    write(fd, argv[1], strlen(argv[1]));
    sleep(1);
    bytes_read = read(fd, buffer, sizeof(buffer));
    buffer[bytes_read] = '\0';
    printf("Received message: %s\n", buffer);
    return 0;
}

~~~



如果回显被开启，同时你正在通过某个串口或终端传输数据，那么在每个字符输入时，字符会被显示并同时发送到串口。当用户输入`111`后，终端将`1`重复发送到串口多次，最终结果就是在接收端看到了大量的`1`。



规范模式  Canonical Mode

规范模式  ICANON 

| 参数       | 作用                                                         |
| ---------- | ------------------------------------------------------------ |
| **CLOCAL** | 忽略调制解调器控制线，无需等待外部信号，适用于直接串口连接。 |
| **CREAD**  | 启用串口接收功能，确保能够接收数据，是串口通信的基本要求。   |

# 工作记录(星期三)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==若想除掉旷野里的杂草，最好的办法就是种上一片庄稼==

____

昨天其实效率还行，但是串口这个例程确实有点难办啊，先再默写一下。

~~~python
import cv2
import time
import numpy as np


def main():
    # 使用GStreamer管道从MIPI摄像头捕获视频，添加视频帧率
    cap = cv2.VideoCapture('/dev/video21')  # USB摄像头
    frames, loopTime, initTime = 0, time.time(), time.time()
    fps = 0
    while True:
        frames += 1
        # 从摄像头捕获帧
        ret, frame = cap.read()
        # 如果捕获到帧，则显示它
        if ret:
            if frames % 30 == 0:
                print("30帧平均帧率:\t", 30 / (time.time() - loopTime), "帧")
                fps = 30 / (time.time() - loopTime)
                loopTime = time.time()
            frame = cv2.cvtColor(frame, cv2.COLOR_RGB2BGR)
            cv2.putText(frame, "FPS: {:.2f}".format(fps), (10, 30), cv2.FONT_HERSHEY_SIMPLEX, 1, (0, 0, 255),
                        2)  # 在图像上显示帧率
            cv2.imshow("MIPI Camera", frame)
        # 按下'q'键退出循环
        if cv2.waitKey(1) & 0xFF == ord("q"):
            break

    print("总平均帧率\t", frames / (time.time() - initTime))
    # 释放资源并关闭窗口
    cap.release()
    cv2.destroyAllWindows()


if __name__ == "__main__":
    main()



~~~



~~~shell
sudo apt update
sudo apt install libtiff5=4.3.0-6ubuntu0.10 libopencv-dev
g++ -o camera main.cc `pkg-config --cflags --libs opencv4`
pip install opencv-python -i https://pypi.tuna.tsinghua.edu.cn/simple
~~~

简单的测试完成了，重新回到RK3568的串口这里了，必须得快点了，在这里耽误的时间太多了，这里虽然有部分原因是串口本身的问题，但更多的其实还是我的问题，行了开始了。



# 工作记录(星期四)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==若想除掉旷野里的杂草，最好的办法就是种上一片麦田==

_____

以后就必然遵循指定的规则了，在公司是绝对不会写反思了，要写也是写的工作记录才对，反思会浪费大量的工作时间，并且有着极低的切换效率，得不偿失，写反思最好的时间其实就是早上起床之后，以及晚上回家之后，这些事件就算不用也是会被你所浪费掉，工作的时间是极其宝贵的，并且心流带给人的认同感、归属感、以及收获是巨大的，后面必然以及肯定要重新学习心流的，但这已经是以后的事情了，而对于工作中的一些相关的事情，最重要的一点是少看手机，手机这个东西对于效率的影响是非常非常巨大的，不然我也不会买小盒子，买软件，等等，就这样了，然后开始写今天的工作内容安排。

____

今天无论如何都要继续向下进行了，之前花费了太多的心力在一些不相干的事情上，这次说什么也不会了，先正念两分钟，然后再写一遍demo，demo现在其实差不多了，然后继续写手册了，总是被一些无关紧要的事情打乱双眼.

_____

还是应该思考一下都要讲什么，而且要讲的很有逻辑。先把一定要讲解的内容罗列出来





串口的介绍

串口的标准和接口

串口的数据格式

串口常用宏定义

串口应用编程步骤

串口实验例程



_____

问题还是很大，测试了很多很多的问题,但还是静不下来躁动的心，其实是存在问题的，

# 工作记录(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==若想除掉旷野里的杂草，最好的办法就是种上一片庄稼==

_____

说一下昨天工作上的问题，昨天技术支持，这时候看QQ是正常的，但是这也太有问题了，看QQ这件事情导致了太多的负面情绪，浮躁、焦虑、不能安静下来,其实前面已经做过很多的努力了，就比如屏蔽所有的群组，就比如设置了一个定时的时间，在规定的时间内才能去看qq等等，但仍旧是没有太多的起色，该浮躁的时候还是浮躁，该心神不宁的时候仍旧是心神不宁，那么我应该怎么做呢，为什么会有这样的情绪呢，其实最靠谱的还是通过手机来确定,其实要认清楚的幻觉只有一个，那就是技术支持的群聊并不能真的给你带来什么，给你带来的反而是无穷无尽的拖累，不好的情绪状态，低下的工作效率，内心的焦虑，如果没有了这一百块钱，我想我甚至都不会考虑去技术支持，一切都是这一百块钱带来的，是啊，根源不就是这一百块钱吗，以后每个月的第一天都做个总结，以后在每个月的第一天，都应该将钱整理一下，发到一个固定的小荷包里面，然后将这些钱用来去买课也好，买电脑配件也罢，请苹果或者苏醒吃饭也好，都可以将钱花出去，而不应该再这样，只要钱不是我的，我自认没有什么压力了，对不对，今天是这个月工作的最后一天，先把这个月的钱给搞出来，我算一下，大约是800块，先存着吧，等中午的时候安排到小荷包里面，除了一个PCIE5.0的固态目前让我感兴趣，其他的倒是也没啥了，或许也不一定，我也能买个板子啊，对吧，都是可以的，看我自己愿意将这些钱花到哪个地方了，一切随心即可，但更重要的是不再看QQ，尽可能的少看QQ。

以后呢就不再主动发起聊天了，真的确实是浮躁、浮躁、再浮躁，以后呢就肯定是通过手表来看QQ了，如果手表没人有消息就不再看QQ了，尽可能的将QQ对你的影响降到最低，行了新的一天加油。该继续写手册了。

____

以后只要登录QQ，你就会想，原来我进步如此缓慢的原因在这里啊，正是因为QQ才阻断了我快速进步的路，也或许我最大的问题不是QQ，而是QQ让我无法静心，只要能静心下来，其实有没有QQ又有什么重要的呢？我最终想要的也只是平静的努力向上罢了。在我因为这个目标不断前行的路上，这一切阻拦我进步的人都将会是我的敌人，不论是人还是事务，原来我的认知还是有问题，你只是将QQ,将直播、将小说、将堕落当成了自己的损友，可实际真的是这样吗，其实不是的，这些都是你的敌人，是不是你死就是我亡的敌人，你的认知从来都有问题，你认为这些东西适当的看一下没问题，但他们确实是想要你的命的，你看不出来吗，真的可笑可悲。

其实一个涨都得涨，并不存在哪个涨的多，或者哪个涨的少，都是有规律可循的，我后面的操作肯定就是先投够一个月的美股，将纳斯达克的仓位搞到跟沪深3000一样的水平，然后大A目前就不加了，接下来的呢就是黄金和BNB，其他倒是不咋重要了。就是坚定信念罢了。一切求简，不能再关注这么多的东西了，确实也没有必要，人的精力是有限的，不强求，如此便挺好的，做任何事情，只求心安，从而能认认真真的做其他事情，顺心意即可。

行了，中午的时候可以小小的回趟家，然后晚上直接就回家吧，下午的时候呢就是继续写手册，你应该知道的到底是哪些东西在害你，哪些东西才是真实的，如此便好。