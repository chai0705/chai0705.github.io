---
description: "9.8工作记录星期日 摆脱一份幻觉比发现一个真理更能使人明智 当你在凝视深渊的时候，深渊也在凝视你 我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐 ____ 今天周日，但是上班，我感觉"
cover: /img/cover/11.webp

title: 工作记录(九月第二个星期)
date: 2024-09-08 21:26:56
categories:
  - 工作日志
---

# 9.8工作记录(星期日)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

____

今天周日，但是上班，我感觉还挺好的 ，我发现唯有在上班的时候才能真正的静下心来，当在家里因为某些事情丧失了理智之后，就真的只有公司这一个地方来给我带来宁静了，还是那句话，有些事情不能因为你想啥就是啥，很多事情都需要精心规划的，而不能想到什么就去做什么，昨天睡不着觉的原因果然就是身体因素，体温太低了，跟那种室温高还不一样，我认为是那些行为的问题，中午的这个行为从根本上来说就是错误的，不对吗。

我确实不应该这样做，我应该继续钻研超频，而不是像昨天那样，浪费了三个小时的时间，而且这个睡眠并没啥用，唯有心理作用罢了，晚上又让我睡不着了，我发现我的心脏跳动的频率都有点乱了，不能静下心来，都有对应的原因，但应该从今天开始应该就会好很多，幸好上班了，而且我也花钱了，还不错吧，然后开始今天的任务。

____

周五晚上完善了一下构建，现在就只有ubuntu这一个构建，我认为就不要有其他的构建了，然后继续研究一下，我是搞源码呢，还是搞一下旋转呢，其实都可以的

烧写5.10的ubuntu22镜像，12:26 开始的 14：29完成，大约120秒，我的镜像大小为4.5G。38.4M每秒，还行吧

~~~shell
#!/bin/bash

# 获取显示器名称
display_name=$(xrandr | grep " connected" | awk '{ print $1 }')

# 判断是否存在 "goodix-ts" 触摸屏设备
touch_name=$(xinput list | grep -i "goodix-ts" | grep -oP '(?<=↳ ).*(?=\s+id)')
touch_id=$(xinput list | grep -i "goodix-ts" | grep -oP '(?<=id=)[0-9]+')

# 如果 "goodix-ts" 没有找到，选择 "ft5x06" 触摸屏设备
if [ -z "$touch_id" ]; then
    touch_name=$(xinput list | grep -i "ft5x06" | grep -oP '(?<=↳ ).*(?=\s+id)')
    touch_id=$(xinput list | grep -i "ft5x06" | grep -oP '(?<=id=)[0-9]+')
fi

# 如果没有找到显示器或触摸屏设备，退出
if [ -z "$display_name" ]; then
    echo "没有找到连接的显示器！"
    exit 1
fi

if [ -z "$touch_id" ]; then
    echo "没有找到触摸屏设备！"
    exit 1
fi

# 显示旋转选项给用户选择
echo "选择旋转角度:"
echo "1. 正常显示"
echo "2. 向左旋转90度"
echo "3. 向右旋转90度"
echo "4. 旋转180度"

read -p "请输入选项 (1-4): " choice

case $choice in
    1)
        # 正常显示
        xrandr --output "$display_name" --rotate normal
        xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 1 0 0 0 1 0 0 0 1
        ;;
    2)
        # 向左旋转90度
        xrandr --output "$display_name" --rotate left
        xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 -1 1 1 0 0 0 0 1
        ;;
    3)
        # 向右旋转90度
        xrandr --output "$display_name" --rotate right
        xinput set-prop "$touch_id" "Coordinate Transformation Matrix" 0 1 0 -1 0 1 0 0 1
        ;;
    4)
        # 旋转180度
        xrandr --output "$display_name" --rotate inverted
        xinput set-prop "$touch_id" "Coordinate Transformation Matrix" -1 0 1 0 -1 1 0 0 1
        ;;
    *)
        echo "无效选项！"
        exit 1
        ;;
esac

echo "显示器和触摸屏旋转已同步完成。"

~~~



#### 1. **创建并切换到一个新的本地分支**

首先，创建一个与远程分支 `origin/ubuntu22.04` 相对应的本地分支。

```

git checkout -b ubuntu22.04
```

这将：

- 创建一个名为 `ubuntu22.04` 的本地分支。
- 将 HEAD 从分离状态附加到新的本地分支 `ubuntu22.04`。

#### 2. **设置本地分支与远程分支的追踪关系**

为了确保你可以在本地分支上提交并推送到远程仓库，你需要设置本地分支与远程分支的追踪关系。

```
git branch --set-upstream-to=origin/ubuntu22.04
```



``` sudo apt update
sudo apt update
apt install libegl-dev libgles-dev libgles1 libglvnd-core-dev libglvnd-dev libopengl-dev
```



目前的问题是，不能打开设置，其他的倒是没啥问题，设置里面一般也没啥吧

# 9.9工作记录(星期一)

==摆脱一份幻觉，比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

___

明天我的CPU和水冷就到了，预测是明天上午大约就到了，如果明天中午来得就明天中午，如果来不及就晚上吃完饭晚上装，就也挺好的，昨天晚上睡眠不错，又一次步入了正轨，很不错，然后开始今天的工作了，今天继续维护我的ubuntu，现在有两个方向，1是搞debian 的，2是搞

~~~shell
git add .
git commit -m "默认会有一个根目录deb包生成，增加删除命令"
git push
~~~



# 9.10工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

____

昨天找了一天的问题，首先是找ubuntu22 gnome桌面无法打开的问题，现在看来都是这个问题，应该是驱动啥的引起的，这个没办法，

nfs-kernel-server

~~~shell
if [ ! -d $TARGET_ROOTFS_DIR ]; then
    mkdir -p $TARGET_ROOTFS_DIR

    debootstrap --variant=buildd \
    --components="main,contrib" \
    --arch="arm64" \
    --foreign \
    --include="ccache,locales,git,ca-certificates,devscripts,libfile-fcntllock-perl,debhelper,rsync,python3,apt-utils,perl-openssl-defaults" \
    "buster" \
    "${TARGET_ROOTFS_DIR}" \
    "https://repo.huaweicloud.com/debian/"

    # 复制当前系统的 DNS 解析配置到目标根文件系统中
    cp -b /etc/resolv.conf $TARGET_ROOTFS_DIR/etc/resolv.conf
    
    # 根据系统架构，复制相应的 qemu 可执行文件，以便在 chroot 环境中运行
    cp -b /usr/bin/qemu-aarch64-static $TARGET_ROOTFS_DIR/usr/bin/

    # 运行 debootstrap 的第二阶段
    chroot "${TARGET_ROOTFS_DIR}" /bin/bash -c "/debootstrap/debootstrap --second-stage"
fi
~~~







# 9.11 工作记录

==当你在凝视深渊的时候，深渊也在凝视你==

==摆脱一份幻觉比发现一个真理更能使人明智==

```
sudo apt install linux-image-generic-hwe-22.04 
```



https://learn.microsoft.com/en-us/community/content/wsl-user-msft-kernel-v6



要查找当前文件夹下大小大于 50MB 的文件，可以使用 `find` 命令。以下是一个查找当前目录及其子目录中所有大于 50MB 文件的命令：

```bash
find . -type f -size +50M
```

文件拆分

假设你有一个 .deb 文件，例如 package.deb，并且想要将其拆分成多个大小为 50MB 的部分文件。

~~~shell
split -b 40M package.deb package_part_
~~~

-b 50M：将文件拆分为 50MB 大小的部分（可以根据你的需要调整大小）。

package_part_：拆分后的文件名的前缀。拆分后将会生成类似于 package_part_aa, package_part_ab, package_part_ac 的文件。


~~~shell
split -b 40M chromium-x11_91.0.4472.164_arm64.deb chromium-x11_91.0.4472.164_arm64_part_
~~~





~~~shell
    if [ ! -f "/packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64.deb" ]; then
        cat "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64_part_aa" \
            "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64_part_ab" \
            > "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64.deb" 
    fi
~~~



~~~shell
    if [ ! -f "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64.deb" ]; then
        cat "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64_part_aa" \
            "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64_part_ab" \
            > "packages/arm64/chromium/chromium-x11_91.0.4472.164_arm64.deb" 
    fi
~~~

今天不错啊，虽然昨天晚上并没怎样睡好觉，但是确实是把debian的相关的内容搞完了，还重新维护了一下github，ubuntu和debian也都用了最新的，以后就会用





```
cp /packages/libmali/mali_csffw.bin /usr/lib/firmware/ 
```

```
apt-get install -fy libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev libgstreamer-plugins-bad1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-base-apps gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav gstreamer1.0-tools gstreamer1.0-x gstreamer1.0-alsa gstreamer1.0-gl gstreamer1.0-gtk3 gstreamer1.0-qt5 gstreamer1.0-pulseaudio
```



~~~
sudo XZ_OPT=-T0 tar -cpJvf linaro-buster-xfce-arm64.tar.xz binary/
sudo XZ_OPT=-T0 tar -cpJvf linaro-bullseye-xfce-arm64.tar.xz binary/
sudo XZ_OPT=-T0 tar -cpJvf linaro-bookworm-xfce-arm64.tar.xz binary/
 
~~~

# 9.13工作记录(星期五)

==摆脱一份幻觉，比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐。==

____

这次的还算及时，也算是找到了最后可以搞的地方，不容易，如果这些钱花的我心安我认为这是没问题的，早上也说了如果这钱花出去能让我摆脱小说这一个成瘾物，我当然认为很值得，一切都是值得的，难道不是吗，是的，将一个无用的成瘾物转换成另一个有用的，我当然认为这是没问题的，我的打算是拆掉服务器的一根内存，然后搞一台新的电脑，这样我的小主机就可以退役了，小主机拿回家可以然后分着用呗，看情况就行，其他的无所谓，只求心意顺，行了，这些都是节后的事情了，然后来看今天的任务，今天肯定是要搞完全部的镜像的，其实昨天也差不多搞完了，先来打包吧，备份一下之前的系统镜像。

> 1.更新3588 3588 邮票孔 3588S三个的镜像
>
> ​	这次打包目录用tar的格式，不然太难解压了，发现得不偿失，虽然打包和解压的时间短了，但是呢有太大了，传输也成问题，还是算了，然后是
>
> 2.打包3568 的镜像
>
> ​	 sudo  TARGET=xfce SOC=rk3568 ./mk-ubuntu-rootfs.sh

___

行了，今天更新完成了三个镜像，速度确实快了很多，而且把3588  3588 邮票孔以及3588S打通了，现在他们的区别真的就只剩下设备树不一样了，其他完全相同，3568的更新可能会有点费劲，明天看看吧，怎样更方便点，今天最后一个小时了，看一下法师说的4418的核心板电压问题，昨天给了一个补丁，这个补丁不一样的地方驱动肯定是没啥的，有区别的就在device.c里了，先看看到底要看啥，先来看核心板原理图，看看是怎么供电的，以及他们说的都是什么意思？

它的电源管理是AXP228，

![image-20240913175238294](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202409131752628.png)



![image-20240913180404992](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202409131804060.png)

没啥意思

# 9.14工作记录(星期六)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点你将永远不快乐==

____

先来更新镜像，要更新的镜像有两个一个是4.19的，一个是5.10的，我先把5.10的搞定

~~~shell
find . -type f -name "*.Identifier" -exec rm -f {} \;
~~~



要使用 `tree` 命令打印二级目录（即只显示当前目录和它的直接子目录），可以使用 `-L` 选项来限制目录树的深度。对于二级目录树，命令如下：

```bash
tree -L 3
```



如果你想解压到原始 `4.19` 目录下，命令可以改为：

```bash
sudo find . -path "*/4.19/*.tar.xz" -execdir tar -xvf {} \;
```

`-execdir` 会在找到的 `.tar.xz` 文件所在的目录下解压文件。

为了将 `overlay_4_19` 目录中的内容复制到当前目录下每个 `4.19` 目录中的 `binary` 目录中，并使用 `sudo` 权限执行，你可以使用以下命令：

```bash
sudo find . -path "*/4.19/binary" -exec cp -r overlay_4_19/* {} \;
```



~~~shell
sudo find . -path "*/4.19/binary" -exec rm  {} \;
~~~



将binary打包回去

~~~shell
sudo find . -path "*/4.19/binary" -type d -execdir sh -c 'XZ_OPT="-T0 -1" tar -cpJvf ../new_binary.tar.xz binary && rm -f ../*.tar.xz && mv ../new_binary.tar.xz ../$(basename $(pwd)).tar.xz' \;

~~~



~~~bash
#!/bin/bash

# 查找所有的4.19目录
find . -path "*/4.19/binary" | while read binary_dir; do
    # 获取binary所在的目录路径
    base_dir=$(dirname "$binary_dir")

    # 查找4.19目录下的.tar.xz文件作为参考打包名称
    tar_file=$(find "$base_dir" -maxdepth 1 -name "*.tar.xz" | head -n 1)

    # 提取参考.tar.xz文件的名称部分
    if [ -n "$tar_file" ]; then
        tar_name=$(basename "$tar_file")
        echo "正在将 $binary_dir 打包为 $base_dir/$tar_name"

        # 使用XZ_OPT=-T0进行多线程打包
        XZ_OPT=-T0 tar -cpJvf "$base_dir/$tar_name" -C "$binary_dir" .
        echo "打包完成：$base_dir/$tar_name"
    else
        echo "未找到参考.tar.xz文件，跳过目录 $base_dir"
    fi
done
~~~



```bash
#!/bin/bash

# 查找4.19目录下的所有rootfs.img
sudo find . -path "*/4.19/rootfs.img" | while read img; do
  echo "挂载 $img 到 /mnt"

  # 挂载rootfs.img到/mnt
  sudo mount "$img" /mnt

  if [ $? -eq 0 ]; then
    echo "挂载成功，开始复制overlay_4_19内容到 $img"
    
    # 复制overlay_4_19内容到挂载的目录
    sudo cp -r overlay_4_19/* /mnt/
    
    echo "复制完成，取消挂载 $img"
    
    # 取消挂载
    sudo umount /mnt
    
    echo "$img 已成功取消挂载"
  else
    echo "挂载失败： $img"
  fi
done
```



~~~shell
#!/bin/bash

# 查找所有的binary目录，并将deb包拷贝进去
sudo find . -path "*/4.19/binary" | while read binary_dir; do
  echo "将 camera_engine_rkaiq_rk3568_arm64.deb 拷贝到 $binary_dir"
  
  # 拷贝deb包到每个binary目录下
  sudo cp camera_engine_rkaiq_rk3568_arm64.deb "$binary_dir/"
  
  echo "进入 $binary_dir 使用 chroot 并安装 deb 包"
  
  # 进入chroot环境并安装deb包
  sudo chroot "$binary_dir" dpkg -i /camera_engine_rkaiq_rk3568_arm64.deb
  
  echo "安装完成"
  sudo rm -rf $binary_dir/*deb
done

~~~



~~~shell

#!/bin/bash

# 查找4.19目录下的所有rootfs.img
sudo find . -path "*/4.19/rootfs.img" | while read img; do
  echo "挂载 $img 到 /mnt"

  # 挂载rootfs.img到/mnt
  sudo mount -o loop "$img" /mnt

  if [ $? -eq 0 ]; then
    echo "挂载成功，将 camera_engine_rkaiq_rk3568_arm64.deb 拷贝到 /mnt"

    # 将deb包拷贝到/mnt
    sudo cp camera_engine_rkaiq_rk3568_arm64.deb /mnt/

    echo "进入chroot环境并安装deb包"
    
    # 进入chroot环境并安装deb包
    sudo chroot /mnt dpkg -i /camera_engine_rkaiq_rk3568_arm64.deb

    echo "安装完成，删除 /mnt 中的 deb 包"
    
    # 删除拷贝进去的 deb 包
    sudo rm /mnt/camera_engine_rkaiq_rk3568_arm64.deb
    
    echo "取消挂载 $img"
    
    # 取消挂载
    sudo umount /mnt
    
    echo "$img 已取消挂载"
  else
    echo "挂载失败: $img"
  fi
done

~~~



~~~shell
apt install gstreamer1.0-plugins-bad gstreamer1.0-plugins-base gstreamer1.0-tools gstreamer1.0-alsa  gstreamer1.0-plugins-base-apps qtmultimedia5-examples gstreamer1.0-plugins-good gstreamer1.0-libav           gstreamer1.0-plugins-ugly
~~~





~~~shell
rk3568_linux_5.10_20240822.tar.xz   rk356x_linux_20240911.tar.xz
~~~



~~~shell
cp ~/rk3568/rk3568/debian10/4.19/debian-buster-xfce-arm64.tar.xz linaro-bullseye-arm64.tar.xz
cp ~/rk3568/rk3568/debian11/4.19/linaro-bullseye-xfce-arm64.tar.xz linaro-bullseye-arm64.tar.xz       
cp ~/rk3568/rk3568/ubuntu20/4.19/ubuntu-focal-xfce-arm64.tar.xz ubuntu-focal-arm64.tar.xz
cp ~/rk3568/rk3568/ubuntu22/4.19/ubuntu-jammy-xfce-arm64.tar.xz ubuntu-jammy-arm64.tar.xz
~~~





~~~shell
cp ~/rk3568/rk3568/debian10/5.10/debian-buster-xfce-arm64.tar.xz linaro-bullseye-arm64.tar.xz
cp ~/rk3568/rk3568/debian11/5.10/linaro-bullseye-xfce-arm64.tar.xz linaro-bullseye-arm64.tar.xz       
cp ~/rk3568/rk3568/ubuntu20/5.10/ubuntu-focal-xfce-arm64.tar.xz ubuntu-focal-arm64.tar.xz
cp ~/rk3568/rk3568/ubuntu22/5.10/ubuntu-jammy-xfce-arm64.tar.xz ubuntu-jammy-arm64.tar.xz
~~~



***************************************************
2024年9月14日
	更新ubuntu debian系统源码
	更新rkbin



~~~shell
cp ~/rk3568/rk3568/debian10/5.10/rootfs.img ./02_Debian10镜像/rootfs.img
cp ~/rk3568/rk3568/debian11/5.10/rootfs.img ./03_Debian11镜像/rootfs.img
cp ~/rk3568/rk3568/ubuntu20/5.10/rootfs.img ./04_Ubuntu20镜像/rootfs.img
cp ~/rk3568/rk3568/ubuntu22/5.10/rootfs.img ./05_Ubuntu22镜像/rootfs.img

XZ_OPT="-T0 -1" tar -cpJvf 03_update_rk3568_mini_20240914.tar.xz 03_update_rk3568_mini &
XZ_OPT="-T0 -1" tar -cpJvf 04_update_rk3568_mini_5.10_20240914.tar.xz 04_update_rk3568_mini_5.10

XZ_OPT="-T0 -1" tar -cpJvf rk356x_linux_mini_20240914.tar.xz rk356x_linux
XZ_OPT="-T0 -1" tar -cpJvf rk3568_linux_5.10_mini_20240914.tar.xz rk3568_linux_5.10
~~~