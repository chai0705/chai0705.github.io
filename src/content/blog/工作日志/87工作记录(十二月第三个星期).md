---
description: "工作记录星期一 摆脱一份幻觉比发现一个真理更能使人明智 当你在凝视深渊的时候，深渊也在凝视你 弱小和无知并不是生存的障碍，傲慢才是 清醒的活着，或清醒的死去 我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这"
cover: /img/cover/6.webp

title: 工作记录(十二月第三个星期)
date: 2025-12-15 10:26:56
categories:
  - 工作日志
---

# 工作记录(星期一)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

____

周六的时候，更新了3568的源码，这次Linux系统镜像的切换总算是没有问题了，现在更新一下那个手册,现在手册算是更新完了，然后继续找一下录音的问题，为什么录音会有这么大的噪声呢？而且这个并不是一定的，在安卓上就都正常，但是在Linux上就很不正常。

再重新测试一下，然后使用两个核心板，一个烧写Linux系统，一个烧写安卓系统，进行对比测试看看。



~~~c
cat /sys/kernel/debug/regmap/0-0020-rk817-codec/registers  > /data/ubuntu_reg.txt
~~~

、

**Replicating Android Audio**

I've been meticulously analyzing the user's request. My focus is on replicating a functional Android audio setup on Ubuntu, targeting an RK3568 platform. I'm prioritizing the user's need to resolve audio recording noise and gain issues. The side-by-side comparison with the existing working Android configuration is key.

**Differentiating Register Values**

I'm now carefully examining the register dumps. I've identified key differences between the Android (left) and Ubuntu (right) configurations. I'm noting specific register variations at rows 15, 38, and 42. The goal is to determine the impact of these bit-level differences on audio performance, particularly the noise and gain. My analysis focuses on the meaning of set and unset bits in each relevant register.

**Pinpointing Audio Differences**

I'm zeroing in on rows 46 and ee. Comparing the values, row 46 shows a significant discrepancy, with Ubuntu (assumed 'right') at a higher value, potentially impacting gain and causing noise. Row ee reveals a smaller, possibly LSB difference. My working hypothesis, aligning with prior context, now assumes the 'left' values correspond to the desired Android configuration, while the 'right' values represent the problematic Ubuntu settings. My next step will be to verify this mapping and understand their functionalities.

**Isolating the Gain Issue**

Okay, so I'm now pretty convinced register 0x46 is the culprit, especially with its gain influence. The Android configuration (left: 95) is indeed lower than the Ubuntu (right: A5), which correlates to the higher noise floor. Register 0x38's bit 0 likely enables a filter or specific mic bias in Android, which also might explain the noise. Lastly, the bit 3 setting in Register 0x42 could control input boost or another related setting.

**Executing Register Changes**

I'm now formulating a plan to implement the register modifications. Using `i2cset`, I'll attempt to overwrite the problematic Ubuntu settings with the values from the working Android setup. I'll need to confirm the I2C address, likely `0x20`, and then execute the specific commands to change registers 0x38, 0x42, 0x46 (crucially), 0x15, and 0xee. My goal is to lower the noise floor. Specifically, I'll update register 0x46 from a5 to 95. Additionally, I'll explain the likely purpose of these adjustments for enhanced understanding and quick feedback.

这份对比数据非常有价值！这就是所谓的“证据确凿”。

通过分析你提供的这两列数据（左边应该是 **Android**，右边是 **Ubuntu**），我们发现了 **5 个关键寄存器** 的不同。其中有 1 个寄存器极有可能是造成杂音的元凶。

### 数据分析

假设 **左边列** 是 Android (好)，**右边列** 是 Ubuntu (坏/杂音大)：

1. **`46: 95` (Android) vs `46: a5` (Ubuntu)** <-- **最大嫌疑人！**
   - **差异：** `0xA5` (165) 比 `0x95` (149) 大。
   - **推测：** 这个区域 (`0x40`~`0x50`) 通常是音频 Codec 的 **PGA Gain (增益)** 或 **ADC Volume (录音音量)** 控制区。
   - **结论：** Ubuntu 的增益设置得比 Android 高，这直接导致了底噪被放大。
2. **`42: 0c` (Android) vs `42: 04` (Ubuntu)**
   - **差异：** 二进制 `0000 1100` vs `0000 0100`。
   - **推测：** 这是路径选择 (MUX) 或 麦克风增强开关 (Boost Switch)。Android 开启了某个位（Bit 3），这可能对应着降噪电路或更优的 Mic Bias 设置。
3. **`38: 01` (Android) vs `38: 00` (Ubuntu)**
   - **推测：** `0x00` 通常意味着关闭，`0x01` 意味着开启。这可能是一个 **HPF (高通滤波器)** 或 **ALC (自动电平控制)** 的开关。Android 开启了它，滤掉了低频噪音。

------

### 解决方案：在 Ubuntu 上“抄作业”

你需要使用 `i2cset` 命令，把 Ubuntu 的寄存器改得和 Android 一模一样。

**注意：** 以下命令假设你的 RK809/RK817 Codec 挂载在 **i2c-1** 总线上，且地址为 **0x20**。 *(如果不确定，请先运行 `i2cdetect -y 1` 确认地址)*

请在 Ubuntu 终端逐行执行以下命令（建议一边录音一边执行，你能听到神奇的变化）：

#### 第一步：核心修复 (降低增益)

这是最关键的一步，应该能立竿见影地减小杂音。

Bash

```
# 将寄存器 0x46 从 0xA5 改为 0x95 (降低增益)
i2cset -f -y 0 0x20 0x46 0x95
```

#### 第二步：优化路径 (开启滤波/优化偏置)

如果还有杂音，继续执行这两条：

Bash

```
# 将寄存器 0x42 从 0x04 改为 0x0c
i2cset -f -y 0 0x20 0x42 0x0c

# 将寄存器 0x38 从 0x00 改为 0x01 (可能开启了滤波器)
i2cset -f -y 0 0x20 0x38 0x01
```

#### 第三步：同步微小差异 (可选)

为了以此防万一，把剩下两个不一样的也改了：

Bash

```
# 将寄存器 0x15 从 0xff 改为 0xfe
i2cset -f -y 0 0x20 0x15 0xfe

# 将寄存器 0xee 从 0x95 改为 0x94
i2cset -f -y 0 0x20 0xee 0x94
```

------

### 如何永久生效？





~~~c
# 注意把 -D hw:0,0 改成了 -D plughw:0,0
arecord -D plughw:0,0 -c 1 -r 16000 -f S16_LE -d 5 test_mono_fix.wav
~~~



/etc/pulse/default.pa

~~~c
load-module module-echo-cancel aec_method=webrtc source_name=denoised_mic sink_name=denoised_speaker
set-default-source denoised_mic
~~~



____

关于这次麦克风相关的问题就到这里了，改了一下3568 buildroot的代码，

# 工作记录(星期二)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

====



~~~sh
#!/bin/bash

# 设置全局语言环境为 C，保证脚本执行时的字符和排序规则为标准 C 语言环境，避免编码错误
export LC_ALL=C

# ============================================================================
# 目录路径定义
# ============================================================================
export RK_SCRIPTS_DIR="$(dirname "$(realpath "$BASH_SOURCE")")"  # 当前脚本所在目录
export RK_COMMON_DIR="$(realpath "$RK_SCRIPTS_DIR/..")"        # common 目录
export RK_SDK_DIR="$(realpath "$RK_COMMON_DIR/../../..")"      # SDK 根目录
export RK_DEVICE_DIR="$RK_SDK_DIR/device/rockchip"             # device/rockchip 目录
export RK_CHIPS_DIR="$RK_DEVICE_DIR/.chips"                    # 芯片元数据目录
export RK_CHIP_DIR="$RK_DEVICE_DIR/.chip"                      # 当前选定芯片的软链接目录

# 默认的构建目标
export RK_DEFAULT_TARGET="all"

# 通用资源目录
export RK_DATA_DIR="$RK_COMMON_DIR/data"
export RK_TOOLS_DIR="$RK_COMMON_DIR/tools"
export RK_EXTRA_PARTS_DIR="$RK_COMMON_DIR/extra-parts"
export RK_KBUILD_DIR="$RK_COMMON_DIR/linux-kbuild"
export RK_CONFIG_IN="$RK_COMMON_DIR/configs/Config.in"

# 钩子脚本 (Hooks) 相关路径
export RK_BUILD_HOOK_DIR="$RK_COMMON_DIR/build-hooks"
export RK_BUILD_HELPER="$RK_BUILD_HOOK_DIR/build-helper"
export RK_POST_HOOK_DIR="$RK_COMMON_DIR/post-hooks"
export RK_POST_HELPER="$RK_POST_HOOK_DIR/post-helper"

# 分区助手
export RK_PARTITION_HELPER="$RK_SCRIPTS_DIR/partition-helper"

# 输出目录定义
export RK_OUTDIR="$RK_SDK_DIR/output"
export RK_PARSED_CMDS="$RK_OUTDIR/.parsed_cmds"
export RK_EXTRA_PART_OUTDIR="$RK_OUTDIR/extra-parts"
export RK_SESSION_DIR="$RK_OUTDIR/sessions"
# 使用更精确的时间戳，并在未定义时初始化
export RK_SESSION="${RK_SESSION:-$(date +%F_%H-%M-%S)}"
export RK_LOG_DIR="$RK_SESSION_DIR/$RK_SESSION"
export RK_LOG_BASE_DIR="$RK_OUTDIR/log"
export RK_ROCKDEV_DIR="$RK_SDK_DIR/rockdev"
export RK_FIRMWARE_DIR="$RK_OUTDIR/firmware"
export RK_CONFIG="$RK_OUTDIR/.config"
export RK_DEFCONFIG_LINK="$RK_OUTDIR/defconfig"

# 获取 SDK 目录的所有者信息，用于权限判断
export RK_OWNER="$(stat --format %U "$RK_SDK_DIR")"
export RK_OWNER_UID="$(stat --format %u "$RK_SDK_DIR")"

# 清理 PATH 变量，防止格式错误
export PATH=$(echo "$PATH" | tr -d ' \t\n')
export PATH=$(echo "$PATH" | sed -e 's|:.:||g' -e 's|::||g')

# ============================================================================
# 功能函数
# ============================================================================

# 检查网络连接 (优先检测国内 DNS，提高检测准确率)
check_network() {
    if ping -c 1 -W 2 114.114.114.114 &> /dev/null; then
        return 0
    elif ping -c 1 -W 2 8.8.8.8 &> /dev/null; then
        return 0
    elif ping -c 1 -W 2 www.baidu.com &> /dev/null; then
        return 0
    fi
    return 1
}

# 安装编译所需的依赖包
install_package() {
    # 获取系统版本代号
    if [ -f /etc/os-release ]; then
        HOSTRELEASE=$(grep VERSION_CODENAME /etc/os-release | cut -d"=" -f2)
    else
        HOSTRELEASE="unknown"
    fi
    
    # 仅在需要时显示检查信息，或者完全静默，这里保留基础信息
    # echo -e "\e[33m当前系统: $HOSTRELEASE\e[0m"
    
    if check_network; then
        # 基础通用依赖包
        local COMMON_PACKAGES=(
            whiptail dialog psmisc acl uuid uuid-runtime curl gpg gnupg gawk git
            aptly aria2 bc binfmt-support bison btrfs-progs build-essential
            ca-certificates ccache cpio cryptsetup debian-archive-keyring
            debian-keyring debootstrap device-tree-compiler dirmngr dosfstools
            dwarves f2fs-tools fakeroot flex gdisk imagemagick
            jq kmod libbison-dev libelf-dev libfdt-dev
            libfile-fcntllock-perl libfl-dev liblz4-tool libncurses-dev libssl-dev
            libusb-1.0-0-dev linux-base locales lzop ncurses-base ncurses-term
            nfs-kernel-server ntpdate p7zip-full parted patchutils pigz pixz pkg-config
            pv python3-dev qemu-user-static rsync swig systemd-container u-boot-tools
            udev unzip uuid-dev wget zip zlib1g-dev distcc
            python3 expect expect-dev cmake vim openssh-server net-tools texinfo htop
            libgmp-dev libmpc-dev
        )

        # 针对 64位系统的兼容包
        if [ "$(uname -m)" = "x86_64" ]; then
             COMMON_PACKAGES+=(lib32ncurses-dev lib32stdc++6 libc6-i386)
        fi

        # 针对不同 Ubuntu 版本的差异化处理
        local EXTRA_PACKAGES=()
        case "$HOSTRELEASE" in
            "bionic") # 18.04
                EXTRA_PACKAGES=(
                    liblz-dev liblzo2-2 liblzo2-dev mtd-utils squashfs-tools schedtool
                    g++-multilib lib32z1-dev lib32ncurses5-dev lib32readline-dev gcc-multilib
                    patchelf chrpath diffstat python3-pip subversion sed binutils
                    bzip2 patch gzip perl tar file tcl android-tools-fsutils openjdk-8-jdk
                    libsdl1.2-dev libesd-java libwxgtk3.0-dev repo bzr cvs mercurial pngcrush xsltproc
                    gperf libc6-dev
                )
                ;;
            "focal"|"jammy") # 20.04 / 22.04
                EXTRA_PACKAGES=(
                    python2 python3-distutils libpython2.7-dev
                )
                ;;
            "noble") # 24.04
                EXTRA_PACKAGES=(
                    python-is-python3 python3-setuptools
                )
                ;;
        esac

        local ALL_PACKAGES=("${COMMON_PACKAGES[@]}" "${EXTRA_PACKAGES[@]}")
        local MISSING_PACKAGES=()

        # 1. 静默检查缺失包
        for pkg in "${ALL_PACKAGES[@]}"; do
            if ! dpkg -s "$pkg" >/dev/null 2>&1; then
                MISSING_PACKAGES+=("$pkg")
            fi
        done

        # 2. 如果存在缺失包，则进行安装流程
        if [ ${#MISSING_PACKAGES[@]} -gt 0 ]; then
            echo -e "\e[34m检测到缺失依赖，正在更新软件源...\e[0m"
            sudo apt-get update -qq

            for pkg in "${MISSING_PACKAGES[@]}"; do
                echo -ne "INSTALL \e[36m$pkg\e[0m ... "
                # 尝试静默安装
                if sudo apt-get install -y --no-install-recommends "$pkg" > /dev/null 2>&1; then
                    echo -e "\e[32mOK\e[0m"
                else
                    echo -e "\e[31mFAIL\e[0m"
                    # 如果静默安装失败，尝试非静默安装以显示错误信息给用户，或者直接让用户看到错误
                    echo -e "\e[31mRetrying $pkg with verbose output:\e[0m"
                    sudo apt-get install -y --no-install-recommends "$pkg"
                fi
            done
            echo -e "\e[32m依赖安装流程结束。\e[0m"
        else
            # 所有包都已存在，完全静默，不显示任何内容，符合“已经有了就不需要显示了”的要求
            : 
        fi
        
    else
        echo -e "\e[31m错误：无法连接网络，跳过依赖检查。\e[0m"
    fi
}


# 设置 Python 版本软链接
set_python() {
    # 仅在需要修改时才显示信息，或者保持静默检查
    
    # 如果已经存在 python 命令且指向正确，则跳过
    if command -v python &>/dev/null; then
        # 简单验证版本，如果不符合预期再介入
        :
    fi

    if [ -f /etc/os-release ]; then
        HOSTRELEASE=$(grep VERSION_CODENAME /etc/os-release | cut -d"=" -f2)
    fi

    # 针对旧版系统强制链接 python2，新版链接 python3
    if [ "$HOSTRELEASE" == "bionic" ] || [ "$HOSTRELEASE" == "focal" ] || [ "$HOSTRELEASE" == "jammy" ]; then
        # 只有在 python 不存在或者指向不符合预期时才修改
        if [ ! -f /usr/bin/python ] || [ "$(readlink -f /usr/bin/python)" != "/usr/bin/python2.7" ]; then
            echo -e "\e[33m正在设置 Python -> Python 2.7\e[0m"
            sudo ln -sf /usr/bin/python2.7 /usr/bin/python
            sudo ln -sf /usr/bin/python2.7 /usr/bin/python2
        fi
    elif [ "$HOSTRELEASE" == "noble" ]; then
        if [ ! -f /usr/bin/python ]; then
             echo -e "\e[33m正在设置 Python -> Python 3\e[0m"
             sudo ln -sf /usr/bin/python3 /usr/bin/python
        fi
        if [ ! -f /usr/bin/python2 ]; then
             sudo ln -sf /usr/bin/python3 /usr/bin/python2
        fi
    fi
}

# 错误处理函数
err_handler() {
    ret=${1:-$?}
    [ "$ret" -eq 0 ] && return

    echo -e "\e[31mERROR: Running $BASH_SOURCE - ${2:-${FUNCNAME[1]}} failed!\e[0m"
    echo -e "\e[31mERROR: exit code $ret from line ${BASH_LINENO[0]}:\e[0m"
    echo "    ${3:-$BASH_COMMAND}"
    echo "ERROR: call stack:"
    for i in $(seq 1 $((${#FUNCNAME[@]} - 1))); do
        SOURCE="${BASH_SOURCE[$i]}"
        LINE=${BASH_LINENO[$(( $i - 1 ))]}
        echo "    $(basename "$SOURCE"): ${FUNCNAME[$i]}($LINE)"
    done
    exit $ret
}

# 显示用法
usage() {
    echo "用法: $(basename $BASH_SOURCE) [OPTIONS]"
    echo "可用选项："
    run_build_hooks usage
    echo -e "cleanall                      \t清理所有内容"
    echo -e "help                          \t显示用法说明"
    rm -f "$INITIAL_ENV"
    exit 0
}

# 强制导出
set -a

# 日志颜色输出函数
rk_log() {
    LOG_COLOR="$1"
    shift
    if [ "$1" = "-n" ]; then
        shift
        LOG_FLAG="-ne"
    else
        LOG_FLAG="-e"
    fi
    echo $LOG_FLAG "\e[${LOG_COLOR}m$@\e[0m"
}

message() { rk_log 36 "$@"; }
notice()  { rk_log 35 "$@"; }
warning() { rk_log 34 "$@"; }
error()   { rk_log 91 "$@"; }
fatal()   { rk_log 31 "$@"; }

# 构建结束处理
finish_build() {
    notice "正在运行 $(basename "${BASH_SOURCE[1]}") - ${@:-${FUNCNAME[1]}} 成功完成."
    cd "$RK_SDK_DIR"
}

# 加载配置
load_config() {
    [ -r "$RK_CONFIG" ] || return 0
    for var in $@; do
        export "$(grep "^$var=" "$RK_CONFIG" | tr -d '"' || true)" &>/dev/null || true
    done
}

# 检查配置
check_config() {
    unset missing
    for var in $@; do
        eval [ -z \"\$$var\" ] || continue
        missing="$missing $var"
    done
    [ "$missing" ] || return 0
    notice "跳过 $(basename "${BASH_SOURCE[1]}") - ${FUNCNAME[1]}，缺少配置项: $missing."
    return 1
}

# 获取内核 Raw 版本
kernel_version_raw() {
    [ -d kernel ] || return 0
    VERSION_KEYS="VERSION PATCHLEVEL"
    VERSION=""
    for k in $VERSION_KEYS; do
        v=$(grep "^$k = " kernel/Makefile | cut -d' ' -f3)
        VERSION=${VERSION:+${VERSION}.}$v
    done
    echo $VERSION
}

# 获取内核版本 (处理目录名)
kernel_version() {
    [ -d kernel ] || return 0
    KERNEL_DIR="$(basename "$(realpath kernel)")"
    case "$KERNEL_DIR" in
        kernel-*)
            echo ${KERNEL_DIR#kernel-}
            return 0
            ;;
    esac
    kernel_version_raw
}

# 启动日志
start_log() {
    LOG_FILE="$RK_LOG_DIR/${2:-$1_$(date +%F_%H-%M-%S)}.log"
    ln -rsf "$LOG_FILE" "$RK_LOG_DIR/$1.log"
    echo "# $(date +"%F %T")" >> "$LOG_FILE"
    echo "$LOG_FILE"
}

# 获取工具链
get_toolchain() {
    MODULE="$1"
    TC_ARCH="${2/arm64/aarch64}"
    TC_VENDOR="${3-none}"
    TC_OS="${4:-linux}"

    MACHINE=$(uname -m)
    if [ "$MACHINE" != x86_64 ]; then
        notice "使用非 x86 工具链 $MODULE!" >&2
        if [ "$TC_ARCH" = aarch64 -a "$MACHINE" != aarch64 ]; then
            echo aarch64-linux-gnu-
        elif [ "$TC_ARCH" = arm -a "$MACHINE" != armv7l ]; then
            echo arm-linux-gnueabihf-
        fi
        return 0
    fi

    if [ "$RK_CHIP_FAMILY" = "rv1126_rv1109" ]; then
        TC_VENDOR=rockchip
    fi

    TC_DIR="$RK_SDK_DIR/prebuilts/gcc/linux-x86/$TC_ARCH"
    if [ "$TC_VENDOR" ]; then
        TC_PATTERN="$TC_ARCH-$TC_VENDOR-$TC_OS-[^-]*-gcc"
    else
        TC_PATTERN="$TC_ARCH-$TC_OS-[^-]*-gcc"
    fi
    
    # 增加 find 的健壮性
    if [ -d "$TC_DIR" ]; then
        GCC="$(find "$TC_DIR" -name "*gcc" | grep -m 1 "/$TC_PATTERN$" || true)"
    fi

    if [ ! -x "$GCC" ]; then
        {
            error "未找到 $MODULE 的预构建 GCC 工具链!"
            error "搜索目录: $TC_DIR"
            error "架构: $TC_ARCH"
            error "供应商: $TC_VENDOR"
            error "操作系统: $TC_OS"
        } >&2
        exit 1
    fi
    echo ${GCC%gcc}
}

# 快捷跳转命令
rroot() { cd "$RK_SDK_DIR"; }
rout() { cd "$RK_OUTDIR"; }
rcommon() { cd "$RK_COMMON_DIR"; }
rscript() { cd "$RK_SCRIPTS_DIR"; }
rchip() { cd "$(realpath "$RK_CHIP_DIR")"; }

set +a

# 检查选项是否合法
option_check() {
    CMDS="$1"
    shift
    for opt in $@; do
        for cmd in $CMDS; do
            if [ "$cmd" = "${opt%%:*}" ]; then
                return 0
            fi
        done
    done
    return 1
}

# 检查 Hook 是否匹配
hook_check() {
    case "$2" in
        init | pre-build | build | post-build) ;;
        *) return 0 ;;
    esac

    [ -f "$RK_PARSED_CMDS" ] || return 0
    
    CMDS="$(sed -n "s@^RK_${2//-/_}_CMDS[^ ]*\(.*\)\" # $(realpath "$1")\$@\1@ip" "$RK_PARSED_CMDS")"

    if echo "$CMDS" | grep -wq default; then
        return 0
    fi
    option_check "$CMDS" "$3"
}

# 运行 Hook
run_hooks() {
    DIR="$1"
    shift
    # 确保 chip dir 存在再查找，避免 find 报错
    local CHIP_HOOK_DIR="$RK_CHIP_DIR/$(basename "$DIR")/"
    local CHECK_DIRS=("$DIR")
    
    if [ -d "$CHIP_HOOK_DIR" ]; then
        CHECK_DIRS=("$CHIP_HOOK_DIR" "$DIR")
    fi

    for dir in "${CHECK_DIRS[@]}"; do
        [ -d "$dir" ] || continue

        for hook in $(find "$dir" -maxdepth 1 -name "*.sh" | sort); do
            hook_check "$hook" "$1" "$2" || continue
            if ! "$hook" $@; then
                HOOK_RET=$?
                err_handler $HOOK_RET "${FUNCNAME[0]} $*" "$hook $*"
                exit $HOOK_RET
            fi
        done
    done
}

# 运行构建 Hooks
run_build_hooks() {
    case "$1" in
        init | pre-build | make-* | usage | parse-cmds)
            run_hooks "$RK_BUILD_HOOK_DIR" $@ || true
            return 0
            ;;
    esac

    LOG_FILE="$(start_log "$1")"
    echo -e "# 运行钩子: $@\n" >> "$LOG_FILE"
    
    # 记录 pipe 的错误码
    run_hooks "$RK_BUILD_HOOK_DIR" $@ 2>&1 | tee -a "$LOG_FILE"
    HOOK_RET=${PIPESTATUS[0]}
    
    if [ $HOOK_RET -ne 0 ]; then
        err_handler $HOOK_RET "${FUNCNAME[0]} $*" "$@"
        exit $HOOK_RET
    fi
}

# 运行 Post Hooks
run_post_hooks() {
    LOG_FILE="$(start_log post-rootfs)"
    echo -e "# 运行钩子: $@\n" >> "$LOG_FILE"
    
    run_hooks "$RK_POST_HOOK_DIR" $@ 2>&1 | tee -a "$LOG_FILE"
    HOOK_RET=${PIPESTATUS[0]}
    
    if [ $HOOK_RET -ne 0 ]; then
        err_handler $HOOK_RET "${FUNCNAME[0]} $*" "$@"
        exit $HOOK_RET
    fi
}

# 检查 SDK 完整性
check_sdk() {
    if ! echo "$RK_SCRIPTS_DIR" | grep -q "device/rockchip/common/scripts$"; then
        fatal "SDK 目录结构异常!"
        error "正在运行 $BASH_SOURCE 来自 $RK_SCRIPTS_DIR"
        exit 1
    fi

    [ -x "$RK_SCRIPTS_DIR/check-sdk.sh" ] && "$RK_SCRIPTS_DIR/check-sdk.sh"

    unset RK_SUDO_ROOT
    if [ "$RK_OWNER_UID" -ne 0 ] && [ "${USER:-$(id -un)}" = "root" ]; then
        export RK_SUDO_ROOT=1
        notice "警告：正在使用 sudo(root) 权限运行构建!"
    fi
}

# 处理 Makefile 选项
makefile_options() {
    unset DEBUG
    check_sdk >&2 || exit 1

    local MAKE_USAGE="$RK_OUTDIR/.make_usage"
    local MAKE_TARGETS="$RK_OUTDIR/.make_targets"

    if [ ! -d "$RK_OUTDIR" ]; then
        MAKE_USAGE=$(mktemp -u .rksdk.XXX)
        MAKE_TARGETS=$(mktemp -u .rksdk.XXX)
    fi

    if [ ! -r "$MAKE_USAGE" ] || [ "$(find "$RK_SCRIPTS_DIR" -cnewer "$MAKE_USAGE")" ]; then
        run_build_hooks make-usage > "$MAKE_USAGE"
    fi

    if [ ! -r "$MAKE_TARGETS" ] || [ "$(find "$RK_SCRIPTS_DIR" -cnewer "$MAKE_TARGETS")" ]; then
        run_build_hooks make-targets > "$MAKE_TARGETS"
    fi

    case "$1" in
        make-targets)
            [ -d "$RK_CHIPS_DIR" ] && ls "$RK_CHIPS_DIR"
            cat "$MAKE_TARGETS"
            ;;
        make-usage)
            cat "$MAKE_USAGE"
            ;;
    esac

    rm -rf /tmp/.rksdk*
    exit 0
}

# 初始化会话
init_session_env() {
    unset INITIAL_SESSION
    INITIAL_ENV=$(mktemp -u)
    env > "$INITIAL_ENV"
    [ "$RK_SESSION" ] || INITIAL_SESSION=1
    
    notice -n "Log colors: "
    message -n "message "
    notice -n "notice "
    warning -n "warning "
    error -n "error "
    fatal "fatal"
    echo
}

# 检查并设置会话
check_and_set_session() {
    if [ -z "$INITIAL_SESSION" ] && [ ! -d "$RK_LOG_DIR" ]; then
        warning "会话($RK_SESSION)无效!"
        export RK_SESSION="$(date +%F_%H-%M-%S)"
        export RK_LOG_DIR="$RK_SESSION_DIR/$RK_SESSION"
        INITIAL_SESSION=1
    fi
    export RK_INITIAL_ENV="$RK_LOG_DIR/initial.env"
    export RK_CUSTOM_ENV="$RK_LOG_DIR/custom.env"
    export RK_FINAL_ENV="$RK_LOG_DIR/final.env"
}

# 解析支持的命令
setup_supported_commands() {
    if [ ! -r "$RK_PARSED_CMDS" ] || [ "$(find "$RK_SCRIPTS_DIR" -cnewer "$RK_PARSED_CMDS")" ]; then
        message "解析支持的命令...\n"
        rm -rf "$RK_PARSED_CMDS"
        run_build_hooks parse-cmds
    fi
    source "$RK_PARSED_CMDS"
}

# 准备环境
prepare_environment() {
    if [ ! -d "$RK_LOG_DIR" ]; then
        rm -rf "$RK_LOG_BASE_DIR" "$RK_LOG_DIR" "$RK_SESSION_DIR/latest"
        mkdir -p "$RK_LOG_DIR"
        ln -rsf "$RK_SESSION_DIR" "$RK_LOG_BASE_DIR"
        ln -rsf "$RK_LOG_DIR" "$RK_SESSION_DIR/latest"
        message "日志保存在 $RK_LOG_DIR"
    fi

    # 仅保留最近10个日志
    if [ -d "$RK_LOG_BASE_DIR" ]; then
        cd "$RK_LOG_BASE_DIR"
        ls -t | sed '1,10d' | xargs -r rm -rf
        cd "$RK_SDK_DIR"
    fi

    if [ "$INITIAL_SESSION" ]; then
        rm -f "$RK_INITIAL_ENV"
        cp "$INITIAL_ENV" "$RK_INITIAL_ENV"
        ln -rsf "$RK_INITIAL_ENV" "$RK_OUTDIR/"
    fi
    rm -f "$INITIAL_ENV"

    run_build_hooks init "$OPTIONS"
    rm -f "$RK_OUTDIR/.tmpconfig*"

    CMDS="$RK_PRE_BUILD_CMDS $RK_BUILD_CMDS $RK_POST_BUILD_CMDS cleanall clean post-rootfs"
    option_check "$CMDS" "$OPTIONS" || return 0
}

# 加载配置环境和自定义环境
load_config_and_custom_env() {
    set -a  # 导出配置环境变量
    source "$RK_CONFIG"
    cp "$RK_CONFIG" "$RK_LOG_DIR"
    export RK_KERNEL_VERSION="$(kernel_version)"

    if [ -z "$INITIAL_SESSION" ]; then
        # 继承会话环境
        sed -n 's/^\(RK_.*=\)\(.*\)/\1"\2"/p' "$RK_FINAL_ENV" > "$INITIAL_ENV"
        source "$INITIAL_ENV"
        rm -f "$INITIAL_ENV"
    else
        # 保存和应用自定义环境
        rm -f "$RK_CUSTOM_ENV"
        for cfg in $(grep "^RK_" "$RK_INITIAL_ENV" || true); do
            env | grep -q "^${cfg//\"/}$" || echo "$cfg" >> "$RK_CUSTOM_ENV"
        done
    fi
    set +a  # 停止导出
}

# 执行构建阶段
execute_build_stages() {
    run_build_hooks pre-build "$OPTIONS"
    run_build_hooks build "$OPTIONS"
    run_build_hooks post-build "$OPTIONS"
}

# 处理特殊命令
handle_special_commands() {
    case "$OPTIONS" in
        cleanall)
            run_build_hooks clean
            rm -rf "$RK_OUTDIR" "$RK_SDK_DIR/rockdev"
            finish_build cleanall
            exit 0
            ;;
        clean:*)
            MODULES="$(echo ${OPTIONS#clean:} | tr ':' ' ')"
            for m in $MODULES; do
                "$RK_SCRIPTS_DIR/mk-$m.sh" clean
            done
            finish_build clean - $MODULES
            exit 0
            ;;
        post-rootfs)
            shift
            TARGET_DIR="$1"
            source "$RK_POST_HELPER"
            POST_DIR="$RK_OUTDIR/$POST_OS"
            mkdir -p "$POST_DIR"

            touch "$POST_DIR/.stamp_post_start"
            run_post_hooks "$TARGET_DIR"
            touch "$POST_DIR/.stamp_post_finish"

            ln -rsf "$TARGET_DIR" "$POST_DIR/target"
            finish_build post-rootfs

            notice "post-rootfs 阶段更改的文件:"
            cd "$TARGET_DIR"
            find . \( -type f -o -type l \) -cnewer "$POST_DIR/.stamp_post_start" | tee "$POST_DIR/.files_post.txt"
            exit 0
            ;;
    esac
}

# 保存最终环境和记录配置
save_and_log_final_environment() {
    # 保存最终环境变量
    rm -f "$RK_FINAL_ENV"
    env > "$RK_FINAL_ENV"
    ln -rsf "$RK_FINAL_ENV" "$RK_OUTDIR/"

    # 日志记录最终配置
    message "\n=========================================="
    message "          最终配置"
    message "=========================================="
    env | grep -E "^RK_.*=.+" | grep -vE "PARTITION_[0-9]" | \
        grep -vE "=\"\"$|_DEFAULT=y|^RK_DEFAULT_TARGET|CMDS=" | \
        grep -vE "^RK_CONFIG|_BASE_CFG=|_LINK=|DIR=|_ENV=|_NAME=|_DTB=" | \
        grep -vE "_HELPER=|_SUPPORTS=|_ARM64=|_ARM=|_HOST=" | \
        grep -vE "^RK_ROOTFS_SYSTEM_|^RK_YOCTO_DISPLAY_PLATFORM_" | sort
    echo
}
~~~



我现在想写一个统一的编译脚本，可以用在编译RK3568 RK3562 RK3588 RK3576上，这是我之前写的单独用在RK3576的build.sh的脚本文件

他的路径为根目录的device/rockchip/common/build.sh，在common目录下还有着很多的目录

Makefile README.md build-hooks build.sh configs data extra-parts fonts kconfig linux-kbuild overlays post-build.sh post-hooks scripts tools 就比如这些，和build,sh配合的是scripts目录的general.sh脚本，他记录了一些build.sh要用到的一些函数，具体内容如下所示：


帮我写一个编译RK3568 RK3588 RK3562 RK3576的通用脚本，他们的区别就是编译的设备树不同，取决于device/rockchip/rk3576目录下的配置文件，例如RK3576就是topeet_rk3576_defconfig



~~~shell
#!/bin/bash

# 在发生错误时调用名为 err_handler 的函数来处理错误，并启用错误处理机制
trap 'err_handler' ERR
set -eE

# 导入一些要用的函数
source $(dirname "$(realpath "$BASH_SOURCE")")/general.sh

# =============================================================================
#  用户配置区：请在此处定义不同芯片对应的 defconfig 文件名
#  这些文件通常位于 device/rockchip/xk35xx/ 下或 device/rockchip/.chips/ 中
# =============================================================================
CFG_RK3568="topeet_rk3568_defconfig"  # 请确认实际文件名
CFG_RK3562="topeet_rk3562_defconfig"  # 请确认实际文件名
CFG_RK3588="topeet_rk3588_defconfig"  # 请确认实际文件名
CFG_RK3576="topeet_rk3576_defconfig"  # 你原本使用的配置

# 记录当前选择的板型配置的文件，用于记住上次的选择
SELECTED_BOARD_FILE="$RK_SDK_DIR/.selected_board_config"

# =============================================================================

# 检查是否第一次运行,如果是第一次运行将安装一些需要的依赖、设置python脚本和交换分区
if [ ! -f "$RK_SDK_DIR/script_run_flag" ]; then
    # 如果是第一次运行,则获取 sudo 权限
    echo -e "\e[31m这是第一次运行脚本，请输入您的用户密码.\e[0m"
    install_package
    # 创建一个标志文件,表示脚本已经运行过一次
    touch "$RK_SDK_DIR/script_run_flag"
    set_python
fi

# 预处理 Makefile 的选项
case "$@" in
    make-*) makefile_options $@ ;;
esac

# =============================================================================
#  核心修改：板型选择逻辑
# =============================================================================
TARGET_DEFCONFIG=""

# 获取终端大小用于 whiptail
TTY_X=$(($(stty size | awk '{print $2}')-6))
TTY_Y=$(($(stty size | awk '{print $1}')-6))

# 如果没有输入参数（交互模式），则先弹出板型选择菜单

if [ -d "$RK_DEVICE_DIR/rk3576" ]; then
	BOARD_OPTION="RK3576"
# 2. 检测 RK3568 (通常目录名为 rk3566_rk3568)
elif [ -d "$RK_DEVICE_DIR/rk3566_rk3568" ]; then
	BOARD_OPTION="RK3568"
# 3. 检测 RK3588
elif [ -d "$RK_DEVICE_DIR/rk3588" ]; then
	BOARD_OPTION="RK3588"
# 4. 检测 RK3562
elif [ -d "$RK_DEVICE_DIR/rk3562" ]; then
	BOARD_OPTION="RK3562"
else
	# 如果找不到已知的芯片目录，报错退出
	echo -e "\e[31mError: 无法自动识别板型！\e[0m"
	echo -e "在 $RK_DEVICE_DIR 下未找到 rk3576, rk3566_rk3568, rk3588 或 rk3562 目录。"
	exit 1
fi

# 根据选择映射到 defconfig
case "$BOARD_OPTION" in
	"RK3568") TARGET_DEFCONFIG="$CFG_RK3568" ;;
	"RK3562") TARGET_DEFCONFIG="$CFG_RK3562" ;;
	"RK3588") TARGET_DEFCONFIG="$CFG_RK3588" ;;
	"RK3576") TARGET_DEFCONFIG="$CFG_RK3576" ;;
	*) echo "Unknown selection"; exit 1 ;;
esac

# 保存选择到文件，以便下次直接使用命令行编译时能记住
echo "$TARGET_DEFCONFIG" > "$SELECTED_BOARD_FILE"
# =============================================================================

# 初始化会话环境，显示日志颜色信息，保存当前环境变量到临时文件中
init_session_env

# 检查 SDK 的要求，确保环境符合运行条件
check_sdk

# 检查会话有效性并设置日志目录，如果无效则重新设置
check_and_set_session

# 准备所需的文件和目录，如固件目录
mkdir -p "$RK_FIRMWARE_DIR"

# 解析支持的命令
setup_supported_commands

# -----------------------------------------------------------
# 运行 init 钩子函数 (使用动态获取的 TARGET_DEFCONFIG)
# -----------------------------------------------------------
if [ -n "$TARGET_DEFCONFIG" ]; then
    message "Initializing SDK for config: $TARGET_DEFCONFIG"
    run_build_hooks init "$TARGET_DEFCONFIG"
else
    # 针对 cleanall 等不需要 defconfig 的情况
    run_build_hooks init
fi
# -----------------------------------------------------------

# 准备日志目录
prepare_environment

# 强制导出配置环境
set -a

# 加载配置环境
source "$RK_CONFIG"
cp "$RK_CONFIG" "$RK_LOG_DIR"
export RK_KERNEL_VERSION="$(kernel_version)"

if [ -z "$INITIAL_SESSION" ]; then
    # 继承会话环境
    sed -n 's/^\(RK_.*=\)\(.*\)/\1"\2"/p' "$RK_FINAL_ENV" > "$INITIAL_ENV"
    source "$INITIAL_ENV"
    rm -f "$INITIAL_ENV"
else
    # 检测和保存自定义环境
    rm -f "$RK_CUSTOM_ENV"
    for cfg in $(grep "^RK_" "$RK_INITIAL_ENV" || true); do
        env | grep -q "^${cfg//\"/}$" || echo "$cfg" >> "$RK_CUSTOM_ENV"
    done
fi

# 解析分区表
source "$RK_PARTITION_HELPER"
rk_partition_init

set +a

# 如果输入的参数为空，则进入图形界面，否则为命令行编译
if [ -z "$1" ]; then
        titlestr="Choose an option ($BOARD_OPTION)" # 在标题显示当前板型
        backtitle="iTOP-RK Building Script, Current Board: $BOARD_OPTION"
        menustr="Compile image | uboot | kernel | rootfs | recovery | all | firmware |updateimg | cleanall"
        
        # (后续的菜单逻辑保持不变)
        
        # 第1页选项数组，包含选项和对应的函数名
        choose_page1+=("uboot" "uboot")
        choose_page1+=("kernel" "kernel")
        choose_page1+=("rootfs" "rootfs")
        choose_page1+=("recovery" "recovery")
        choose_page1+=("all_img" "all")
        choose_page1+=("firmware" "firmware")
        choose_page1+=("updateimg" "updateimg")
        choose_page1+=("cleanall" "cleanall")

        # 第2页选项数组，包含选项和对应的函数名
        choose_page2+=("buildroot" "buildroot")
        choose_page2+=("debian12" "debian12")
        choose_page2+=("ubuntu22" "ubuntu22")

        # 第3页选项数组，包含选项和对应的函数名
        choose_page3+=("buildroot_update" "buildroot")
        choose_page3+=("debian12_update" "debian12")
        choose_page3+=("ubuntu22_update" "ubuntu22")

        OPTIONS=$(whiptail --title "${titlestr}" --backtitle "${backtitle}" --notags \
                                                        --menu "${menustr}" "${TTY_Y}" "${TTY_X}" $((TTY_Y - 8))  \
                                                        --cancel-button Exit --ok-button Select "${choose_page1[@]}" \
                                                        3>&1 1>&2 2>&3)
else
        OPTIONS="$@"
fi

# 根据用户选择的选项，判断是否需要显示第2页菜单
if [[ $OPTIONS == "rootfs" ]]; then
    titlestr="Choose an option"
    backtitle="iTOP-RK Building Script"
    menustr="Compile single rootfs img | buildroot | debian12 | ubuntu22"
    # 使用whiptail创建第二页菜单，保存用户选择的选项到变量OPTIONS中
    OPTIONS=$(whiptail --title "${titlestr}" --backtitle "${backtitle}" --notags \
                        --menu "${menustr}" "${TTY_Y}" "${TTY_X}" $((TTY_Y - 8))  \
                        --cancel-button Exit --ok-button Select "${choose_page2[@]}" \
                        3>&1 1>&2 2>&3)
fi

# 根据用户选择的选项，判断是否需要显示第3页菜单
if [[ $OPTIONS == "all_img" ]]; then

    titlestr="Choose an option"
    backtitle="iTOP-RK Building Script"
    menustr="Compile update.img image | buildroot | debian12 | ubuntu22 "
    # 使用whiptail创建第3页菜单，保存用户选择的选项到变量OPTIONS中
    OPTIONS=$(whiptail --title "${titlestr}" --backtitle "${backtitle}" --notags \
                        --menu "${menustr}" "${TTY_Y}" "${TTY_X}" $((TTY_Y - 8))  \
                        --cancel-button Exit --ok-button Select "${choose_page3[@]}" \
                        3>&1 1>&2 2>&3)
fi

# 验证和处理选项
CMDS="$RK_INIT_CMDS $RK_PRE_BUILD_CMDS $RK_BUILD_CMDS $RK_POST_BUILD_CMDS"
for opt in $OPTIONS; do
    case "$opt" in
        help | h | -h | --help | usage | \?)
            usage
            ;;
        clean:*)
            # 检查 clean 的模块
            for m in $(echo ${opt#clean:} | tr ':' ' '); do
                grep -wq clean_hook "$RK_SCRIPTS_DIR/mk-$m.sh" 2>/dev/null || usage
            done
            ;&
        shell | cleanall)
            # 只能单独使用的选项
            if [ "$opt" = "$OPTIONS" ]; then
                break
            fi
            error "错误: $opt 不能与其他选项组合使用!"
            ;;
        post-rootfs)
            # 处理 post-rootfs 特殊选项
            if [ "$opt" = "$1" -a -d "$2" ]; then
                OPTIONS=$opt  # 隐藏参数以便后续检查
                break
            fi
            error "错误: $opt 应作为第一个选项并跟随 rootfs 目录!"
            ;;
        *)
            # 检查未处理的选项
            if ! option_check "$CMDS" "$opt"; then
                error "错误: 未处理的选项: $opt"
                usage
            fi
            ;;
    esac
done

# 设置实际的内核版本
export RK_KERNEL_VERSION_RAW=$(kernel_version_raw)
export RK_KERNEL_VERSION="$(kernel_version)"

# 处理特殊命令
case "$OPTIONS" in
    cleanall)
        run_build_hooks clean
        rm -rf "$RK_OUTDIR" "$RK_SDK_DIR/rockdev"
        # 清除记录的板卡选择，下次重新选择
        rm -f "$SELECTED_BOARD_FILE"
        finish_build cleanall
        exit 0 ;;
    clean:*)
        MODULES="$(echo ${OPTIONS#clean:} | tr ':' ' ')"
        for m in $MODULES; do
            "$RK_SCRIPTS_DIR/mk-$m.sh" clean
        done
        finish_build clean - $MODULES
        exit 0 ;;
    post-rootfs)
        shift
        TARGET_DIR="$1"

        source "$RK_POST_HELPER"
        POST_DIR="$RK_OUTDIR/$POST_OS"
        mkdir -p "$POST_DIR"

        touch "$POST_DIR/.stamp_post_start"
        run_post_hooks "$TARGET_DIR"
        touch "$POST_DIR/.stamp_post_finish"

        ln -rsf "$TARGET_DIR" "$POST_DIR/target"
        finish_build post-rootfs

        notice "post-rootfs 阶段更改的文件:"
        cd "$TARGET_DIR"
        find . \( -type f -o -type l \) -cnewer "$POST_DIR/.stamp_post_start" | tee "$POST_DIR/.files_post.txt"
        exit 0 ;;
esac

# 保存最终环境变量
rm -f "$RK_FINAL_ENV"
env > "$RK_FINAL_ENV"
ln -rsf "$RK_FINAL_ENV" "$RK_OUTDIR/"

# 日志记录配置
message "\n=========================================="
message "          最终配置 ($BOARD_OPTION)"
message "=========================================="
env | grep -E "^RK_.*=.+" | grep -vE "PARTITION_[0-9]" | \
    grep -vE "=\"\"$|_DEFAULT=y|^RK_DEFAULT_TARGET|CMDS=" | \
    grep -vE "^RK_CONFIG|_BASE_CFG=|_LINK=|DIR=|_ENV=|_NAME=|_DTB=" | \
    grep -vE "_HELPER=|_SUPPORTS=|_ARM64=|_ARM=|_HOST=" | \
    grep -vE "^RK_ROOTFS_SYSTEM_|^RK_YOCTO_DISPLAY_PLATFORM_" | sort
echo

# 构建前阶段（子模块配置等）
run_build_hooks pre-build $OPTIONS

# 构建阶段（编译等）
run_build_hooks build $OPTIONS

# 构建后阶段（固件打包等）
run_build_hooks post-build $OPTIONS
~~~



确实这样就没有问题了，那我现在还有必要这样改一下吗，好像也没啥必要啊，对吗，可以后面徐徐图之，现在的话其实是没必要的，可以先将大部分的内容来整理一下



~~~shell
#!/bin/bash

if [ -z "$BASH_SOURCE" ]; then
	echo "Not in bash, switching to it..."
	case "${@:-shell}" in
		shell) ./build.sh shell ;;
		*)
			./build.sh $@
			bash
			;;
	esac
fi

usage_clean()
{
	usage_oneline "cleanall" "cleanup all"
	for s in $(grep -rwl clean_hook "$RK_CHIP_SCRIPTS_DIR" \
		"$RK_SCRIPTS_DIR" 2>/dev/null | grep "/mk-" | \
		sed "s/^.*mk-\(.*\).sh/\1/" | grep -v "^all$"); do
		usage_oneline "clean-$s" "cleanup $s"
	done
}

usage()
{
	echo "Usage: $(basename $BASH_SOURCE) [OPTIONS]"
	echo "Available options:"

	run_build_hooks usage

	# Global options
	usage_clean
	usage_oneline "post-rootfs <rootfs dir>" "trigger post-rootfs hook scripts"
	usage_oneline "help" "display this information"
	echo ""
	echo "Default option is '$RK_DEFAULT_TARGET'."

	rm -f "$INITIAL_ENV"
	exit 0
}

# Export global functions
set -a

rk_log()
{
	LOG_COLOR="$1"
	shift
	if [ "$1" = "-n" ]; then
		shift
		LOG_FLAG="-ne"
	else
		LOG_FLAG="-e"
	fi
	echo $LOG_FLAG "\e[${LOG_COLOR}m$@\e[0m"
}

message()
{
	rk_log 36 "$@" # light blue
}

notice()
{
	rk_log 35 "$@" # purple
}

warning()
{
	rk_log 34 "$@" # dark blue
}

error()
{
	rk_log 91 "$@" # light red
}

fatal()
{
	rk_log 31 "$@" # dark red
}

usage_oneline()
{
	printf "%-40s%s\n" "$1" "${*:2}"
}

usage_makefile_oneline()
{
	printf "  %-22s - %s\n" "$(echo "$1" | grep -o "^[^[^:^ ]*")" "${*:2}"
}

finish_build()
{
	notice "Running $(basename "${BASH_SOURCE[1]}") - ${@:-${FUNCNAME[1]}} succeeded."
	cd "$RK_SDK_DIR"
}

load_config()
{
	[ -r "$RK_CONFIG" ] || return 0

	for var in $@; do
		export "$(grep "^$var=" "$RK_CONFIG" | tr -d '"' || true)" \
			&>/dev/null || true
	done
}

check_config()
{
	unset missing
	for var in $@; do
		eval [ -z \"\$$var\" ] || continue

		missing="$missing $var"
	done

	[ "$missing" ] || return 0

	notice "Skipping $(basename "${BASH_SOURCE[1]}") - ${FUNCNAME[1]} for missing configs: $missing."
	return 1
}

kernel_version_raw()
{
	[ -d kernel ] || return 0

	VERSION_KEYS="VERSION PATCHLEVEL"
	VERSION=""

	for k in $VERSION_KEYS; do
		v=$(grep "^$k = " kernel/Makefile | cut -d' ' -f3)
		VERSION=${VERSION:+${VERSION}.}$v
	done
	echo $VERSION
}

kernel_version()
{
	[ -d kernel ] || return 0

	KERNEL_DIR="$(basename "$(realpath kernel)")"
	case "$KERNEL_DIR" in
		kernel-*)
			echo ${KERNEL_DIR#kernel-}
			return 0
			;;
	esac

	kernel_version_raw
}

start_log()
{
	LOG_FILE="$RK_LOG_DIR/${2:-$1_$(date +%F_%H-%M-%S)}.log"
	ln -rsf "$LOG_FILE" "$RK_LOG_DIR/$1.log"
	echo "# $(date +"%F %T")" >> "$LOG_FILE"
	echo "$LOG_FILE"
}

get_toolchain()
{
	MODULE="$1"
	TC_ARCH="${2/arm64/aarch64}"
	TC_VENDOR="${3-none}"
	TC_OS="${4:-linux}"

	MACHINE=$(uname -m)
	if [ "$MACHINE" != x86_64 ]; then
		notice "Using Non-x86 toolchain for $MODULE!" >&2

		if [ "$TC_ARCH" = aarch64 -a "$MACHINE" != aarch64 ]; then
			echo aarch64-linux-gnu-
		elif [ "$TC_ARCH" = arm -a "$MACHINE" != armv7l ]; then
			echo arm-linux-gnueabihf-
		fi
		return 0
	fi

	# RV1126 uses custom toolchain
	if [ "$RK_CHIP_FAMILY" = "rv1126_rv1109" ]; then
		TC_VENDOR=rockchip830
	fi

	TC_DIR="$RK_SDK_DIR/prebuilts/gcc/linux-x86/$TC_ARCH"
	if [ "$TC_VENDOR" ]; then
		TC_PATTERN="$TC_ARCH-$TC_VENDOR-$TC_OS-[^-]*-gcc"
	else
		TC_PATTERN="$TC_ARCH-$TC_OS-[^-]*-gcc"
	fi
	GCC="$(find "$TC_DIR" -name "*gcc" | grep -m 1 "/$TC_PATTERN$" || true)"
	if [ ! -x "$GCC" ]; then
		{
			error "No prebuilt GCC toolchain for $MODULE!"
			error "Arch: $TC_ARCH"
			error "Vendor: $TC_VENDOR"
			error "OS: $TC_OS"
		} >&2
		exit 1
	fi

	echo ${GCC%gcc}
}

ensure_tools()
{
	for t in "$@"; do
		if [ "$RK_ROOTFS_PREFER_PREBUILT_TOOLS" ] || \
			[ "$RK_ROOTFS_PREBUILT_TOOLS" ] || \
			[ ! -e "$t" ]; then
			install -v -D -m 0755 "$RK_TOOLS_DIR/armhf/${t##*/}" "$t"
			continue
		fi

		if [ ! -e "$t" ]; then
			warning "Unable to install $t!"
		fi
	done
}

# For developing shell only

rroot()
{
	cd "$RK_SDK_DIR"
}

rout()
{
	cd "$RK_OUTDIR"
}

rcommon()
{
	cd "$RK_COMMON_DIR"
}

rscript()
{
	cd "$RK_SCRIPTS_DIR"
}

rchip()
{
	cd "$(realpath "$RK_CHIP_DIR")"
}

set +a
# End of global functions

err_handler()
{
	ret=${1:-$?}
	if [ "$ret" -eq 0 ]; then
		return 0
	fi

	fatal "ERROR: Running $BASH_SOURCE - ${2:-${FUNCNAME[1]}} failed!"
	fatal "ERROR: exit code $ret from line ${BASH_LINENO[0]}:"
	fatal "    ${3:-$BASH_COMMAND}"
	fatal "ERROR: call stack:"
	for i in $(seq 1 $((${#FUNCNAME[@]} - 1))); do
		SOURCE="${BASH_SOURCE[$i]}"
		LINE=${BASH_LINENO[$(( $i - 1 ))]}
		fatal "    $(basename "$SOURCE"): ${FUNCNAME[$i]}($LINE)"
	done
	exit $ret
}

# option_check "<supported commands>" <option 1> [option 2] ...
option_check()
{
	CMDS="$1"
	shift

	for opt in $@; do
		for cmd in $CMDS; do
			# NOTE: There might be patterns in commands
			if echo "${opt%%:*}" | grep -qE "^$cmd$"; then
				return 0
			fi
		done
	done
	return 1
}

# hook_check <hook> <stage> <cmd>
hook_check()
{
	case "$2" in
		init | pre-build | build | post-build) ;;
		*) return 0 ;;
	esac

	SCRIPT="$(realpath "$1" --relative-to "$RK_SDK_DIR")"
	CMDS="$(sed -n \
		"s@^RK_${2//-/_}_CMDS[^ ]*\(.*\)\" # $SCRIPT\$@\1@ip" \
		"$RK_PARSED_CMDS")"

	if echo "$CMDS" | grep -wq default; then
		return 0
	fi

	option_check "$CMDS" "$3"
}

# Run specific hook scripts
do_run_hooks()
{
	HOOK_DIR="$1"
	shift

	[ -d "$HOOK_DIR" ] || return 0

	for hook in $(find "$HOOK_DIR" -maxdepth 1 -name "*.sh" | sort); do
		# Ignore unrelated hooks
		hook_check "$hook" "$@" || continue

		if "$hook" "$@"; then
			continue
		else
			HOOK_RET=$?
			err_handler $HOOK_RET \
				"${FUNCNAME[0]} $*" "$hook $*"
			exit $HOOK_RET
		fi
	done
}

run_hooks()
{
	case "${2:-usage}" in
		usage)
			do_run_hooks "$RK_COMMON_DIR/$1" "${@:2}"
			do_run_hooks "$RK_CHIP_DIR/$1" "${@:2}"
			;;
		*)
			# Prefer chips' hooks than the common ones
			do_run_hooks "$RK_CHIP_DIR/$1" "${@:2}"
			do_run_hooks "$RK_COMMON_DIR/$1" "${@:2}"
			;;
	esac
}

# Run build hook scripts for normal stages
run_build_hooks()
{
	# Don't log these stages (either interactive or with useless logs)
	case "$1" in
		init | pre-build | make-* | usage | parse-cmds)
			run_hooks "$RK_BUILD_HOOK_DIR" "$@" || true
			return 0
			;;
	esac

	LOG_FILE="$(start_log "$1")"

	echo -e "# run hook: $@\n" >> "$LOG_FILE"
	run_hooks "$RK_BUILD_HOOK_DIR" "$@" 2>&1 | tee -a "$LOG_FILE"
	HOOK_RET=${PIPESTATUS[0]}
	if [ $HOOK_RET -ne 0 ]; then
		err_handler $HOOK_RET "${FUNCNAME[0]} $*" "$@"
		exit $HOOK_RET
	fi
}

# Run post hook scripts for post-rootfs stage
run_post_hooks()
{
	LOG_FILE="$(start_log post-rootfs)"

	echo -e "# run hook: $@\n" >> "$LOG_FILE"
	run_hooks "$RK_POST_HOOK_DIR" "$@" 2>&1 | tee -a "$LOG_FILE"
	HOOK_RET=${PIPESTATUS[0]}
	if [ $HOOK_RET -ne 0 ]; then
		err_handler $HOOK_RET "${FUNCNAME[0]} $*" "$@"
		exit $HOOK_RET
	fi
}

setup_environments()
{
	export LC_ALL=C

	export RK_SCRIPTS_DIR="$(dirname "$(realpath "$BASH_SOURCE")")"
	export RK_COMMON_DIR="$(realpath "$RK_SCRIPTS_DIR/..")"
	export RK_SDK_DIR="$(realpath "$RK_COMMON_DIR/../../..")"
	export RK_DEVICE_DIR="$RK_SDK_DIR/device/rockchip"
	export RK_CHIPS_DIR="$RK_DEVICE_DIR/.chips"
	export RK_CHIP_DIR="$RK_DEVICE_DIR/.chip"
	export RK_CHIP_SCRIPTS_DIR="$RK_CHIP_DIR/scripts"

	export RK_DEFAULT_TARGET="all"
	export RK_DATA_DIR="$RK_COMMON_DIR/data"
	export RK_TOOLS_DIR="$RK_COMMON_DIR/tools"
	export RK_EXTRA_PARTS_DIR="$RK_COMMON_DIR/extra-parts"
	export RK_KBUILD_DIR="$RK_COMMON_DIR/linux-kbuild"
	export RK_CONFIG_IN="$RK_COMMON_DIR/configs/Config.in"

	export RK_BUILD_HOOK_DIR="build-hooks"
	export RK_POST_HOOK_DIR="post-hooks"

	export RK_BUILD_HELPER="$RK_SCRIPTS_DIR/build-helper"
	export RK_POST_HELPER="$RK_SCRIPTS_DIR/post-helper"
	export RK_PARTITION_HELPER="$RK_SCRIPTS_DIR/partition-helper"

	export RK_OUTDIR="$RK_SDK_DIR/output"
	export RK_EXTRA_PART_OUTDIR="$RK_OUTDIR/extra-parts"
	export RK_SESSION_DIR="$RK_OUTDIR/sessions"
	export RK_SESSION="${RK_SESSION:-$(date +%F_%H-%M-%S)}"
	export RK_LOG_DIR="$RK_SESSION_DIR/$RK_SESSION"
	export RK_LOG_BASE_DIR="$RK_OUTDIR/log"
	export RK_ROCKDEV_DIR="$RK_SDK_DIR/rockdev"
	export RK_FIRMWARE_DIR="$RK_OUTDIR/firmware"
	export RK_CONFIG="$RK_OUTDIR/.config"
	export RK_DEFCONFIG_LINK="$RK_OUTDIR/defconfig"
	export RK_OWNER="$(stat --format %U "$RK_SDK_DIR")"
	export RK_OWNER_UID="$(stat --format %u "$RK_SDK_DIR")"

	RK_PARSED_CMDS="$RK_OUTDIR/.parsed_cmds"
	RK_MAKE_USAGE="$RK_OUTDIR/.make_usage"
}

check_sdk() {
	if ! echo "$RK_SCRIPTS_DIR" | \
		grep -q "device/rockchip/common/scripts$"; then
		fatal "SDK corrupted!"
		error "Running $BASH_SOURCE from $RK_SCRIPTS_DIR:"
		ls --file-type "$RK_SCRIPTS_DIR"
		exit 1
	fi

	"$RK_SCRIPTS_DIR/check-sdk.sh"

	# Detect sudo(root)
	unset RK_SUDO_ROOT
	if [ "$RK_OWNER_UID" -ne 0 ] && [ "${USER:-$(id -un)}" = "root" ]; then
		export RK_SUDO_ROOT=1
		notice "Running within sudo(root) environment!"
		echo
	fi
}

parse_scripts()
{
	mkdir -p "$RK_OUTDIR"

	if [ ! -r "$RK_MAKE_USAGE" ] || \
		[ "$(find "$RK_SCRIPTS_DIR" "$RK_CHIP_DIR" "$RK_CHIP_DIR/" \
			-cnewer "$RK_MAKE_USAGE" 2>/dev/null)" ]; then
		{
			TEMP_FILE=$(mktemp -u)

			for c in $(ls "$RK_CHIPS_DIR"); do
				usage_makefile_oneline "$c" "choose $c"
			done > $TEMP_FILE

			run_build_hooks make-usage >> $TEMP_FILE

			usage_clean | \
				while read LINE; do
					usage_makefile_oneline $LINE
				done >> $TEMP_FILE
			mv $TEMP_FILE "$RK_MAKE_USAGE"
		}&
	fi

	if [ ! -r "$RK_PARSED_CMDS" ] || \
		[ "$(find "$RK_SCRIPTS_DIR" "$RK_CHIP_DIR" "$RK_CHIP_DIR/" \
			-cnewer "$RK_PARSED_CMDS" 2>/dev/null)" ]; then
		{
			TEMP_FILE=$(mktemp -u)
			{
				echo "#!/bin/bash"
				run_build_hooks parse-cmds
			} > $TEMP_FILE
			mv $TEMP_FILE "$RK_PARSED_CMDS"
		}&
	fi

	wait
}

makefile_options()
{
	unset DEBUG

	setup_environments
	check_sdk >&2 || exit 1

	parse_scripts

	case "$1" in
		make-usage) cat "$RK_MAKE_USAGE" ;;
		make-targets) cat "$RK_MAKE_USAGE" | awk '{print $1}' ;;
	esac

	exit 0
}

main()
{
	# Early handler of Makefile options
	case "$@" in
		make-*) makefile_options $@ ;;
	esac

	[ -z "$DEBUG" ] || set -x

	trap 'err_handler' ERR
	set -eE

	# Save intial envionments
	unset INITIAL_SESSION
	INITIAL_ENV=$(mktemp -u)
	env > "$INITIAL_ENV"

	[ "$RK_SESSION" ] || INITIAL_SESSION=1

	# Setup basic environments
	setup_environments

	# Log SDK information
	MANIFEST="$RK_SDK_DIR/.repo/manifest.xml"
	if [ -e "$MANIFEST" ]; then
		if [ ! -L "$MANIFEST" ]; then
			MANIFEST="$RK_SDK_DIR/.repo/manifests/$(grep -o "[^\"]*\.xml" "$MANIFEST")"
		fi
		TAG="$(grep -o "linux-.*-gen-rkr[^.\"]*" "$MANIFEST" | \
			head -n 1 || true)"
		MANIFEST="$(basename "$(realpath "$MANIFEST")")"
		notice "\n############### Rockchip Linux SDK ###############\n"
		notice "Manifest: $MANIFEST"
		if [ "$TAG" ]; then
			notice "Version: $TAG"
		fi
		echo
	fi

	notice -n "Log colors: "
	message -n "message "
	notice -n "notice "
	warning -n "warning "
	error -n "error "
	fatal "fatal"
	echo

	# Check SDK requirements
	check_sdk

	# Check for session validation
	if [ -z "$INITIAL_SESSION" ] && [ ! -d "$RK_LOG_DIR" ]; then
		warning "Session($RK_SESSION) is invalid!"

		export RK_SESSION="$(date +%F_%H-%M-%S)"
		export RK_LOG_DIR="$RK_SESSION_DIR/$RK_SESSION"
		INITIAL_SESSION=1
	fi
	export RK_INITIAL_ENV="$RK_LOG_DIR/initial.env"
	export RK_CUSTOM_ENV="$RK_LOG_DIR/custom.env"
	export RK_FINAL_ENV="$RK_LOG_DIR/final.env"

	mkdir -p "$RK_FIRMWARE_DIR"

	cd "$RK_SDK_DIR"
	[ -f README.md ] || ln -rsf "$RK_COMMON_DIR/README.md" .
	[ -d common ] || ln -rsf "$RK_COMMON_DIR" .

	# TODO: Remove it in the repo manifest.xml
	rm -f envsetup.sh

	OPTIONS="${@:-$RK_DEFAULT_TARGET}"

	# Special handle for chip and defconfig
	# e.g. ./build.sh rk3588:rockchip_defconfig
	for opt in $OPTIONS; do
		if [ -d "$RK_CHIPS_DIR/${opt%%:*}" ]; then
			OPTIONS=$(echo "$OPTIONS" | xargs -n 1 | \
				sed "s/^$opt$/chip:$opt/" | xargs)
		elif echo "$opt" | grep -q "^[0-9a-z_]*_defconfig$"; then
			OPTIONS=$(echo "$OPTIONS" | xargs -n 1 | \
				sed "s/^$opt$/defconfig:$opt/" | xargs)
		fi
	done

	# Parse supported commands
	parse_scripts
	source "$RK_PARSED_CMDS"

	# Options checking
	CMDS="$RK_INIT_CMDS $RK_PRE_BUILD_CMDS $RK_BUILD_CMDS \
		$RK_POST_BUILD_CMDS"
	for opt in $OPTIONS; do
		case "$opt" in
			help | h | -h | --help | usage | \?) usage ;;
			clean-*)
				# Check cleanup module
				MODULE="$(echo ${opt#clean-})"
				grep -wq clean_hook \
					"$RK_SCRIPTS_DIR/mk-$MODULE.sh" \
					"$RK_CHIP_SCRIPTS_DIR/mk-$MODULE.sh" \
					2>/dev/null || usage
				;&
			shell | buildroot-shell | bshell | cleanall)
				# Check single options
				if [ "$opt" = "$OPTIONS" ]; then
					break
				fi

				error "ERROR: $opt cannot combine with other options!"
				;;
			post-rootfs)
				if [ "$opt" = "$1" -a -d "$2" ]; then
					# Hide args from later checks
					OPTIONS=$opt
					break
				fi

				error "ERROR: $opt should be the first option followed by rootfs dir!"
				;;
			*)
				# Make sure that all options are handled
				if option_check "$CMDS" $opt; then
					continue
				fi

				error "ERROR: Unhandled option: $opt"
				;;
		esac

		usage
	done

	# Prepare log dirs
	if [ ! -d "$RK_LOG_DIR" ]; then
		rm -rf "$RK_LOG_BASE_DIR" "$RK_LOG_DIR" "$RK_SESSION_DIR/latest"
		mkdir -p "$RK_LOG_DIR"
		ln -rsf "$RK_SESSION_DIR" "$RK_LOG_BASE_DIR"
		ln -rsf "$RK_LOG_DIR" "$RK_SESSION_DIR/latest"
		message "Log saved at $RK_LOG_DIR"
	fi

	# Drop old logs
	cd "$RK_LOG_BASE_DIR"
	rm -rf $(ls -t | sed '1,10d')
	cd "$RK_SDK_DIR"

	# Save initial envionments
	if [ "$INITIAL_SESSION" ]; then
		rm -f "$RK_INITIAL_ENV"
		cp "$INITIAL_ENV" "$RK_INITIAL_ENV"
		ln -rsf "$RK_INITIAL_ENV" "$RK_OUTDIR/"
	fi
	rm -f "$INITIAL_ENV"

	# Init stage (preparing SDK configs, etc.)
	run_build_hooks init $OPTIONS
	rm -f "$RK_OUTDIR/.tmpconfig*"

	# No need to go further
	CMDS="$RK_PRE_BUILD_CMDS $RK_BUILD_CMDS $RK_POST_BUILD_CMDS \
		cleanall clean-.* post-rootfs"
	option_check "$CMDS" $OPTIONS || return 0

	# Force exporting config environments
	set -a

	# Load config environments
	source "$RK_CONFIG"
	cp "$RK_CONFIG" "$RK_LOG_DIR"
	export RK_KERNEL_VERSION="$(kernel_version)"

	if [ -z "$INITIAL_SESSION" ]; then
		# Inherit session environments
		sed -n 's/^\(RK_.*=\)\(.*\)/\1"\2"/p' "$RK_FINAL_ENV" > \
			"$INITIAL_ENV"
		source "$INITIAL_ENV"
		rm -f "$INITIAL_ENV"
	else
		# Detect and save custom environments

		# Find custom environments
		rm -f "$RK_CUSTOM_ENV"
		for cfg in $(grep "^RK_" "$RK_INITIAL_ENV" || true); do
			env | grep -q "^${cfg//\"/}$" || \
				echo "$cfg" >> "$RK_CUSTOM_ENV"
		done

		# Allow custom environments overriding
		if [ -e "$RK_CUSTOM_ENV" ]; then
			ln -rsf "$RK_CUSTOM_ENV" "$RK_OUTDIR/"

			warning "WARN: Found custom environments:"
			cat "$RK_CUSTOM_ENV"

			warning "Assuming that is expected, please clear them if otherwise."
			read -t 10 -p "Press enter to continue."
			source "$RK_CUSTOM_ENV"

			if grep -q "^RK_KERNEL_VERSION=" "$RK_CUSTOM_ENV"; then
				warning "Custom RK_KERNEL_VERSION ignored!"
			fi

			if grep -q "^RK_ROOTFS_SYSTEM=" "$RK_CUSTOM_ENV"; then
				warning "Custom RK_ROOTFS_SYSTEM ignored!"
				load_config RK_ROOTFS_SYSTEM
			fi
		fi
	fi

	# Parse partition table
	source "$RK_PARTITION_HELPER"
	rk_partition_init

	set +a

	# The real kernel version: 4.4/4.19/5.10/6.1, etc.
	export RK_KERNEL_VERSION_RAW=$(kernel_version_raw)
	export RK_KERNEL_VERSION="$(kernel_version)"

	# Handle special commands
	case "$OPTIONS" in
		cleanall)
			run_build_hooks clean
			rm -rf "$RK_OUTDIR" "$RK_SDK_DIR/rockdev"
			finish_build cleanall
			exit 0 ;;
		clean-*)
			MODULE="$(echo ${OPTIONS#clean-})"
			if [ -x "$RK_CHIP_SCRIPTS_DIR/mk-$MODULE.sh" ]; then
				"$RK_CHIP_SCRIPTS_DIR/mk-$MODULE.sh" clean
			else
				"$RK_SCRIPTS_DIR/mk-$MODULE.sh" clean
			fi
			finish_build clean - $MODULE
			exit 0 ;;
		post-rootfs)
			shift

			touch "$RK_LOG_DIR/.stamp_post_start"
			run_post_hooks "$@"

			TARGET_DIR="$1"
			source "$RK_POST_HELPER"
			POST_DIR="$RK_OUTDIR/$POST_OS"
			mkdir -p "$POST_DIR"
			ln -rsf "$TARGET_DIR" "$POST_DIR/target"
			finish_build post-rootfs

			notice "Files changed in post-rootfs stage:"
			cd "$TARGET_DIR"
			find . \( -type f -o -type l \) \
				-cnewer "$RK_LOG_DIR/.stamp_post_start" | \
				tee "$POST_DIR/.files_post.txt"
			exit 0 ;;
	esac

	# Save final environments
	rm -f "$RK_FINAL_ENV"
	env > "$RK_FINAL_ENV"
	ln -rsf "$RK_FINAL_ENV" "$RK_OUTDIR/"

	# Log configs
	message "\n=========================================="
	message "          Final configs"
	message "=========================================="
	env | grep -E "^RK_.*=.+" | grep -vE "PARTITION_[0-9]" | \
		grep -vE "=\"\"$|_DEFAULT=y|^RK_DEFAULT_TARGET|CMDS=" | \
		grep -vE "^RK_CONFIG|_BASE_CFG=|_LINK=|DIR=|_ENV=|_NAME=|_DTB=" | \
		grep -vE "_HELPER=|_SUPPORTS=|_ARM64=|_ARM=|_HOST=" | \
		grep -vE "^RK_ROOTFS_SYSTEM_|^RK_YOCTO_DISPLAY_PLATFORM_" | sort
	echo

	# Pre-build stage (submodule configuring, etc.)
	run_build_hooks pre-build $OPTIONS

	# Build stage (building, etc.)
	run_build_hooks build $OPTIONS

	# Post-build stage (firmware packing, etc.)
	run_build_hooks post-build $OPTIONS
}

if [ "$0" != "$BASH_SOURCE" ]; then
	# Sourced, executing it directly
	"$BASH_SOURCE" ${@:-shell}
elif [ "$0" == "$BASH_SOURCE" ]; then
	# Executed directly
	main "${@%%:}"
fi

~~~



# 工作记录(星期四)

==摆脱一份幻觉比发现一个真理更能使人明智==



amixer contents

~~~c
    es8388_sound: es8388-sound {
        status = "okay";
        compatible = "rockchip,multicodecs-card";
        rockchip,card-name = "rockchip-es8388";
        hp-det-gpio = <&gpio1 RK_PD5 GPIO_ACTIVE_LOW>;
        io-channels = <&saradc 3>;
        io-channel-names = "adc-detect";
        keyup-threshold-microvolt = <1800000>;
        poll-interval = <100>;
        hp-con-gpio = <&gpio4 RK_PB1 GPIO_ACTIVE_HIGH>;
        rockchip,pre-power-on-delay-ms = <30>;
        rockchip,post-power-down-delay-ms = <40>;
        rockchip,format = "i2s";
        rockchip,mclk-fs = <256>;
        rockchip,cpu = <&sai1>;
        rockchip,codec = <&es8388>;
        rockchip,audio-routing =
            "Headphone", "LOUT1",
            "Headphone", "ROUT1",
            "Speaker", "LOUT2",
            "Speaker", "ROUT2",
            "Headphone", "Headphone Power",
            "Headphone", "Headphone Power",
            "Speaker", "Speaker Power",
            "Speaker", "Speaker Power",
            "LINPUT1", "Main Mic",
            "LINPUT2", "Main Mic",
            "RINPUT1", "Headset Mic",
            "RINPUT2", "Headset Mic";
        pinctrl-names = "default";
        pinctrl-0 = <&hp_det>;
        play-pause-key {
            label = "playpause";
            linux,code = <KEY_PLAYPAUSE>;
            press-threshold-microvolt = <2000>;
        };
    };
~~~





~~~c
dmesg | grep -i "es8388"
dmesg | grep -i "asoc"
dmesg | grep -i "sound"
~~~



# 工作记录(星期五)

==摆脱一份幻觉比发现一个真理更能使人明智==

==当你在凝视深渊的时候，深渊也在凝视你==

==弱小和无知并不是生存的障碍，傲慢才是==

==清醒的活着，或清醒的死去==

==我希望用一生的时间不断深入和学习电子计算机相关的全部知识，将其整理成网络并分享出去，与更多志同道合的朋友共同进步，你清楚的知道，如果做不到这一点，你将永远不快乐==

____

今天的任务应该是没啥了

~~~shell
# 构建Debian根文件系统镜像
build_debian() {
    # 检查RK_DEBIAN配置是否存在，不存在则返回false
    check_config RK_DEBIAN || false

    # 定义镜像输出目录（优先使用传入参数，默认值为RK_OUTDIR/debian）
    IMAGE_DIR="${1:-$RK_OUTDIR/debian}"
    # 定义架构（优先使用RK_DEBIAN_ARCH，默认值为armhf）
    ARCH=${RK_DEBIAN_ARCH:-armhf}

    # 执行Debian环境检查脚本
    "$RK_SCRIPTS_DIR/check-debian.sh"

    # 打印构建开始提示信息
    message "=========================================="
    message "          Start building $RK_DEBIAN_VERSION($ARCH)"
    message "=========================================="

    # 进入debian工作目录
    cd debian || { error "Failed to enter debian directory"; return 1; }

    # 检查基础Debian压缩包是否存在，不存在则构建
    if [ ! -f linaro-$RK_DEBIAN_VERSION-alip-*.tar.gz ]; then
        RELEASE=$RK_DEBIAN_VERSION TARGET=desktop ARCH=$ARCH ./mk-base-debian.sh
        # 创建版本-架构的软链接
        ln -sf linaro-$RK_DEBIAN_VERSION-alip-*.tar.gz linaro-$RK_DEBIAN_VERSION-$ARCH.tar.gz
    fi

    # 定义Debian根文件系统构建脚本名称
    DEBIAN_SCRIPT=mk-rootfs-$RK_DEBIAN_VERSION.sh

    # 如果指定了Debian镜像源，替换脚本中的默认源
    if [ "$RK_DEBIAN_MIRROR" ]; then
        notice "Using mirror source $RK_DEBIAN_MIRROR in $DEBIAN_SCRIPT..."
        sed -i "s#\(http://\)[^/]*#\1$RK_DEBIAN_MIRROR#" "$DEBIAN_SCRIPT"
    fi

    # 执行根文件系统构建脚本（debug版本）
    VERSION=debug ARCH=$ARCH ./$DEBIAN_SCRIPT

    # 制作镜像文件
    ./mk-image.sh

    # 检查post-rootfs日志是否存在，不存在则警告
    if ! [ -r "$RK_LOG_DIR/post-rootfs.log" ]; then
        warning "Building without post-rootfs stage!"
    fi

    # 创建rootfs.ext4软链接（相对路径+强制覆盖）
    ln -rsf "$PWD/linaro-rootfs.img" "$IMAGE_DIR/rootfs.ext4"

    # 完成构建流程
    finish_build build_debian "$@"
}
~~~





~~~c
RELEASE=bookworm TARGET=desktop ARCH=arm64 ./mk-base-debian.sh
~~~



1.改为英文(放弃中文)

2.更新xserver库

3.更新libmali库

4.更新iqfile



![image-20251219165448107](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202512191654231.png)

![image-20251219164956623](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202512191649729.png)

![image-20251219165518992](https://chai-1301855619.cos.ap-beijing.myqcloud.com/202512191655067.png)

~~~shell

### Use Rockchip DRM driver
Section "OutputClass"
    Identifier  "RockchipDRM"
    MatchDriver "rockchip"
    Option      "PrimaryGPU"     "yes"
EndSection

Section "Device"
    Identifier  "Rockchip Graphics"
    Driver      "modesetting"

### Use Rockchip RGA 2D HW accel
#    Option      "AccelMethod"    "exa"

### Use GPU HW accel
    Option      "AccelMethod"    "glamor"

    Option      "DRI"            "2"

### Set to "always" to avoid tearing, could lead to up 50% performance loss
    Option      "FlipFB"         "always"

### Limit flip rate and drop frames for "FlipFB" to reduce performance lost
#    Option      "MaxFlipRate"    "60"

    Option      "NoEDID"         "true"
    Option      "UseGammaLUT"    "true"

### Set virtual screen size (scaled by VOP hardware)
#    Option "VirtualSize" "DSI-1:600x1080"

### Set physical display paddings <top,bottom,left,right>
#    Option "Padding" "DSI-1:180,300,300,540"

EndSection

Section "Screen"
    Identifier  "Default Screen"
    Device      "Rockchip Graphics"
    Monitor     "Default Monitor"
EndSection

### Valid values for rotation are "normal", "left", "right"
Section "Monitor"
    Identifier  "Default Monitor"
    Option      "Rotate"         "normal"
EndSection

~~~







~~~c
gst-launch-1.0 v4l2src device=/dev/video11 ! \video/x-raw,format=NV12,width=800,height=1280,framerate=30/1 ! fpsdisplaysink
~~~



~~~c
root@topeet:~# cat /proc/meminfo | grep Cma
CmaTotal:          16384 kB
CmaAllocated:       2608 kB
CmaReleased:       13776 kB
CmaFree:           13776 kB
root@topeet:~# 
~~~

你的 `CmaTotal` 只有 **16MB** (`16384 kB`)。 对于 RK3576 的 ISP 来说，这点内存**完全不够**，甚至连让 ISP 启动最基础的图像处理都不够，更别提需要大量缓存的 3DNR（3D降噪）功能了。

### 为什么 16MB 不够？

我们可以简单算一笔账：

- 一张 1080P (NV12) 的原始图像大约需要 **3MB**。
- RK3576 的 ISP 流水线通常需要 **4~8 个缓冲区** 来轮转处理图像。
- 开启 3DNR 功能后，还需要额外的内存来存储**前几帧的历史图像**用于对比降噪。
- **计算结果**：光是缓冲区队列就需要 20MB+，而你只有 16MB，系统当然会报 `no bay3d buffer available`（没有可用的 3DNR 缓冲区）。

添加cmdline 的cma=256M 选项即可正常显示,



~~~c
FIRMWARE_VER: 1.0
MACHINE_MODEL: RK3588
MACHINE_ID: 007
MANUFACTURER: RK3588
MAGIC: 0x5041524B
ATAG: 0x00200800
MACHINE: 0xffffffff
CHECK_MASK: 0x80
PWR_HLD: 0,0,A,0,1
TYPE: GPT
GROW_ALIGN: 0
CMDLINE: mtdparts=:0x00002000@0x00004000(uboot),0x00002000@0x00006000(misc),0x00020000@0x00008000(boot),0x00040000@0x00028000(recovery),0x00010000@0x00068000(backup),-@0x00078000(rootfs:grow)
uuid:rootfs=614e0000-0000-4b53-8000-1d28000054a9
uuid:boot=7A3F0000-0000-446A-8000-702F00006273
~~~



amixer -c 0 cset numid=42  1  //MC Boost Gain
amixer -c 0 cset numid=43  7  //ADC PGA Gain
 amixer -c 0 cset numid=41 200  //ADC Capture Volume